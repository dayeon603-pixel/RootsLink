# 2026-05-30 — satellite: SPS 640 (+30), NeuroVoice 1949 (+58)

## [07:00 UTC] Session — satellite

### Cross-project work
- **SPS**: `tests/test_metrics_layerwise_noarch.py` (30 tests, new).
  SPS noarch suite: 640 tests (was 610, +30).
- **NeuroVoice**: 3 new noarch files (+58 tests) — correction_store, federated,
  on_device. NeuroVoice noarch suite: 1949 tests (was 1891, +58).

### No new rootslink work this session
rootslink suite: 230 tests. Satellite-only this session.

---

## [~08:30 UTC] Session — satellite

### Cross-project work
- **SPS**: `tests/test_sps_api_noarch.py` (+30) → 670 tests.
- **NeuroVoice**: `test_sonar_utils_noarch.py` (+20) + `test_confidence_noarch.py` (+18)
  → 1987 tests.

### No new rootslink work this session
rootslink suite: 230 tests. Satellite-only.

---

## [~10:00 UTC] Session — satellite

### Cross-project work
- **SPS**: `tests/test_sps_structuredsens_noarch.py` (+30) → 700 tests.
- **NeuroVoice**: `test_alignment_loss_noarch.py` (+18) + `test_sonar_projector_noarch.py`
  (+20) → 2025 tests.

### No new rootslink work this session
rootslink suite: 230 tests. Satellite-only.

---

## [~12:00 UTC] Session — satellite

### Cross-project work
- **SPS**: `tests/test_adv_report_noarch.py` (30 tests, new) —
  AdversarialSPSReport + RelativeSPSReport fields + summary() branches.
  SPS noarch suite: 730 tests (was 700, +30).
- **NeuroVoice**: 4 new noarch test files (+96 tests) —
  test_audio_denoiser_noarch.py (18) + test_language_router_noarch.py (25) +
  test_lang_pack_manager_noarch.py (28) + test_decoder_heads_noarch.py (25).
  NeuroVoice noarch suite: 2121 tests (was 2025, +96).

### No new rootslink work this session
rootslink suite: 230 tests. Satellite-only.

---

## [~14:00 UTC] Session — satellite

### Cross-project work
- **SPS**: `tests/test_rsps_summary_extended_noarch.py` (+30) → 760 tests.
- **NeuroVoice**: `test_vocabulary_inventory_noarch.py` (+30) +
  `test_phoneme_articulatory_noarch.py` (+30) → 2181 tests.

### No new rootslink work this session
rootslink suite: 230 tests. Satellite-only.

---

## [~15:00 UTC] Session — satellite

### Cross-project work
- **NeuroVoice**: `test_encoder_extended_noarch.py` (+30) → 2211 tests.

### No new rootslink work this session
rootslink suite: 230 tests. Satellite-only.

---

## [~16:00 UTC] Session — satellite

### Cross-project work
- **NeuroVoice**: `test_ctc_head_extended_noarch.py` (+30) → 2241 tests.

### No new rootslink work this session
rootslink suite: 230 tests. Satellite-only.

---

## [~17:00 UTC] Session — satellite

### Cross-project work
- **NeuroVoice**: `test_decoder_extended_noarch.py` (+30) → 2271 tests.

### No new rootslink work this session
rootslink suite: 230 tests. Satellite-only.

---

## [~18:00 UTC] Session — satellite

### Cross-project work
- **NeuroVoice**: `test_pipeline_extended_noarch.py` (+30) → 2301 tests.

### No new rootslink work this session
rootslink suite: 230 tests. Satellite-only.

---

## [~06:00 UTC] Session — satellite

### Cross-project work
- **SPS**: `tests/test_adv_rel_estimators_noarch.py` (30 tests, new) —
  AdversarialSPSEstimator constructor (14): emb/adv estimator identity, config defaulting
  to emb_estimator.config, custom config, two-instance independence, epsilon accessible.
  RelativeSPSEstimator constructor (16): t/arb estimator identity, config defaulting to
  t_estimator.config, custom config, device default "cpu", arb config not leaked.
  SPS noarch suite: 497 tests (was 467, +30).
  Also fixed pyproject.toml build-backend: `setuptools.backends.legacy:build` →
  `setuptools.build_meta` (uv compatibility).
- **NeuroVoice**: `tests/test_serving_inference_noarch.py` (30 tests, new) —
  StreamingDecoder constructor (14): window_samples/hop_samples stored + defaults,
  callbacks None by default, vocoder None by default, _buffer is deque, starts empty,
  _samples_since_last_inference=0, two instances have independent buffers.
  reset() (8): clears buffer, zeroes counter, calls preprocessor.reset_stream(), safe on
  empty buffer, returns None, does not affect window_samples/hop_samples.
  Introspection (8): isclass, __all__ has exactly "StreamingDecoder", push/reset/
  decode_offline/_infer callable, two instances independent.
  NeuroVoice noarch suite: 2331 tests (was 2301, +30).

### No new metaMirage work this session
Sign-flip confirmed. Dashboard final. Satellite-only from this point forward.
