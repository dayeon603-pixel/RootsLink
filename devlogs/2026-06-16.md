# RootsLink Devlog — 2026-06-16

## [12:00 UTC] Session — R248 (Satellite Tracking)

RootsLink is in planning phase. No new code or tests this session.

### SPS R248
- `tests/test_core_source_9_noarch.py` (+30, suite 9,778 → 9,808).
- 9th source pass: `_collect_sensitivities()` full body + `build_adversarial_sps_estimator()` factory.
- Fixed NeuroVoice conftest.py torch/einops/loguru stubs (all NV noarch tests now runnable).

### NeuroVoice R248
- `tests/test_vocoder_source_8_noarch.py` (+30, suite 11,481 → 11,511).
- 8th source pass: `VocoderDiscriminator.__init__()` tail (msd/downsamplers) +
  `VocoderDiscriminator.forward()` complete.

| Repo | Suite | Delta |
|------|-------|-------|
| SPS | 9,808 | +30 |
| NeuroVoice | 11,511 | +30 |
| Combined | 21,319 | +60 |
| metaMirage | post-deadline | — |

---

## [15:00 UTC] Session — R249 (Satellite Tracking)

RootsLink is in planning phase. No new code or tests this session.

### SPS R249
- `tests/test_core_source_10_noarch.py` (+30, suite 9,808 → 9,838).
- 10th source pass for `src/sps/core.py` — `RelativeSPSReport` dataclass fields (all
  14: sps_t/sps_arb/rsps_point/rsps_ci/rsps_ci_excludes_one/mean_sens_t/std_sens_t/
  n_samples_t/mean_sens_arb/std_sens_arb/n_samples_arb/sens_ci_t/loo_t/config) +
  `summary()` method opening (def / > 1.05/0.95 thresholds / MORE stable / ≈ / LESS
  stable interp strings / sig_label YES—CI excludes 1 / no—CI contains 1) +
  `RelativeSPSEstimator` class declaration + docstring (Computes rSPS / Runs two
  SPSEstimators / RelativeSPSReport / delta-method 95% CI / log-Wald interval /
  t_estimator / arb_estimator / config args).

### NeuroVoice R249
- `tests/test_vocoder_source_9_noarch.py` (+30, suite 11,511 → 11,541).
- 9th source pass for `src/models/vocoder.py` — `LPCNetConfig` dataclass fields (all
  8 fields with type annotations + inline docstrings: n_articulatory/lpc_order/
  feature_gru_dim/sample_gru_a_dim/sample_gru_b_dim/n_excitation_categories/
  upsample_factor/mu_law_mu) + `LPCNetVocoder` class declaration + docstring
  opening (title / Architecture / frame-rate block with feature_gru/lpc_head/cond_head
  arrows / sample-rate block with prev_sample/gru_a/gru_b/output_proj arrows /
  LPC synthesis filter label).

| Repo | Suite | Delta |
|------|-------|-------|
| SPS | 9,838 | +30 |
| NeuroVoice | 11,541 | +30 |
| Combined | 21,379 | +60 |
| metaMirage | post-deadline | — |

---

## [18:00 UTC] Session — R250 (Satellite Tracking)

RootsLink is in planning phase. No new code or tests this session.

### SPS R250
- `tests/test_core_source_11_noarch.py` (+30, suite 9,838 → 9,868).
- 11th source pass for `src/sps/core.py` — `RelativeSPSReport.summary()` f-string lines
  (Relative SPS Analysis header / sps_t:.4f / mean_sens_t ± std / sens_ci_t bounds /
  loo_t.get / n_samples_t / SPS_arb label / sps_arb:.4f / mean_sens_arb ± std /
  n_samples_arb / rSPS point estimate / rsps_point:.4f / rsps_ci bounds /
  Significant (CI ≠ 1)) + `RelativeSPSEstimator.__init__` body (t_estimator /
  arb_estimator / config or t_estimator.config + delta_method_rsps_ci import) +
  `estimate()` first half (T pass and T_arb pass result dict extractions).

### NeuroVoice R250
- `tests/test_vocoder_source_10_noarch.py` (+30, suite 11,541 → 11,571).
- 10th source pass for `src/models/vocoder.py` — `LPCNetVocoder.__init__` body
  (signature / self.config / P/A/F/Ga/Gb/Q variable assignments / section comments /
  feature_gru GRU / lpc_head Linear / cond_head Linear / sample_embed Embedding /
  gru_a_cell GRUCell / gru_b_cell GRUCell / output_proj Linear) +
  `_init_weights()` body (xavier_uniform_ / zeros_ lpc_head weight/bias / LPC head
  comment) + `forward()` docstring (Teacher-forced title / target_excitation param /
  T_audio formula / logits key).

| Repo | Suite | Delta |
|------|-------|-------|
| SPS | 9,868 | +30 |
| NeuroVoice | 11,571 | +30 |
| Combined | 21,439 | +60 |
| metaMirage | post-deadline | — |

---

## [06:17 UTC] Session — R251 (Satellite Tracking)

RootsLink is in planning phase. No new code or tests this session.

### SPS R251
- `tests/test_core_source_12_noarch.py` (+30, suite 9,868 → 9,898).
- 12th source pass for `src/sps/core.py` — `RelativeSPSEstimator.estimate()` second half
  (delta_method_rsps_ci call + rsps_ci tuple + rsps_ci_excludes_one + per-sample
  CI/LOO + logger.info + return RelativeSPSReport kwargs sps_t through mean_sens_arb).
- Infrastructure fix: detached-HEAD accumulation resolved across all 5 repos.

### NeuroVoice R251
- `tests/test_vocoder_source_11_noarch.py` (+30, suite 11,571 → 11,601).
- 11th source pass for `src/models/vocoder.py` — `LPCNetVocoder.forward()` full body
  (variable unpacking / frame-rate conditioning / allocation / teacher-forced loop /
  return dict + all inline shape comments).

| Repo | Suite | Delta |
|------|-------|-------|
| SPS | 9,898 | +30 |
| NeuroVoice | 11,601 | +30 |
| Combined | 21,499 | +60 |
| metaMirage | post-deadline | — |

---

## [07:17 UTC] Session — R252 (Satellite Tracking)

RootsLink is in planning phase. No new code or tests this session.

### SPS R252
- `tests/test_core_source_13_noarch.py` (+30, suite 9,898 → 9,928).
- 13th source pass for `src/sps/core.py` — RelativeSPSEstimator.estimate() return
  tail + build_relative_sps_estimator() factory fully covered.

### NeuroVoice R252
- `tests/test_vocoder_source_12_noarch.py` (+30, suite 11,601 → 11,631).
- 12th source pass for `src/models/vocoder.py` — LPCNetVocoder.synthesise_chunk()
  signature + docstring + config/device unpacking + frame-rate conditioning +
  T_chunk/T_audio + state-init block start.

| Repo | Suite | Delta |
|------|-------|-------|
| SPS | 9,928 | +30 |
| NeuroVoice | 11,631 | +30 |
| Combined | 21,559 | +60 |
| metaMirage | post-deadline | — |
