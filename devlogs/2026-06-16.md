# metaMirage Devlog — 2026-06-16

## [00:00 UTC] Session — R244 (Satellite Tracking)

metaMirage deadline (2026-04-16) has passed. Repository is in archive state.

### SPS R244
- `tests/test_core_source_5_noarch.py` (+30, suite 9,658 → 9,688).
- 5th source pass for `src/sps/core.py` — SPSEstimator.estimate() logger.info format
  string + return dict keys (sps/mean_sensitivity/std_sensitivity/n_samples/sens_ci/loo) +
  _get_embeddings() full body + build_sps_estimator() factory + AdversarialSPSReport
  class declaration.

### NeuroVoice R244
- `tests/test_vocoder_source_4_noarch.py` (+30, suite 11,361 → 11,391).
- 4th source pass for `src/models/vocoder.py` — _ResBlock._init_weights() body +
  _pad() static method + forward() full method + HiFiGANVocoder class declaration +
  __init__ start (config/input_conv).

| Repo | Suite | Delta |
|------|-------|-------|
| SPS | 9,688 | +30 |
| NeuroVoice | 11,391 | +30 |
| Combined | 21,079 | +60 |
| metaMirage | post-deadline | — |

---

## [03:00 UTC] Session — R245 (Satellite Tracking)

metaMirage deadline (2026-04-16) has passed. Repository is in archive state.

### SPS R245
- `tests/test_core_source_6_noarch.py` (+30, suite 9,688 → 9,718).
- 6th source pass for `src/sps/core.py` — AdversarialSPSReport docstring extended
  (ratio near 1.0/0.0 descriptions / T_adv maximises / sps_adv<=sps_emb inequalities) +
  field declarations (sps_emb/sps_adv/adv_gap_ratio/mean_sens_*/std_sens_*/n_samples/
  emb_sens_ci/adv_sens_ci/loo_emb/loo_adv/config:Optional) +
  summary() method (def / > 0.95/0.80 thresholds / label strings).

### NeuroVoice R245
- `tests/test_vocoder_source_5_noarch.py` (+30, suite 11,391 → 11,421).
- 5th source pass for `src/models/vocoder.py` — HiFiGANVocoder.__init__ upsampling
  loop (ups/mrfs ModuleLists / ch / for rate,k_size enumerate / ConvTranspose1d /
  ch//=2) + MRF blocks + post_net Sequential + _init_weights() body +
  forward() signature + Synthesise docstring + permute + input_conv + for up,mrf_blocks.

| Repo | Suite | Delta |
|------|-------|-------|
| SPS | 9,718 | +30 |
| NeuroVoice | 11,421 | +30 |
| Combined | 21,139 | +60 |
| metaMirage | post-deadline | — |

---

## [06:00 UTC] Session — R246 (Satellite Tracking)

metaMirage deadline (2026-04-16) has passed. Repository is in archive state.

### SPS R246
- `tests/test_core_source_7_noarch.py` (+30, suite 9,718 → 9,748).
- 7th source pass for `src/sps/core.py` — AdversarialSPSReport.summary() f-string lines
  (SPS_emb label / sps_emb:.4f / mean_sens_emb ± std / emb_sens_ci tuple access /
  loo_emb.get / SPS_adv label / sps_adv:.4f / mean_sens_adv ± std / adv_sens_ci /
  loo_adv.get / gap ratio label / adv_gap_ratio:.4f / ({gap_label}) / N Samples) +
  AdversarialSPSEstimator class declaration + docstring + __init__ body (emb_estimator/adv_estimator
  params + self assignments + config or emb_estimator.config).

### NeuroVoice R246
- `tests/test_vocoder_source_6_noarch.py` (+30, suite 11,421 → 11,451).
- 6th source pass for `src/models/vocoder.py` — HiFiGANVocoder.forward() inner loop
  (leaky_relu / up(x) / T_curr / art_up interpolate / size/mode/align_corners /
  ).permute / xs torch.stack / xs.mean / out post_net / return out) +
  _PeriodDiscriminator class + docstring + __init__
  (period / super / self.period / convs ModuleList / Conv2d 1→32→128→512→1024→1024 / post Conv2d) +
  forward() start (# x:(B,1,T) / B,C,T=x.shape / if T%period!=0 /
  pad=period-(T%period) / F.pad reflect).

| Repo | Suite | Delta |
|------|-------|-------|
| SPS | 9,748 | +30 |
| NeuroVoice | 11,451 | +30 |
| Combined | 21,199 | +60 |
| metaMirage | post-deadline | — |

---

## [09:00 UTC] Session — R247 (Satellite Tracking)

metaMirage deadline (2026-04-16) has passed. Repository is in archive state.

### SPS R247
- `tests/test_core_source_8_noarch.py` (+30, suite 9,748 → 9,778).
- 8th source pass for `src/sps/core.py` — `AdversarialSPSEstimator.estimate()`:
  import inside estimate (bootstrap_ci/loo_spectral_gap) + T_emb pass
  (emb_result/sps_emb/mean_sens_emb/std_sens_emb/n_samples) + T_adv pass
  (adv_result/sps_adv/mean_sens_adv/std_sens_adv) + adv_gap_ratio
  (round/sps_adv/max/sps_emb/1e-12) + CI/LOO dispatch
  (emb_sens_list/adv_sens_list/_collect_sensitivities/bootstrap_ci×2/loo_spectral_gap×2) +
  logger.info format string + return AdversarialSPSReport keyword args
  (sps_emb/sps_adv/adv_gap_ratio/mean_sens_emb/std_sens_emb/mean_sens_adv/std_sens_adv/
  n_samples/emb_sens_ci/adv_sens_ci).

### NeuroVoice R247
- `tests/test_vocoder_source_7_noarch.py` (+30, suite 11,451 → 11,481).
- 7th source pass for `src/models/vocoder.py` — `_PeriodDiscriminator.forward()` body
  (x.view / fmaps init / for conv / leaky_relu(conv(x)) / fmaps.append / post(x) /
  return flatten) + `_ScaleDiscriminator` class + docstring + `__init__`
  (norm/try-except weight_norm / convs ModuleList / all 7 Conv1d norms / post Conv1d) +
  `VocoderDiscriminator` class + docstring (Combined/training-only) +
  `__init__` MPD start (self.mpd / _PeriodDiscriminator list / _ScaleDiscriminator).

| Repo | Suite | Delta |
|------|-------|-------|
| SPS | 9,778 | +30 |
| NeuroVoice | 11,481 | +30 |
| Combined | 21,259 | +60 |
| metaMirage | post-deadline | — |

---

## [12:00 UTC] Session — R248 (Satellite Tracking)

metaMirage deadline (2026-04-16) has passed. Repository is in archive state.

### SPS R248
- `tests/test_core_source_9_noarch.py` (+30, suite 9,778 → 9,808).
- 9th source pass: `_collect_sensitivities()` full body + `build_adversarial_sps_estimator()` factory.
- Fixed NeuroVoice conftest.py torch/einops/loguru stubs.

### NeuroVoice R248
- `tests/test_vocoder_source_8_noarch.py` (+30, suite 11,481 → 11,511).
- 8th source pass: `VocoderDiscriminator.__init__()` tail + full `VocoderDiscriminator.forward()`.

| Repo | Suite | Delta |
|------|-------|-------|
| SPS | 9,808 | +30 |
| NeuroVoice | 11,511 | +30 |
| Combined | 21,319 | +60 |
| metaMirage | post-deadline | — |

---

## [15:00 UTC] Session — R249 (Satellite Tracking)

metaMirage deadline (2026-04-16) has passed. Repository is in archive state.

### SPS R249
- `tests/test_core_source_10_noarch.py` (+30, suite 9,808 → 9,838).
- 10th source pass for `src/sps/core.py` — `RelativeSPSReport` dataclass fields (all
  14: sps_t/sps_arb/rsps_point/rsps_ci/rsps_ci_excludes_one/mean_sens_t/std_sens_t/
  n_samples_t/mean_sens_arb/std_sens_arb/n_samples_arb/sens_ci_t/loo_t/config) +
  `summary()` method opening (def / > 1.05/0.95 thresholds / MORE stable / ≈ / LESS
  stable interp strings / sig_label YES—CI excludes 1 / no—CI contains 1) +
  `RelativeSPSEstimator` class declaration + docstring (Computes rSPS / Runs two
  SPSEstimators / RelativeSPSReport / delta-method 95% CI / log-Wald interval /
  t_estimator / arb_estimator / config args).

### NeuroVoice R249
- `tests/test_vocoder_source_9_noarch.py` (+30, suite 11,511 → 11,541).
- 9th source pass for `src/models/vocoder.py` — `LPCNetConfig` dataclass fields (all
  8 fields with type annotations + inline docstrings: n_articulatory/lpc_order/
  feature_gru_dim/sample_gru_a_dim/sample_gru_b_dim/n_excitation_categories/
  upsample_factor/mu_law_mu) + `LPCNetVocoder` class declaration + docstring
  opening (title / Architecture / frame-rate block with feature_gru/lpc_head/cond_head
  arrows / sample-rate block with prev_sample/gru_a/gru_b/output_proj arrows /
  LPC synthesis filter label).

| Repo | Suite | Delta |
|------|-------|-------|
| SPS | 9,838 | +30 |
| NeuroVoice | 11,541 | +30 |
| Combined | 21,379 | +60 |
| metaMirage | post-deadline | — |

---

## [18:00 UTC] Session — R250 (Satellite Tracking)

metaMirage deadline (2026-04-16) has passed. Repository is in archive state.

### SPS R250
- `tests/test_core_source_11_noarch.py` (+30, suite 9,838 → 9,868).
- 11th source pass for `src/sps/core.py` — `RelativeSPSReport.summary()` f-string lines
  (Relative SPS Analysis header / sps_t:.4f / mean_sens_t ± std / sens_ci_t bounds /
  loo_t.get / n_samples_t / SPS_arb label / sps_arb:.4f / mean_sens_arb ± std /
  n_samples_arb / rSPS point estimate / rsps_point:.4f / rsps_ci bounds /
  Significant (CI ≠ 1)) + `RelativeSPSEstimator.__init__` body (t_estimator /
  arb_estimator / config or t_estimator.config + delta_method_rsps_ci import) +
  `estimate()` first half (T pass and T_arb pass result dict extractions).

### NeuroVoice R250
- `tests/test_vocoder_source_10_noarch.py` (+30, suite 11,541 → 11,571).
- 10th source pass for `src/models/vocoder.py` — `LPCNetVocoder.__init__` body
  (signature / self.config / P/A/F/Ga/Gb/Q variable assignments / section comments /
  feature_gru GRU / lpc_head Linear / cond_head Linear / sample_embed Embedding /
  gru_a_cell GRUCell / gru_b_cell GRUCell / output_proj Linear) +
  `_init_weights()` body (xavier_uniform_ / zeros_ lpc_head weight/bias / LPC head
  comment) + `forward()` docstring (Teacher-forced title / target_excitation param /
  T_audio formula / logits key).

| Repo | Suite | Delta |
|------|-------|-------|
| SPS | 9,868 | +30 |
| NeuroVoice | 11,571 | +30 |
| Combined | 21,439 | +60 |
| metaMirage | post-deadline | — |

---

## [06:17 UTC] Session — R251 (Satellite Tracking)

metaMirage deadline (2026-04-16) has passed. Repository is in archive state.

### SPS R251
- `tests/test_core_source_12_noarch.py` (+30, suite 9,868 → 9,898).
- 12th source pass for `src/sps/core.py` — `RelativeSPSEstimator.estimate()` second half:
  delta_method_rsps_ci call (all 6 emb_*/arb_* kwargs) + rsps_ci tuple +
  rsps_ci_excludes_one + per-sample CI/LOO block (t_sens_list / sens_ci_t /
  loo_t) + logger.info format strings + return RelativeSPSReport keyword args
  (sps_t / sps_arb / rsps_point / rsps_ci / rsps_ci_excludes_one /
  mean_sens_t / std_sens_t / n_samples_t / mean_sens_arb).
- Infrastructure: resolved detached-HEAD accumulation — 50 orphaned commits
  recovered and pushed to origin/main for all 5 repos.

### NeuroVoice R251
- `tests/test_vocoder_source_11_noarch.py` (+30, suite 11,571 → 11,601).
- 11th source pass for `src/models/vocoder.py` — `LPCNetVocoder.forward()` full body:
  variable unpacking (B/T_art/U/Q/Ga/Gb) + frame-rate conditioning
  (feat/lpc_coeff/cond/cond_upsampled + inline shape comments) + allocation
  block (T_audio / logits zeros / ha/hb zeros / prev_targets / shift) +
  teacher-forced loop (for t / sample_embed / inp_a / gru_a_cell / gru_b_cell /
  output_proj / return dict).

| Repo | Suite | Delta |
|------|-------|-------|
| SPS | 9,898 | +30 |
| NeuroVoice | 11,601 | +30 |
| Combined | 21,499 | +60 |
| metaMirage | post-deadline | — |

---

## [07:17 UTC] Session — R252 (Satellite Tracking)

metaMirage deadline (2026-04-16) has passed. Repository is in archive state.

### SPS R252
- `tests/test_core_source_13_noarch.py` (+30, suite 9,898 → 9,928).
- 13th source pass for `src/sps/core.py` — RelativeSPSEstimator.estimate() return
  tail + build_relative_sps_estimator() factory fully covered.

### NeuroVoice R252
- `tests/test_vocoder_source_12_noarch.py` (+30, suite 11,601 → 11,631).
- 12th source pass for `src/models/vocoder.py` — LPCNetVocoder.synthesise_chunk()
  signature + docstring + config/device unpacking + frame-rate conditioning +
  T_chunk/T_audio + state-init block start.

| Repo | Suite | Delta |
|------|-------|-------|
| SPS | 9,928 | +30 |
| NeuroVoice | 11,631 | +30 |
| Combined | 21,559 | +60 |
| metaMirage | post-deadline | — |

---

## [08:17 UTC] Session — R253 (Satellite Tracking)

metaMirage deadline (2026-04-16) has passed. Repository is in archive state.

### SPS R253
- `tests/test_core_source_14_noarch.py` (+30, suite 9,928 → 9,958).
- 14th source pass for `src/sps/core.py` — `RelativeSPSReport` docstring interpretation
  section (rSPS > 1 / rSPS ≈ 1 / rSPS < 1 / log-Wald derivation / moderate n /
  rsps_ci_excludes_one flag / 5% level) + attributes section (sps_t / sps_arb /
  rsps_point / rsps_ci / rsps_ci_excludes_one / mean_sens_t / n_samples_t /
  mean_sens_arb / n_samples_arb / sens_ci_t / loo_t / config) +
  `RelativeSPSEstimator` delta-method derivation (Var(log rSPS) / exponentiating) +
  `estimate()` docstring + `_collect_sensitivities()` unique one-liner docstring.

### NeuroVoice R253
- `tests/test_vocoder_source_13_noarch.py` (+30, suite 11,631 → 11,661).
- 13th source pass for `src/models/vocoder.py` — `LPCNetVocoder.synthesise_chunk()`
  state-init tail (hb zeros / lpc_buf zeros / y[n-1]..y[n-P] comment / prev_q=Q_CENTER) +
  state-restore block (else / ha/hb/lpc_buf/prev_q from state dict) +
  allocation (Q=cfg / Q_CENTER=Q//2 / mu / waveform zeros) +
  frame loop (for frame / a_k / # (P,) / c_frame / # (Ga,) / for i in range(U) /
  t=frame*U+i / # Predict excitation distribution) +
  inner loop first half (prev_t tensor / e_emb=sample_embed / # (1, 8) /
  inp_a squeeze+cat / # (1, 8+Ga) / logits=output_proj / # (1, Q) /
  # Sample from distribution / probs softmax / q_sample multinomial).

| Repo | Suite | Delta |
|------|-------|-------|
| SPS | 9,958 | +30 |
| NeuroVoice | 11,661 | +30 |
| Combined | 21,619 | +60 |
| metaMirage | post-deadline | — |

---

## [09:12 UTC] Session — R254 (Satellite Tracking)

metaMirage deadline (2026-04-16) has passed. Repository is in archive state.

### SPS R254
- `tests/test_core_source_15_noarch.py` (+30, suite 9,958 → 9,988).
- 15th source pass for `src/sps/core.py` — module-level docstring (all Implements lines) +
  `SPSConfig` class declaration + docstring + all 7 field declarations +
  `@field_validator("device")` + `validate_device` signature + `torch.device(v)`.

### NeuroVoice R254
- `tests/test_vocoder_source_14_noarch.py` (+30, suite 11,661 → 11,691).
- 14th source pass for `src/models/vocoder.py` — `synthesise_chunk()` inner loop tail
  (μ-law decode / LPC synthesis / clip / buffer update / return) +
  `mu_law_encode()` static method (def / docstring / body).
- `src/models/vocoder.py` now comprehensively covered across all 14 source passes.

| Repo | Suite | Delta |
|------|-------|-------|
| SPS | 9,988 | +30 |
| NeuroVoice | 11,691 | +30 |
| Combined | 21,679 | +60 |
| metaMirage | post-deadline | — |

---

## [10:16 UTC] Session — R255 (Satellite Tracking)

metaMirage deadline (2026-04-16) has passed. Repository is in archive state.

### SPS R255
- `tests/test_core_source_16_noarch.py` (+30, suite 9,988 → 10,018).
- 16th source pass for `src/sps/core.py` — `StructuredSensitivityEstimator._forward()`
  body branches (hasattr last_hidden_state / return slice / isinstance torch.Tensor /
  dim==3 / raise TypeError) + SPSEstimator converges a.s. (Definition 6) +
  estimate() docstring Returns (Dict with keys / sps / mean_sensitivity /
  std_sensitivity / n_samples) + internal imports (import math / from sps.stats) +
  sens_list.tolist() + build_sps_estimator() Args doc + body kwargs
  (model/transform_family/divergence_fn) + section headers
  (# RelativeSPSReport Def 8 / # RelativeSPSEstimator Def 8).
- First time crossing 10,000 test milestone in SPS.

### NeuroVoice R255
- `tests/test_train_vocoder_source_1_noarch.py` (+30, suite 11,691 → 11,721).
- 1st source pass for `src/training/train_vocoder.py` — module docstring
  (GAN losses formulas / L_G/L_D/L_adv/L_fm/L_mel / paired data note / 30 min note) +
  all imports + VocoderTrainConfig @dataclass(slots=True) + field declarations
  (max_steps/batch_size/lr_g/lr_d/betas/lr_decay+comment/lambda_fm+comment/
  lambda_mel+comment).

| Repo | Suite | Delta |
|------|-------|-------|
| SPS | 10,018 | +30 |
| NeuroVoice | 11,721 | +30 |
| Combined | 21,739 | +60 |
| metaMirage | post-deadline | — |

---

## [11:17 UTC] Session — R256 (Satellite Tracking)

metaMirage deadline (2026-04-16) has passed. Repository is in archive state.

### SPS R256
- `tests/test_core_source_17_noarch.py` (+30, suite 10,018 → 10,048).
- 17th source pass for `src/sps/core.py` — `AdversarialSPSEstimator.estimate()`
  docstring + body section comments + summary() strings + factory Example block +
  RelativeSPS comments + AdversarialSPSReport docstring additions.

### NeuroVoice R256
- `tests/test_train_vocoder_source_2_noarch.py` (+30, suite 11,721 → 11,751).
- 2nd source pass for `src/training/train_vocoder.py` — `VocoderTrainConfig` remaining
  fields + `_mel_spectrogram()` + `_discriminator_loss()` + `_generator_adv_loss()`.
- conftest.py: numpy stub fix.

| Repo | Suite | Delta |
|------|-------|-------|
| SPS | 10,048 | +30 |
| NeuroVoice | 11,751 | +30 |
| Combined | 21,799 | +60 |
| metaMirage | post-deadline | — |

---

## [12:20 UTC] Session — R257 (Satellite Tracking)

metaMirage deadline (2026-04-16) has passed. Repository is in archive state.

### SPS R257
- `tests/test_core_source_18_noarch.py` (+30, suite 10,048 → 10,078).
- 18th source pass for `src/sps/core.py` — `StructuredSensitivityEstimator.estimate()` body +
  `_forward()` docstring + `SPSEstimator.estimate()` body + `build_relative_sps_estimator()` Args.

### NeuroVoice R257
- `tests/test_train_vocoder_source_3_noarch.py` (+30, suite 11,751 → 11,781).
- 3rd source pass for `src/training/train_vocoder.py` — `_feature_matching_loss()` +
  `run_vocoder_training()` signature + docstring + body opening.

| Repo | Suite | Delta |
|------|-------|-------|
| SPS | 10,078 | +30 |
| NeuroVoice | 11,781 | +30 |
| Combined | 21,859 | +60 |
| metaMirage | post-deadline | — |

---

## [13:13 UTC] Session — R258 (Satellite Tracking)

metaMirage deadline (2026-04-16) has passed. Repository is in archive state.

### State at Session Start
- SPS suite: 10,078 | NeuroVoice suite: 11,781
- Phase 0: all repos clean (zero Claude-authored commits)
- Phase 0B: all failure files have **Date:** and **Status:** fields

### SPS R258
- `tests/test_core_source_19_noarch.py` (+30, suite 10,078 → 10,108).
- 19th source pass for `src/sps/core.py` — `RelativeSPSEstimator.estimate()` body code.

### NeuroVoice R258
- `tests/test_train_vocoder_source_4_noarch.py` (+30, suite 11,781 → 11,811).
- 4th source pass for `src/training/train_vocoder.py` — training loop setup.

| Repo | Suite | Delta |
|------|-------|-------|
| SPS | 10,108 | +30 |
| NeuroVoice | 11,811 | +30 |
| Combined | 21,919 | +60 |
| metaMirage | post-deadline | — |

---

## [14:15 UTC] Session — R259 (Satellite Tracking)

metaMirage deadline (2026-04-16) has passed. Repository is in archive state.

### State at Session Start
- SPS suite: 10,108 | NeuroVoice suite: 11,811
- Phase 0: all repos clean (zero Claude-authored commits)
- Phase 0B: all failure files have **Date:** and **Status:** fields

### SPS R259
- `tests/test_core_source_20_noarch.py` (+30, suite 10,108 → 10,138).
- 20th source pass for `src/sps/core.py` — `_collect_sensitivities()` body +
  `build_relative_sps_estimator()` factory. `core.py` fully covered.

### NeuroVoice R259
- `tests/test_train_vocoder_source_5_noarch.py` (+30, suite 11,811 → 11,841).
- 5th source pass for `src/training/train_vocoder.py` — discriminator step +
  generator step (losses / backward / schedulers / global_step / logging).

| Repo | Suite | Delta |
|------|-------|-------|
| SPS | 10,138 | +30 |
| NeuroVoice | 11,841 | +30 |
| Combined | 21,979 | +60 |
| metaMirage | post-deadline | — |

---

## [15:15 UTC] Session — R260 (Satellite Tracking)

metaMirage deadline (2026-04-16) has passed. Repository is in archive state.

### State at Session Start
- SPS suite: 10,138 | NeuroVoice suite: 11,841
- Phase 0: all repos clean (zero Claude-authored commits)
- Phase 0B: all failure files have **Date:** and **Status:** fields

### SPS R260
- `tests/test_stats_source_1_noarch.py` (+30, suite 10,138 → 10,168).
- 1st source pass for `src/sps/stats.py` — module-level docstring + `bootstrap_ci()` full.
- Starts new source file: `src/sps/core.py` (20 passes, complete) → `src/sps/stats.py`.

### NeuroVoice R260
- `tests/test_train_vocoder_source_6_noarch.py` (+30, suite 11,841 → 11,871).
- 6th source pass for `src/training/train_vocoder.py` — val_every/checkpoint_every blocks +
  post-loop tail + `_validate_vocoder()` full + `_set_seed()` start.

| Repo | Suite | Delta |
|------|-------|-------|
| SPS | 10,168 | +30 |
| NeuroVoice | 11,871 | +30 |
| Combined | 22,039 | +60 |
| metaMirage | post-deadline | — |

---

## [16:15 UTC] Session — R261 (Satellite Tracking)

metaMirage deadline (2026-04-16) has passed. Repository is in archive state.

### State at Session Start
- SPS suite: 10,168 | NeuroVoice suite: 11,871
- Phase 0: all repos clean (zero Claude-authored commits)
- Phase 0B: all failure files have **Date:** and **Status:** fields

### SPS R261
- `tests/test_stats_source_2_noarch.py` (+30, suite 10,168 → 10,198).
- 2nd source pass for `src/sps/stats.py` — `loo_spectral_gap()` full +
  `delta_method_rsps_ci()` signature + docstring opening.

### NeuroVoice R261
- `tests/test_train_vocoder_source_7_noarch.py` (+30, suite 11,871 → 11,901).
- 7th (final) source pass for `src/training/train_vocoder.py` — `_set_seed()` tail +
  logger continuation + segment crop block + training loop flow.
  `train_vocoder.py` fully covered (7 passes).

| Repo | Suite | Delta |
|------|-------|-------|
| SPS | 10,198 | +30 |
| NeuroVoice | 11,901 | +30 |
| Combined | 22,099 | +60 |
| metaMirage | post-deadline | — |

---

## [15:14 UTC] Session — R262 (Satellite Tracking)

metaMirage deadline (2026-04-16) has passed. Repository is in archive state.

### State at Session Start
- SPS suite: 10,198 | NeuroVoice suite: 11,901
- Phase 0: all repos clean (zero Claude-authored commits)
- Phase 0B: all failure files have **Date:** and **Status:** fields

### SPS R262
- `tests/test_stats_source_18_noarch.py` (+30, suite 10,198 → 10,228).
- 18th source pass for `src/sps/stats.py` — `delta_method_rsps_ci()` body +
  `loo_batch_spectral_gap()` full (sig/doc/body).

### NeuroVoice R262
- `tests/test_encoder_source_3_noarch.py` (+30, suite 11,901 → 11,931).
- 3rd source pass for `src/models/encoder.py` — module-level docstring + imports +
  `__all__` + config/class docstrings + forward docstrings + inline comments.

| Repo | Suite | Delta |
|------|-------|-------|
| SPS | 10,228 | +30 |
| NeuroVoice | 11,931 | +30 |
| Combined | 22,159 | +60 |
| metaMirage | post-deadline | — |

---

## [16:11 UTC] Session — R263 (Satellite Tracking)

metaMirage deadline (2026-04-16) has passed. Repository is in archive state.

### State at Session Start
- SPS suite: 10,228 | NeuroVoice suite: 11,931
- Phase 0: all repos clean (zero Claude-authored commits)
- Phase 0B: all failure files have **Date:** and **Status:** fields

### SPS R263
- `tests/test_stats_source_19_noarch.py` (+30, suite 10,228 → 10,258).
- 19th source pass for `src/sps/stats.py` — `spectral_gap_report()` full
  (signature / docstring / body).

### NeuroVoice R263
- `tests/test_encoder_source_4_noarch.py` (+30, suite 11,931 → 11,961).
- 4th source pass for `src/models/encoder.py` — remaining inline comments:
  _TCNBlock.forward + NeuralEncoder.__init__ + NeuralEncoder.forward comments +
  module-level docstring output box + References section.

| Repo | Suite | Delta |
|------|-------|-------|
| SPS | 10,258 | +30 |
| NeuroVoice | 11,961 | +30 |
| Combined | 22,219 | +60 |
| metaMirage | post-deadline | — |

---

## [17:12 UTC] Session — R264 (Satellite Tracking)

metaMirage deadline (2026-04-16) has passed. Repository is in archive state.

### State at Session Start
- SPS suite: 10,258 | NeuroVoice suite: 11,961
- Phase 0: all repos clean (zero Claude-authored commits)
- Phase 0B: all failure files have **Date:** and **Status:** fields

### SPS R264
- `tests/test_stats_source_20_noarch.py` (+30, suite 10,258 → 10,288).
- 20th source pass for `src/sps/stats.py` — `compare_architecture_cis()` full
  (signature: arch_gaps dict[str,list[float]] / n_boot=1000 / seed=42 / -> dict[str,dict] /
  docstring: title / runs ``spectral_gap_report`` per arch / Adds ``dominates`` field /
  arch_gaps mapping desc / n_boot passthrough desc / ``"ranking"`` key doc /
  body: reports: dict[str,dict]={} / for arch,gaps in arch_gaps.items() /
  spectral_gap_report(gaps,n_boot=n_boot,seed=seed) / ranking sorted lambda on
  reports[a]["mean"] / reports["ranking"]=ranking type:ignore / return reports) +
  `permutation_test_gap_diff()` (def / gaps_a/gaps_b list[float] / n_perm=10_000 /
  alternative="two-sided" / -> dict / Two-sample permutation test docstring /
  rng=random.Random(seed) / na,nb=len / early return dict / pooled=gaps_a+gaps_b /
  obs_diff=mean_a-mean_b / p_value=(extreme+1)/(n_perm+1) / "alternative":alternative).

### NeuroVoice R264
- `tests/test_encoder_source_5_noarch.py` (+30, suite 11,961 → 11,991).
- 5th source pass for `src/models/encoder.py` — NeuralEncoder.__init__ construction:
  self.input_proj nn.Conv1d(config.in_channels,config.hidden_dim,kernel_size=1) +
  self.tcn nn.ModuleList([_TCNBlock(dim=config.hidden_dim / kernel_size=config.kernel_size /
  dilation=2**i [16sp] / dropout=config.dropout [16sp] / for i in range(config.n_layers)]) +
  self.channel_attn nn.MultiheadAttention(embed_dim=config.hidden_dim /
  num_heads=config.n_attention_heads / dropout=config.dropout [12sp] / batch_first=True) +
  self.attn_norm nn.LayerNorm(config.hidden_dim) +
  self.output_proj nn.Linear(config.hidden_dim,config.output_dim) +
  self.output_norm nn.LayerNorm(config.output_dim) +
  MaskedAutoencoderDecoder.__init__ (encoder_dim=256 / decoder_dim=128 / decoder_depth=2 /
  in_channels=64 / self.mask_token nn.Parameter(torch.zeros(1,1,encoder_dim)) /
  self.proj nn.Linear(encoder_dim,decoder_dim) / TransformerEncoderLayer kwargs:
  d_model=decoder_dim / dim_feedforward=decoder_dim*4 / dropout=0.0 / norm_first=True /
  self.transformer nn.TransformerEncoder(layer,num_layers=decoder_depth,enable_nested_tensor=False) /
  self.head nn.Linear(decoder_dim,in_channels) / nn.init.normal_(self.mask_token,std=0.02)).

| Repo | Suite | Delta |
|------|-------|-------|
| SPS | 10,288 | +30 |
| NeuroVoice | 11,991 | +30 |
| Combined | 22,279 | +60 |
| metaMirage | post-deadline | — |

### Next Steps
- SPS R265: complete `permutation_test_gap_diff()` body (n_total / mean_a / mean_b /
  extreme=0 / for loop rng.shuffle / perm_a=pooled[:na] / perm_b=pooled[na:] /
  perm_diff / two-sided abs check / greater/less branches / return dict all 6 keys).
  `stats.py` fully covered.
- NV R265: 6th (final) source pass for `src/models/encoder.py` — `_TCNBlock.__init__`
  body (padding formula / self.conv Conv1d / self.norm LayerNorm / self.dropout Dropout /
  self.act GELU) + `EncoderConfig` field assignments (7 fields). `encoder.py` fully covered.

---

## [18:24 UTC] Session — R266 (Satellite Tracking)

metaMirage deadline (2026-04-16) has passed. Repository is in archive state.

### State at Session Start
- SPS suite: 10,318 | NeuroVoice suite: 12,021
- Phase 0: all repos clean (zero Claude-authored commits)
- Phase 0B: all failure files have **Date:** and **Status:** fields

### SPS R266
- `tests/test_metrics_source_13_noarch.py` (+30, suite 10,318 → 10,348).
- 13th source pass for `src/sps/metrics.py` — `class SPSReport:` declaration /
  `Attributes:` docstring header / `References` module header /
  `summary()` ternary tail (`"= 1 (indistinguishable)" if abs(...) < 1e-3` /
  `"< 1 (FAILURE: higher semantic sensitivity)"`) /
  `relative_sps()` `semantic_sps: float,` + `arbitrary_sps: float,` params /
  `estimate_arbitrary_sps()` `data_iterator: Iterator[dict[str, torch.Tensor]],` +
  `config: SPSConfig,` + `model: nn.Module,` params /
  `class LayerwiseSPSAnalyzer:` declaration / `def __init__(` signature /
  `transform_family: TransformationFamily,` / `config: SPSConfig,` /
  `def compute_profile(` / `def compute_profile_from_batches(` /
  `target_layer: nn.Module,` + `layer_idx: int,` params /
  `def _find_layers(model: nn.Module) -> list[nn.Module]:` full signature /
  `config: Optional[SPSConfig] = None,` in `full_sps_analysis()` /
  `raise ValueError("Spectral gap requires ... 'embeddings' in the first batch.")` /
  `def _get_embedding_layer(model: nn.Module) -> nn.Embedding:` signature /
  positional ordering assertions (compute_profile before compute_profile_from_batches /
  _find_layers before full_sps_analysis / _get_embedding_layer after full_sps_analysis).

### NeuroVoice R266
- `tests/test_ctc_head_source_3_noarch.py` (+30, suite 12,021 → 12,051).
- 3rd source pass for `src/models/ctc_head.py` — module docstring full text:
  "CTC decoder head for end-to-end EMG → text training." /
  "The head sits on top of the TCN encoder (`NeuralEncoder`) and converts" /
  "encoder latents to log-probabilities over a tokenizer vocabulary plus" /
  "the CTC blank symbol. Training uses `torch.nn.CTCLoss`. Decoding can" /
  "be greedy (fast, used during training validation)" /
  "optional language model rescoring (used at eval time)." /
  "Two tokenizers are supported:" /
  "- CharTokenizer: lowercase letters + space + apostrophe (~30 tokens)." /
  "Simplest possible vocabulary, never has out-of-vocabulary errors," /
  "but the CTC head must learn longer sequences for the same utterance." /
  "- BPETokenizer: byte-pair encoding from a sentencepiece model." /
  "~500-2000 tokens, shorter output sequences, better for the longer" /
  "Gaddy utterances. Requires a trained sentencepiece model file." /
  "For the Tier 2 Gaddy baseline we use CharTokenizer first" /
  "time setup), then can swap in BPE if the character WER plateaus high." +
  `import torch.nn as nn` / `if TYPE_CHECKING:` / `__all__ = [` /
  `"ctc_greedy_decode",` / `class CharTokenizer:` declaration /
  `CharTokenizer` class docstring (full) /
  `def __init__(self, extra_chars: str = "") -> None:` /
  `base = "abcdefghijklmnopqrstuvwxyz '"` / `ordered.append(c)` /
  `self._char_to_idx: dict[str, int] = {` / `def vocab_size(self) -> int:`.

| Repo | Suite | Delta |
|------|-------|-------|
| SPS | 10,348 | +30 |
| NeuroVoice | 12,051 | +30 |
| Combined | 22,399 | +60 |
| metaMirage | post-deadline | — |

### Next Steps
- SPS R267: 14th source pass for `src/sps/metrics.py` — CharTokenizer encode/decode
  method signatures (def encode/decode) + return type annotations + docstring opening
  lines + CTCHeadConfig class decl/docstring + CTCHead class decl/docstring +
  CTCHead.__init__ and forward signatures. Metrics.py still has ~30 uncovered lines.
- NV R267: 4th source pass for `src/models/ctc_head.py` — CharTokenizer encode/decode
  method signatures + return type annotations + for-c-in-text loop + for-idx loop +
  CTCHeadConfig class decl + docstring + CTCHead class decl/docstring/init/forward.
