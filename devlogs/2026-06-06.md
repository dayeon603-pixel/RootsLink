# rootslink Devlog — 2026-06-06

## [00:10 UTC] Session — satellite R43

No new rootslink R&D this session (satellite-only).
- **SPS**: `tests/test_jacobian_29_noarch.py` (+30 noarch tests, suite→3658). Module-level structure of jacobian.py: module docstring claims (Theorem 2 corrected two-part, Definition 5, Corollary 1, four key function names, Kang 2026 reference), __all__ ordering (SpectralGapResult at index 0, length 7, all six exports present, SpectralGapResult precedes directional_derivative_norm), module-level bindings (ForwardFn = Callable[[torch.Tensor], torch.Tensor], logger = logging.getLogger(__name__)), import block (from __future__ / logging / dataclasses / typing → torch / torch.nn → sps.transformations / sps.utils).
- **NeuroVoice**: `tests/test_serving_session_adapt_24_noarch.py` (+30 noarch tests, suite→5361). Import block of session_adapt.py: from __future__ import annotations, from pathlib import Path (forward-compat stub), from typing import Optional, import torch/torch.nn as nn, from torch.optim import AdamW, from torch.utils.data import DataLoader TensorDataset (DataLoader before TensorDataset), from loguru import logger (not bare import loguru); device setup: dev=torch.device(device), pipeline=pipeline.to(dev), calibration tensors .to(dev), device: str="cpu" default, return pipeline.cpu() at both strategy exit points; log messages: all seven logger.info/debug verbatim strings confirmed.

## [02:06 UTC] Session — R44

No new rootslink R&D this session (satellite-only).
- **SPS**: `tests/test_jacobian_30_noarch.py` (+30 noarch tests, suite→3688). SpectralGapResult dataclass internals: @dataclass(slots=True) decorator, all six field names, field(init=False) on mean_* fields, __post_init__ computing mean().item() on all three tensors, __repr__ with :.4f on each mean.
- **NeuroVoice**: `tests/test_serving_session_adapt_25_noarch.py` (+30 noarch tests, suite→5391). _update_norm_stats internals: exact signature (pipeline: nn.Module, calibration_data: torch.Tensor, -> None), bn_layers list comprehension, early-return logger.debug guard, m.train()/m.momentum=None pre-forward, torch.no_grad() + pipeline(calibration_data) forward, m.eval()/m.momentum=0.1 post-forward.
