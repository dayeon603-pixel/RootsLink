# rootslink Devlog — 2026-06-06

## [00:10 UTC] Session — satellite R43

No new rootslink R&D this session (satellite-only).
- **SPS**: `tests/test_jacobian_29_noarch.py` (+30 noarch tests, suite→3658). Module-level structure of jacobian.py: module docstring claims (Theorem 2 corrected two-part, Definition 5, Corollary 1, four key function names, Kang 2026 reference), __all__ ordering (SpectralGapResult at index 0, length 7, all six exports present, SpectralGapResult precedes directional_derivative_norm), module-level bindings (ForwardFn = Callable[[torch.Tensor], torch.Tensor], logger = logging.getLogger(__name__)), import block (from __future__ / logging / dataclasses / typing → torch / torch.nn → sps.transformations / sps.utils).
- **NeuroVoice**: `tests/test_serving_session_adapt_24_noarch.py` (+30 noarch tests, suite→5361). Import block of session_adapt.py: from __future__ import annotations, from pathlib import Path (forward-compat stub), from typing import Optional, import torch/torch.nn as nn, from torch.optim import AdamW, from torch.utils.data import DataLoader TensorDataset (DataLoader before TensorDataset), from loguru import logger (not bare import loguru); device setup: dev=torch.device(device), pipeline=pipeline.to(dev), calibration tensors .to(dev), device: str="cpu" default, return pipeline.cpu() at both strategy exit points; log messages: all seven logger.info/debug verbatim strings confirmed.

## [02:06 UTC] Session — R44

No new rootslink R&D this session (satellite-only).
- **SPS**: `tests/test_jacobian_30_noarch.py` (+30 noarch tests, suite→3688). SpectralGapResult dataclass internals: @dataclass(slots=True) decorator, all six field names, field(init=False) on mean_* fields, __post_init__ computing mean().item() on all three tensors, __repr__ with :.4f on each mean.
- **NeuroVoice**: `tests/test_serving_session_adapt_25_noarch.py` (+30 noarch tests, suite→5391). _update_norm_stats internals: exact signature (pipeline: nn.Module, calibration_data: torch.Tensor, -> None), bn_layers list comprehension, early-return logger.debug guard, m.train()/m.momentum=None pre-forward, torch.no_grad() + pipeline(calibration_data) forward, m.eval()/m.momentum=0.1 post-forward.

## [05:14 UTC] Session — satellite R45

No new rootslink R&D this session (satellite-only).
- **SPS**: `tests/test_jacobian_31_noarch.py` (+30 noarch tests, suite→3718). directional_derivative_norm internals: signature (fn: ForwardFn, x/v: torch.Tensor, *, eps: float = 1e-3, -> torch.Tensor), v_magnitude = v.flatten(start_dim=1).norm(dim=1), v_unit = normalize_directions(v), scalar_fn closure, jvp(scalar_fn, x, v_unit, create_graph=False) → (_, jvp_val), norms = jvp_val.flatten(start_dim=1).norm(dim=1), torch.where(v_magnitude > eps, norms, torch.zeros_like(norms)).
- **NeuroVoice**: `tests/test_serving_session_adapt_26_noarch.py` (+30 noarch tests, suite→5421). adapt_to_session function signature: all seven parameters, keyword-only *, return -> NeuroVoicePipeline, __all__ = ["adapt_to_session"].

## [07:30 UTC] Session — satellite R46

No new rootslink R&D this session (satellite-only).
- **SPS**: `tests/test_jacobian_32_noarch.py` (+30 noarch tests, suite→3748). restricted_operator_norm internals: signature (fn: ForwardFn, x/directions: torch.Tensor, -> torch.Tensor), B/K/seq_len/hidden = directions.shape, norms_per_dir: list[torch.Tensor] = [], for k in range(K) loop, v_k = directions[:, k, :, :], directional_derivative_norm(fn, x, v_k) call, torch.stack(norms_per_dir, dim=1), stacked.max(dim=1).values return.
- **NeuroVoice**: `tests/test_serving_session_adapt_27_noarch.py` (+30 noarch tests, suite→5451). adapt_to_session Strategy 1 body: dev/pipeline device setup, _update_norm_stats call, update_norm_layers_only or calibration_articulatory is None guard, inner None check, "No labels provided" log, return pipeline.cpu() at both exits.

## [05:04 UTC] Session — satellite R47

No new rootslink R&D this session (satellite-only).
- **SPS**: `tests/test_jacobian_33_noarch.py` (+30 noarch tests, suite→3778). adversarial_worst_direction function internals: signature (fn: ForwardFn, x: torch.Tensor, directions: torch.Tensor, -> torch.Tensor), same K-loop body as restricted_operator_norm (B, K, seq_len, hidden unpacking, norms_per_dir list, for k in range(K), v_k = directions[:, k, :, :] slice, directional_derivative_norm(fn, x, v_k) call, norms_per_dir.append(jvp_norm)), stacked = torch.stack(norms_per_dir, dim=1), worst_k = stacked.argmax(dim=1), gather via directions[torch.arange(B, device=x.device), worst_k], return worst_dirs.
- **NeuroVoice**: `tests/test_serving_session_adapt_28_noarch.py` (+30 noarch tests, suite→5481). adapt_to_session Strategy 2 setup: logger.info(f"Fine-tuning on {calibration_neural.shape[0]} calibration utterances for {n_steps} steps."), TensorDataset(calibration_neural.to(dev), calibration_articulatory.to(dev)), DataLoader(dataset, batch_size=min(4, len(dataset)), shuffle=True, drop_last=False), optimiser = AdamW(pipeline.parameters(), lr=lr, weight_decay=0.0), criterion = nn.MSELoss().

## [06:05 UTC] Session — satellite R48

No new rootslink R&D this session (satellite-only; deadline April 16, 2026 passed).
- **SPS**: `tests/test_jacobian_34_noarch.py` (+30 noarch tests, suite→3808). full_spectral_norm function internals: signature (fn: ForwardFn, x: torch.Tensor, n_iter: int = 16, n_probe: int = 8, -> torch.Tensor), B/seq_len/hidden = x.shape unpacking, device = x.device, max_norm = torch.zeros(B, device=device) init, for _ in range(n_probe) loop, v = torch.randn(B, seq_len, hidden, device=device), v = normalize_directions(v), jvp_norm = directional_derivative_norm(fn, x, v), max_norm = torch.maximum(max_norm, jvp_norm), return max_norm.
- **NeuroVoice**: `tests/test_serving_session_adapt_29_noarch.py` (+30 noarch tests, suite→5511). adapt_to_session Strategy 2 training loop: pipeline.train(), step = 0, while step < n_steps outer loop, for neural, art_target in loader: inner loop, if step >= n_steps: break guard, pred = pipeline(neural), T = min(pred.shape[1], art_target.shape[1]), loss = criterion(pred[:, :T, :], art_target[:, :T, :]), optimiser.zero_grad(), loss.backward(), nn.utils.clip_grad_norm_(pipeline.parameters(), 1.0), optimiser.step(), step += 1, step % 50 == 0 progress log with {step}/{n_steps} and {loss.item():.5f}, pipeline.eval(), logger.info("Session adaptation complete."), return pipeline.cpu().

## [07:09 UTC] Session — satellite R49

No new rootslink R&D this session (satellite-only).
- **SPS**: `tests/test_jacobian_35_noarch.py` (+30 noarch tests, suite→3838). spectral_gap function internals: signature (fn: ForwardFn, x: torch.Tensor, directions: torch.Tensor, n_probe_full: int = 16, eps: float = 1e-12, -> SpectralGapResult), x = x.detach().requires_grad_(True) detach-reattach, rest_norm = restricted_operator_norm(fn, x, directions) call, full_norm = full_spectral_norm(fn, x, n_probe=n_probe_full) keyword-arg call, normalized = (1.0 - rest_norm / full_norm.clamp(min=eps)).clamp(0.0, 1.0), SpectralGapResult construction with .detach() on all three tensor args, spectral_gap at __all__[5].
- **NeuroVoice**: `tests/test_serving_inference_3_noarch.py` (+30 noarch tests, suite→5541). serving/inference.py StreamingDecoder: import block (from __future__, collections.deque, typing, numpy, torch, relative pipeline/vocoder/preprocessor imports), push() internals (transform_stream call, for-t-in-range(processed.shape[1]) loop, buffer append, counter increment/decrement, while-condition), _infer() internals (np.stack window assembly, torch.from_numpy unsqueeze to(device), torch.no_grad, squeeze/cpu/numpy, on_articulatory guard+call, vocoder+on_audio guard+call).

## [08:06 UTC] Session — satellite R50

No new R&D this session (satellite-only).
- **SPS**: `tests/test_jacobian_36_noarch.py` (+30 noarch tests, suite→3868). verify_spectral_gap_bound source internals: inline `import math`, exact formula `math.exp(-(1.0 - mean_gap) * lipschitz_constant)`, `float(satisfied)` cast, `max(theorem3, 1e-12)` denominator guard; signature (sps_value: float, lipschitz_constant: float, mean_gap: float, -> dict[str, float]); at __all__[6] (last); return type contracts (all five values are Python float, not bool); sps_value key preserves exact input.
- **NeuroVoice**: `tests/test_serving_inference_4_noarch.py` (+30 noarch tests, suite→5571). serving/inference.py decode_offline internals: self.preprocessor.transform(neural) (non-streaming, distinct from transform_stream in push()), C/T/W shape unpacking, T<W guard with np.pad mode="edge", torch.from_numpy(processed).unsqueeze(0).to(self.device), audio_np: Optional[np.ndarray] = None init, two torch.no_grad() blocks, vocoder branch, return {"articulatory": art_np, "audio": audio_np}; reset() method: _buffer.clear(), _samples_since_last_inference=0, preprocessor.reset_stream().

## [09:10 UTC] Session — satellite R51

No new rootslink R&D this session (satellite-only).
- **SPS**: `tests/test_transformations_2_noarch.py` (+30 noarch tests, suite→3898).
- **NeuroVoice**: `tests/test_finetune_3_noarch.py` (+30 noarch tests, suite→5601).

## [10:15 UTC] Session — satellite R52

No new metaMirage R&D this session (satellite-only; deadline April 16, 2026 passed).
- **SPS**: `tests/test_transformations_3_noarch.py` (+30 noarch tests, suite→3928). SynonymSubstitutionFamily.sample outer structure: B,seq_len,_=embeddings.shape; device=embeddings.device; perturbed_ids=input_ids.clone(); positions=torch.randperm(seq_len).tolist(); n_substituted>=self.config.max_substitutions; tid=input_ids[b,pos].item(); candidates=self.synonym_map.get(int(tid),[]); perturbed_embeddings=self.embedding_layer(perturbed_ids); magnitudes norm; return. Epsilon rejection: valid=magnitudes<=epsilon; valid.view(B,1,1).expand_as(embeddings); torch.where; magnitudes*valid.float(); with torch.no_grad(); n_substituted counter; syn lookup; perturbed_ids[b,pos]=syn. EmbeddingPerturbationFamily._random_orthogonal_directions Gram-Schmidt: flat_dim=seq_len*hidden; torch.randn; torch.zeros_like; for-k-in-range(K); v=raw[:,k,:]; for-j-in-range(k); u=ortho[:,j,:]; proj=(v*u).sum(dim=-1,keepdim=True)*u; v=v-proj; norm.clamp(min=1e-12).
- **NeuroVoice**: `tests/test_finetune_4_noarch.py` (+30 noarch tests, suite→5631). run_finetune training loop: for epoch in range(1,max_epochs+1):; pipeline.train(); neural=batch["neural"].to(dev); target=batch["articulatory"].to(dev); T=min(pred.shape[1],target.shape[1]); loss=mse_criterion(pred,target); pred[:,:,_VOICING_IDX]; (target[:,:,_VOICING_IDX]>0.5).float(); total_loss=loss+voicing_loss_weight*voicing_loss; epoch_loss+=total_loss.item(). Optimizer/warmup/scheduler: optimiser.zero_grad/backward/step; scale=(global_step+1)/warmup_steps; g["lr"]=g["lr"]*scale; scheduler.step(); global_step+=1; epoch_loss/=len(train_loader); pred=pipeline(neural). Validation+early-stopping+checkpoint: val_loss=_validate(...); val_loss<best_val_loss; best_val_loss=val_loss; patience_counter=0/+=1; pipeline.save; patience>=early_stop_patience; best_val_loss=float("inf"); NeuroVoicePipeline.from_checkpoint(...).cpu(); global_step=0.

## [11:05 UTC] Session — satellite R53

No new R&D this session (satellite-only).
- **SPS**: `tests/test_transformations_4_noarch.py` (+30 noarch tests, suite→3958). EmbeddingPerturbationFamily.semantic_directions body: B, seq_len, hidden = embeddings.shape, device = embeddings.device, K = self.config.n_directions, if self.config.use_synonym_directions and self.synonym_map: dispatch, return self._synonym_directions(embeddings, input_ids, K), return self._random_orthogonal_directions(B, seq_len, hidden, K, device). SynonymSubstitutionFamily.semantic_directions body: K=1, all_dirs list init, syn_ids=input_ids.clone(), for-b/for-s loops, tid/candidates lookup, syn_ids[b,s] assignment, no_grad syn_emb, delta=syn_emb-embeddings, delta+=substitution_prob*randn_like, d_norm normalize, append unsqueeze, cat dim=1 return. AdversarialEmbeddingFamily delegation: return self._t_emb.semantic_directions(embeddings, input_ids).
- **NeuroVoice**: `tests/test_finetune_5_noarch.py` (+30 noarch tests, suite→5661). run_finetune log strings: epoch progress f"Epoch {epoch}/{max_epochs} — train: {epoch_loss:.5f} | val: {val_loss:.5f}" (em dash), new-best f"  ↳ New best val loss ({best_val_loss:.5f}). Saved.", early-stop f"Early stopping at epoch {epoch} (patience={early_stop_patience}).", final f"Fine-tuning complete. Best val loss: {best_val_loss:.5f}", startup log with train/val counts. DataLoader construction: train_loader (shuffle=True, num_workers=4, pin_memory=True, drop_last=True), val_loader (shuffle=False, num_workers=2, pin_memory=True). total_steps = max_epochs * len(train_loader). logger.warning for missing pretrain_checkpoint.

## [12:08 UTC] Session — satellite R54

No new R&D this session (satellite-only).
- **SPS**: `tests/test_transformations_5_noarch.py` (+30 noarch tests, suite→3988). AdversarialEmbeddingFamily.sample body: late import `from sps.jacobian import adversarial_worst_direction`, `device = embeddings.device`, `B = embeddings.size(0)`, `directions = self.semantic_directions(embeddings, input_ids)`, `x = embeddings.detach().requires_grad_(True)`, `with torch.enable_grad():`, `v_star = adversarial_worst_direction(self.forward_fn, x, directions)`, `normalize_directions(v_star.detach().flatten(start_dim=1)).view_as(embeddings)`, `alpha = torch.empty(B, device=device).uniform_(0.0, epsilon)`, `perturbed = embeddings + alpha.view(B, 1, 1) * v_star`. EmbeddingPerturbationFamily._synonym_directions body: `all_directions: list[torch.Tensor] = []`, `for _ in range(K):` outer loop, `syn_ids = input_ids.clone()` per-iteration, nested for-b/for-s loops, `candidates = self.synonym_map.get(int(tid), [])`, `with torch.no_grad(): syn_embeddings = self.embedding_layer(syn_ids)`, `delta = syn_embeddings - embeddings`, `noise_scale = self.config.projection_noise_scale`, `all_directions.append(d_norm.unsqueeze(1))`. build_wordnet_synonym_map body: try/except ImportError, `from nltk.corpus import wordnet as wn`, `vocab_size = vocab_size or tokenizer.vocab_size`, `synonym_map: dict[int, list[int]] = {}`, for-token-id loop, `word = tokenizer.decode([token_id]).strip()`, isalpha/len guard, `synonym_tokens: list[int] = []`, `seen: set[str] = {word}`, `lemma.name().replace("_", " ")`, encode/single-token check, logger.info "Built WordNet synonym map".
- **NeuroVoice**: `tests/test_pipeline_noarch.py` (+30 noarch tests, suite→5691). First pass on `src/models/pipeline.py` NeuroVoicePipeline: `__all__ = ["NeuroVoicePipeline"]`, relative imports from `.encoder`/`.decoder`, `from loguru import logger`. `__init__` body: `super().__init__()`, `self.encoder = NeuralEncoder(encoder_config)`, `self.decoder = ArticulatoryDecoder(decoder_config)`, `if freeze_encoder:` → `p.requires_grad_(False)`, `logger.info("Encoder parameters frozen.")`. `forward` body: `latent = self.encoder(neural)`, `art: torch.Tensor = self.decoder(latent, tone_cond)`, `return art`. Checkpoint methods: `load_encoder_weights` (map_location="cpu"/weights_only=True/"encoder_state_dict" not in ckpt/raise KeyError/strict=True), `save` (torch.save 4-key dict: encoder_state_dict/decoder_state_dict/encoder_config/decoder_config), `from_checkpoint` (weights_only=False/cls construction/pipeline.encoder.load_state_dict/pipeline.decoder.load_state_dict/return pipeline).

## [13:10 UTC] Session — satellite R55

No new R&D this session (satellite-only).
- **SPS**: `tests/test_transformations_6_noarch.py` (+30 noarch tests, suite→4018). __init__ body source expressions for all three transformation families: EmbeddingPerturbationFamily.__init__ — `self.embedding_layer = embedding_layer`, `self.synonym_map = synonym_map or {}`, `self.config = config or EmbeddingPerturbationConfig()`, `embedding_layer: nn.Embedding` annotation, `synonym_map: Optional[dict[int, list[int]]] = None`, `config: Optional[EmbeddingPerturbationConfig] = None`, `-> None:`, no ValueError guard. SynonymSubstitutionFamily.__init__ — `if not synonym_map:`, `raise ValueError(`, "SynonymSubstitutionFamily requires a non-empty synonym_map." message, `build_wordnet_synonym_map` hint, plain `self.synonym_map = synonym_map` (no `or {}`), `self.config = config or SynonymSubstitutionConfig()`, `synonym_map: dict[int, list[int]]` non-Optional. AdversarialEmbeddingFamily.__init__ — `self.forward_fn = forward_fn`, `self.embedding_layer = embedding_layer`, `self.synonym_map = synonym_map or {}`, `self.config = config or AdversarialEmbeddingConfig()`, `emb_cfg = EmbeddingPerturbationConfig(`, `n_directions=self.config.n_directions`, `use_synonym_directions=self.config.use_synonym_directions`, `projection_noise_scale=self.config.projection_noise_scale`, `self._t_emb = EmbeddingPerturbationFamily(`, `embedding_layer=embedding_layer` kwarg.
- **NeuroVoice**: `tests/test_pipeline_2_noarch.py` (+30 noarch tests, suite→5721). Remaining pipeline.py body details: load_encoder_weights — `ckpt = torch.load(checkpoint_path,`, `missing, unexpected = self.encoder.load_state_dict(`, `ckpt["encoder_state_dict"], strict=True`, `if missing:`/`if unexpected:` guards, warning log strings, `f"Loaded encoder weights from {checkpoint_path}."` trailing period, `checkpoint_path: Path` annotation. save — `"encoder_config": self.encoder.config`, `"decoder_config": self.decoder.config`, `f"Saved pipeline checkpoint to {path}."`. from_checkpoint — `encoder_config=ckpt["encoder_config"]`, `decoder_config=ckpt["decoder_config"]`, `freeze_encoder=freeze_encoder`, `f"Loaded pipeline from {path}."`, `return pipeline`, `@classmethod`, `freeze_encoder: bool = False` default. Annotations — `freeze_encoder: bool = False` in __init__, `for p in self.encoder.parameters():`, `tone_cond: Optional[torch.Tensor] = None`, `-> torch.Tensor:`, all imported names verified.
