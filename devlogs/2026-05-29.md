# 2026-05-29 — rootslink: score_reasons explainability tests (+22); satellite SPS/NeuroVoice/AlphaNexus

## [00:18 UTC] Session

### State at Session Start
- rootslink: 136 tests (from 2026-05-28 session).
- `score_reasons()` function in `backend/algorithms/opportunity_fit.py` had zero test coverage.

### Work Completed

**`tests/test_score_reasons.py` (22 tests, new)**

Tests for `score_reasons(user, opp) → (reasons: list[str], barriers: list[str])` —
the explainability function that generates human-readable fit summaries.

Note: required `pip install loguru sqlalchemy pydantic-settings` in CI environment
to resolve missing runtime dependencies before tests could run.

TestScoreReasonsStructure (4):
  returns tuple of two lists; all items are strings; ineligible/no-overlap user
  produces non-empty barriers; perfect-fit user produces zero barriers.

TestPositiveReasons (8):
  skill match reason present when Jaccard > 0.3; interest alignment reason present;
  remote mode reason present; free cost reason present; aspiration alignment reason
  present for research type; non-remote → no remote reason; paid → no free reason;
  skill match reason mentions opportunity field tags.

TestBarrierReasons (6):
  eligibility barrier present when education_level fails; no eligibility barrier when
  eligible; tight deadline (<14 days) → deadline barrier; far deadline → no deadline
  barrier; financial barrier + cost > 0 → financial/cost barrier; language barrier
  flag → language barrier message.

TestReasonsNoFalsePositives (4):
  no skill reason when zero skill overlap; no interest reason when no interest overlap;
  no financial barrier when financial flag absent; no language barrier when flag absent.

rootslink suite: 158 tests (was 136, +22).

### Cross-project work
- **SPS**: `tests/test_adversarial_gap_integration_noarch.py` (50 tests). SPS suite: 395 tests.
- **NeuroVoice**: `tests/test_train_vocoder_noarch.py` (25 tests). NeuroVoice suite: 1106 tests.
- **AlphaNexus**: `tests/engine/forecast.test.js` (20 tests). AlphaNexus suite: 138 tests.

### Results / Observations
- All 22 tests passed on first run (after deps installed). No test failures.
- `score_reasons` uses the same internal `_skill_match`, `_eligibility`, etc. helpers
  as `opportunity_fit_score`. The threshold for skill match reason is >0.3 (Jaccard),
  which is more lenient than the scoring function — even a partial match generates
  a reason.
- `test_aspiration_alignment_reason_research`: goals "research and publish science in a
  lab" matches research type keywords ["research", "science", "publish", "lab"] → score
  1.0 → reason generated.

### Hypotheses
- H-reasons1: The barrier list in `score_reasons` is conservative — it only reports
  barriers that directly correspond to the hardcoded checks in the function body, not
  all possible combinations. This means users with complex barrier combinations may see
  incomplete explanations. A future improvement could generate a reason for every
  sub-score below a threshold.

### Next Steps
**rootslink:** Schema validation tests for `backend/schemas/` (UserCreate, UserRead,
  ScoredOpportunity, MatchResult pydantic models).

---

## [~continuation] Session

### State at Session Start
- rootslink: 158 tests (from 00:18 UTC session above).
- `backend/schemas/` (user.py, mentor.py, opportunity.py, matching.py) had zero test
  coverage despite being pure Pydantic v2 models — no torch, no DB, no network.

### Work Completed

**`tests/test_schemas_noarch.py` (42 tests, new)**

Full Pydantic v2 validation tests for all 4 schema modules, loaded via `sys.path.insert`
(backend/ added to path for clean `from schemas.X import Y` imports).

TestUserCreate (12): valid minimal; default mobility_intent=0.5; default language="English";
  default interests=[]; optional region=None; optional goals=None; name=""→ValidationError;
  name>120 chars→ValidationError; invalid email→ValidationError; mobility_intent<0→
  ValidationError; mobility_intent>1→ValidationError; country=""→ValidationError.

TestUserUpdate (3): all-None valid; partial update valid; mobility_intent out-of-range→
  ValidationError.

TestMentorCreate (9): valid minimal; default capacity=3; default availability=True;
  default diaspora_status=False; name=""→ValidationError; capacity=0→ValidationError;
  capacity=21→ValidationError; experience_years=-1→ValidationError; invalid email→
  ValidationError.

TestMentorUpdate (2): all-None valid; capacity>20→ValidationError.

TestOpportunityCreate (7): valid minimal; default impact_score=5.0; default cost=0.0;
  title=""→ValidationError; cost<0→ValidationError; impact_score<1→ValidationError;
  impact_score>10→ValidationError.

TestOpportunityFilter (2): all-None valid; partial filter valid.

TestScoredOpportunity (3): valid construction; link defaults to None; link can be set.

TestMatchResult (1): full nested construction (ScoredOpportunity + ScoredMentor embedded).

TestInteractionCreate (3): minimal valid (only user_id + action); mentor_id set;
  opportunity_id set.

rootslink suite: 200 tests (was 158, +42).

### Cross-project work

**SPS**: `tests/test_stats_extended_noarch.py` (50 tests, new). SPS suite: 445 tests
  (was 395, +50).

**NeuroVoice**: `tests/test_eval_metrics_noarch.py` (30 tests, new) — evaluation/metrics
  noarch. NeuroVoice suite: 1136 tests (was 1106, +30).

### Results / Observations
- All Pydantic v2 ge/le/min_length/max_length validators work as expected. No
  unexpected coercions observed.
- `EmailStr` correctly rejects "not-an-email" and "bad" — requires email-validator
  package (already in backend/requirements.txt).
- `UserRead` and `MentorRead` inherit from their Create counterparts and add id +
  created_at (with `from_attributes=True`). These are tested via the parent class
  validators (shared fields are identical).
- `InteractionCreate.action` has no enum constraint in the model — any string is
  accepted. The constraint ("clicked/saved/applied/...") is enforced at the service
  layer, not the schema layer.

### Hypotheses
- H-schema1: The choice to use `float` rather than `Annotated[float, Field(ge=0, le=1)]`
  for fields like `brain_drain_risk` in MatchResult means the service layer must
  validate the range. This is intentional — MatchResult is an output schema (read-only),
  not an input schema, so validation is expected to happen upstream.
- H-schema2: `mobility_intent` default=0.5 is a reasonable prior — exactly halfway
  between definitely-staying (0) and definitely-leaving (1). A more informative prior
  would use country-level brain drain rates, but 0.5 avoids introducing bias before
  any survey data is collected.

### Next Steps
**rootslink:** Backend API route tests (FastAPI TestClient for `/match`, `/users`,
  `/opportunities` endpoints — requires test DB fixture).
**rootslink:** Brain drain risk score integration test with real pydantic objects.

---

## [~continuation] Session — satellite

### Cross-project work
- **NeuroVoice**: `test_vocabulary_tiers_noarch.py` (58 tests) + `test_tier1_template_noarch.py`
  (51 tests). NeuroVoice suite: 1245 tests (was 1136, +109).

---

## [~continuation] Session

### State at Session Start
- rootslink: 200 tests (from prior session, test_schemas_noarch.py).
- FastAPI route tests (TestClient with SQLite in-memory) not yet written.
- All schemas confirmed valid via test_schemas_noarch.py.

### Work Completed

**`tests/test_api_routes_noarch.py` (30 tests, new)**

FastAPI TestClient route tests against an in-memory SQLite test DB.

Fixture design:
- `Base.metadata.create_all(bind=test_engine)` creates all ORM tables before tests.
- `app.dependency_overrides[get_db] = override_db` replaces production DB with test DB.
- `with patch("backend.main.init_db"):` prevents the lifespan from calling the
  production `init_db()` during TestClient startup.
- `app.dependency_overrides.clear()` + `engine.dispose()` clean up after each test function.

TestHealthEndpoints (3): GET /health → 200; status == 'ok'; GET /info → algorithms list.

TestOverviewStats (5): GET /match/stats/overview → 200; total_users == 0 initially;
  total_mentors == 0; total_opportunities == 0; total_interactions == 0.

TestUserCreate (6): POST /users/ → 201; id is int; name matches payload; email matches;
  duplicate email → 409; missing name → 422.

TestGetUser (3): GET /users/{id} → 200; name correct; GET /users/999 → 404.

TestListUsers (2): GET /users/ → 200 with list; after create, list has 1 item.

TestUpdateUser (4): PATCH /users/{id} → 200; name updated; unset fields unchanged;
  PATCH /users/999 → 404.

TestInteractionLog (5): POST /match/interaction → 201; response has status='logged';
  response has action key; action matches payload; with optional mentor_id + opportunity_id.

TestMatchNotFound (2): GET /match/999 → 404; detail contains "not found".

rootslink suite: 230 tests (was 200, +30).

### Cross-project work
- **NeuroVoice**: `tests/test_articulatory_noarch.py` (25 tests) — NeuroVoice suite: 1270.
- **AlphaNexus**: `tests/widget/widget.test.js` (26 tests) — AlphaNexus suite: 164.

### Results / Observations
- `GET /match/stats/overview` is defined AFTER `GET /{user_id}` in the router file, but
  uses 3 path segments (`/match/stats/overview`) vs 2 (`/match/{user_id}`), so there is
  no conflict — Starlette matches by segment count before type coercion.
- `with patch("backend.main.init_db"):` is the critical guard — without it, TestClient
  startup triggers `init_db()` which creates `rootslink.db` in the working directory.
- The `VALID_USER` fixture uses a unique email per test file; each test function creates
  a fresh in-memory SQLite engine, so there is no cross-test state leakage.

### Hypotheses
- H-api1: The 409 duplicate-email constraint is enforced at the application layer
  (query before insert), not via a DB UNIQUE constraint on the User.email column.
  This means a race condition is theoretically possible in high-concurrency production
  use — a proper UNIQUE index in the migration would be more robust.

### Next Steps
**rootslink:** Brain drain risk score integration test with real pydantic objects.
**rootslink:** Mentor CRUD route tests (backend/routers/mentors.py).

---

## [04:18 UTC] Session — satellite

### Cross-project work
- **SPS**: `tests/test_utils_noarch.py` (19 tests). SPS suite: 464 tests (was 445, +19).
- **NeuroVoice**: `tests/test_lang_registry_noarch.py` (72 tests). NeuroVoice suite: 1342 tests (was 1270, +72).

### No new rootslink work this session
rootslink suite: 230 tests. Satellite-only this session.

---

## [~05:30 UTC] Session — satellite

### Cross-project work
- **NeuroVoice**: audio module noarch tests — preprocessing (28 tests), VAD (22 tests),
  beamformer (25 tests). NeuroVoice suite: 1417 tests (was 1342, +75).

### No new rootslink work this session
rootslink suite: 230 tests. Satellite-only this session.

---

## [06:25 UTC] Session — satellite

### Cross-project work
- **NeuroVoice**: 6 new noarch test suites (+167 tests) + Python 3.11 slots fix.
  NeuroVoice suite: 1584 tests (was 1417, +167).

### No new rootslink work this session
rootslink suite: 230 tests. Satellite-only this session.
Next: vocabulary_inventory noarch tests; sonar_utils noarch tests (NeuroVoice).

---

## [~07:30 UTC] Session — satellite

### Cross-project work
- **NeuroVoice**: 7 new noarch test suites (+154 tests) — beam_decoder (59), ble_bridge (27),
  code_switch (25), cosyvoice (14), kokoro (8), piper (8), voice_bank (13).
  NeuroVoice suite: 1738 tests (was 1584, +154).

---

## [08:30 UTC] Session — satellite

### Cross-project work
- **SPS**: `tests/test_transformations_extended_noarch.py` (31 tests, new) —
  AdversarialEmbeddingFamily init + _t_emb delegation, family field storage,
  config slots=True immutability, TransformationFamily ABC interface.
  SPS suite: 495 tests (was 464, +31).

### No new rootslink work this session
rootslink suite: 230 tests. Satellite-only this session.

---

## [~10:00 UTC] Session — satellite

### Cross-project work
- **SPS**: `tests/test_sps_config_noarch.py` (25 tests) — SPSConfig pydantic v2
  BaseModel. SPS suite: 520 tests (was 495, +25).
- **NeuroVoice**: `tests/test_encoder_tcn_noarch.py` (25) + `tests/test_vocoder_hifigan_noarch.py`
  (25) — TCN padding/dilation/RF arithmetic; HiFi-GAN VocoderConfig invariants,
  _ResBlock._pad, VocoderDiscriminator structure. NeuroVoice suite: 1788 (was 1738, +50).

### No new rootslink work this session
rootslink suite: 230 tests. Satellite-only this session.
