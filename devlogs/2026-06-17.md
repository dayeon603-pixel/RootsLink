# metaMirage Devlog — 2026-06-17

## [00:00 UTC] Session — R271 (Satellite Tracking)

metaMirage deadline (2026-04-16) has passed. Repository is in archive state.

### State at Session Start
- SPS suite: 10,468 | NeuroVoice suite: 12,171
- Phase 0: all repos clean (zero Claude-authored commits)
- Phase 0B: all failure files have **Date:** and **Status:** fields

### SPS R271
- `tests/test_jacobian_source_1_noarch.py` (+30, suite 10,468 → 10,498).
- 1st source pass for `src/sps/jacobian.py` — module docstring title
  ("Jacobian-based analysis: restricted operator norm and semantic spectral gap.") +
  "Implements Theorem 2 (corrected, two-part) and Definition 5 / Corollary 1." +
  Key functions section (restricted_operator_norm / full_spectral_norm /
  spectral_gap / verify_spectral_gap_bound entries with descriptions) +
  References block ("Kang (2026) Theorem 2 (corrected), Definition 5, Corollary 1.") +
  full import block + `logger` + `ForwardFn = Callable[[torch.Tensor], torch.Tensor]` +
  `SpectralGapResult` `@dataclass(slots=True)` declaration +
  docstring Attributes section (all 6 attributes with E_x[] descriptions) +
  `mean_*: float = field(init=False)` declarations (×3) +
  `__post_init__` body opening: `self.mean_restricted_norm = self.restricted_norm.mean().item()`.

### NeuroVoice R271
- `tests/test_pretrain_source_3_noarch.py` (+30, suite 12,171 → 12,201).
- 3rd source pass for `src/training/pretrain.py` — `val_loader` DataLoader +
  Models block (encoder / mae_decoder) + params + AdamW optimiser +
  OneCycleLR scheduler + `global_step = 0` / `best_val_loss = float("inf")` +
  logger.info startup message + while loop opening
  (encoder.train / mae_decoder.train / for batch / batch["neural"] / batch["mask"]).

| Repo | Suite | Delta |
|------|-------|-------|
| SPS | 10,498 | +30 |
| NeuroVoice | 12,201 | +30 |
| Combined | 22,699 | +60 |
| metaMirage | post-deadline | — |

## [07:30 UTC] Session — R272 (Satellite Tracking)

metaMirage deadline (2026-04-16) has passed. Repository is in archive state.

### State at Session Start
- SPS suite: 10,498 | NeuroVoice suite: 12,201
- Phase 0: all repos clean (zero Claude-authored commits)
- Phase 0B: all failure files have **Date:** and **Status:** fields

### SPS R272
- `tests/test_jacobian_source_2_noarch.py` (+30, suite 10,498 → 10,528).
- 2nd source pass for `src/sps/jacobian.py` — `SpectralGapResult.__post_init__()`
  tail (`self.mean_full_norm = self.full_spectral_norm.mean().item()` /
  `self.mean_gap = self.normalized_gap.mean().item()`) +
  docstring Attributes mean_full_norm / mean_gap entries +
  `__repr__()` full f-string +
  `directional_derivative_norm()` full function.

### NeuroVoice R272
- `tests/test_pretrain_source_4_noarch.py` (+30, suite 12,201 → 12,231).
- 4th source pass for `src/training/pretrain.py` — Forward + MAE loss +
  Backward + checkpoint + final save + `_masked_mse()` body.

| Repo | Suite | Delta |
|------|-------|-------|
| SPS | 10,528 | +30 |
| NeuroVoice | 12,231 | +30 |
| Combined | 22,759 | +60 |
| metaMirage | post-deadline | — |

## [15:00 UTC] Session — R273 (Satellite Tracking)

metaMirage deadline (2026-04-16) has passed. Repository is in archive state.

### State at Session Start
- SPS suite: 10,528 | NeuroVoice suite: 12,231
- Phase 0: all repos clean (zero Claude-authored commits)
- Phase 0B: all failure files have **Date:** and **Status:** fields

### SPS R273
- `tests/test_jacobian_source_3_noarch.py` (+30, suite 10,528 → 10,558).
- 3rd source pass for `src/sps/jacobian.py` — `restricted_operator_norm()` complete.

### NeuroVoice R273
- `tests/test_pretrain_source_5_noarch.py` (+30, suite 12,231 → 12,261).
- 5th source pass for `src/training/pretrain.py` — `_evaluate()` function complete.

| Repo | Suite | Delta |
|------|-------|-------|
| SPS | 10,558 | +30 |
| NeuroVoice | 12,261 | +30 |
| Combined | 22,819 | +60 |
| metaMirage | post-deadline | — |

## [15:20 UTC] Session — R274 (Satellite Tracking)

metaMirage deadline (2026-04-16) has passed. Repository is in archive state.

### State at Session Start
- SPS suite: 10,558 | NeuroVoice suite: 12,261
- Phase 0: all repos clean (zero Claude-authored commits)
- Phase 0B: all failure files have **Date:** and **Status:** fields

### SPS R274
- `tests/test_jacobian_source_9_noarch.py` (+30, suite 10,558 → 10,588).
- 9th source pass for `src/sps/jacobian.py` — `adversarial_worst_direction()` complete.

### NeuroVoice R274
- `tests/test_training_finetune_source_5_noarch.py` (+30, suite 12,261 → 12,291).
- 5th source pass for `src/training/finetune.py` — `_validate()` function complete.

| Repo | Suite | Delta |
|------|-------|-------|
| SPS | 10,588 | +30 |
| NeuroVoice | 12,291 | +30 |
| Combined | 22,879 | +60 |
| metaMirage | post-deadline | — |

## [10:30 UTC] Session — R275 (Satellite Tracking)

metaMirage deadline (2026-04-16) has passed. Repository is in archive state.

### State at Session Start
- SPS suite: 10,588 | NeuroVoice suite: 12,291
- Phase 0: all repos clean (zero Claude-authored commits)
- Phase 0B: all failure files have **Date:** and **Status:** fields

### SPS R275
- `tests/test_jacobian_source_10_noarch.py` (+30, suite 10,588 → 10,618).
- 10th source pass for `src/sps/jacobian.py` — `full_spectral_norm()` function:
  section header + `__all__` entry + function def +
  params (`fn: ForwardFn` / `x: torch.Tensor` / `n_iter: int = 16` / `n_probe: int = 8`) +
  return `-> torch.Tensor:` +
  docstring (title `Estimate sigma_max(Jf(x)) via randomized power iteration...` /
  upper-bound property / tight-high-prob / Jacobian-avoided /
  Algorithm: probe Uniform(S^{D-1}) / JVP / max-over-probes /
  Args fn/x/n_iter(unused/API-consistency)/n_probe / Returns shape (B,)) +
  body (`B, seq_len, hidden = x.shape` / `device = x.device` /
  `max_norm = torch.zeros(B, device=device)` / `for _ in range(n_probe):` /
  `v = torch.randn(B, seq_len, hidden, device=device)` / `v = normalize_directions(v)` /
  `jvp_norm = directional_derivative_norm(fn, x, v)` /
  `max_norm = torch.maximum(max_norm, jvp_norm)` / `return max_norm`).

### NeuroVoice R275
- `tests/test_pretrain_source_6_noarch.py` (+30, suite 12,291 → 12,321).
- 6th source pass for `src/training/pretrain.py` — module docstring full body +
  `run_pretrain()` exact default parameter values:
  Module docstring (`Masked Autoencoder pre-training loop.` title /
  `Trains NeuralEncoder + MaskedAutoencoderDecoder on unlabelled neural` /
  decoder-discarded note / `Training objective:` / reconstruct-signal /
  `Loss = mean squared error over masked timesteps only (following MAE).` /
  `L = (1 / |M|) Σ_{t ∈ M} ||x_t - x̂_t||²_2` formula /
  `where M is the set of masked timestep indices` / `x̂_t is the reconstruction.`) +
  signature defaults (`decoder_dim: int = 128` / `decoder_depth: int = 2` /
  `max_steps: int = 100_000` / `warmup_steps: int = 2_000` / `batch_size: int = 32` /
  `lr: float = 1e-4` / `weight_decay: float = 0.05` / `val_split: float = 0.05` /
  `checkpoint_every: int = 5_000` / `seed: int = 42`) +
  extras (`grad_clip: float = 1.0` / `checkpoint_dir: Path = Path("checkpoints/pretrain")` /
  `device: str = "cuda" if torch.cuda.is_available() else "cpu"` /
  `*,` keyword-only marker / `-> NeuralEncoder:` / `checkpoint_dir.mkdir(...)`).

| Repo | Suite | Delta |
|------|-------|-------|
| SPS | 10,618 | +30 |
| NeuroVoice | 12,321 | +30 |
| Combined | 22,939 | +60 |
| metaMirage | post-deadline | — |

### Next Steps
- SPS R276: 11th source pass for `src/sps/jacobian.py` — `spectral_gap()`
  function: signature + docstring (Definition 5 / gamma-bar formula /
  near-1 strong-separation / near-0 failure /
  Args fn/x/directions/n_probe_full/eps / Returns SpectralGapResult) +
  body (`x.detach().requires_grad_(True)` /
  `rest_norm = restricted_operator_norm(fn, x, directions)` /
  `full_norm = full_spectral_norm(fn, x, n_probe=n_probe_full)` /
  `normalized = (1.0 - rest_norm / full_norm.clamp(min=eps)).clamp(0.0, 1.0)` /
  `return SpectralGapResult(restricted_norm=rest_norm.detach(), ...)`).
- NV R276: 7th source pass for `src/training/pretrain.py` — `run_pretrain()` body
  details: set_seed / mkdir / dev / data-split comment /
  `n_val = max(1, int(len(dataset) * val_split))` / `n_train = len(dataset) - n_val` /
  `torch.utils.data.random_split(dataset, [n_train, n_val], generator=...)` /
  `train_loader = DataLoader(train_ds, ..., shuffle=True, num_workers=4, ...)` /
  `val_loader = DataLoader(val_ds, ..., shuffle=False, num_workers=2, ...)`.

## [12:00 UTC] Session — R276 (Satellite Tracking)

metaMirage deadline (2026-04-16) has passed. Repository is in archive state.

### State at Session Start
- SPS suite: 10,618 | NeuroVoice suite: 12,321
- Phase 0: all repos clean (zero Claude-authored commits)
- Phase 0B: all failure files have **Date:** and **Status:** fields

### SPS R276
- `tests/test_jacobian_source_11_noarch.py` (+30, suite 10,618 → 10,648).
- 11th source pass for `src/sps/jacobian.py` — `spectral_gap()` function:
  section header (`# Definition 5: Semantic Spectral Gap`) +
  function def + params (`fn: ForwardFn` / `x: torch.Tensor` /
  `directions: torch.Tensor` / `n_probe_full: int = 16` / `eps: float = 1e-12`) +
  return annotation (`-> SpectralGapResult:`) +
  docstring (title / gamma-bar formula / near-1/near-0 interpretations /
  Args fn/x/directions/n_probe_full/eps / Returns SpectralGapResult) +
  body (`x = x.detach().requires_grad_(True)` /
  `rest_norm = restricted_operator_norm(fn, x, directions)` /
  `full_norm = full_spectral_norm(fn, x, n_probe=n_probe_full)` /
  `# Normalize: gamma-bar = 1 - restricted / full` /
  `# Clamp to [0, 1] to handle estimation noise in full_norm` /
  `normalized = (1.0 - rest_norm / full_norm.clamp(min=eps)).clamp(0.0, 1.0)` /
  `return SpectralGapResult(restricted_norm=rest_norm.detach(),` /
  `full_spectral_norm=full_norm.detach(), normalized_gap=normalized.detach())`).

### NeuroVoice R276
- `tests/test_pretrain_source_7_noarch.py` (+30, suite 12,321 → 12,351).
- 7th source pass for `src/training/pretrain.py` — `run_pretrain()` body opening:
  `set_seed(seed)` / `dev = torch.device(device)` /
  `# ── Data split` section header /
  `n_val = max(1, int(len(dataset) * val_split))` /
  `n_train = len(dataset) - n_val` /
  `train_ds, val_ds = torch.utils.data.random_split(dataset, [n_train, n_val], generator=...)` /
  `train_loader = DataLoader(train_ds, batch_size=batch_size, shuffle=True,` /
  `num_workers=4, pin_memory=True, drop_last=True)` /
  `val_loader   = DataLoader(val_ds,   batch_size=batch_size, shuffle=False,` /
  `num_workers=2, pin_memory=True)` /
  Models block: `encoder = NeuralEncoder(encoder_config).to(dev)` /
  `mae_decoder = MaskedAutoencoderDecoder(encoder_dim=encoder_config.output_dim, ...)` /
  `params = list(encoder.parameters()) + list(mae_decoder.parameters())`.

| Repo | Suite | Delta |
|------|-------|-------|
| SPS | 10,648 | +30 |
| NeuroVoice | 12,351 | +30 |
| Combined | 22,999 | +60 |
| metaMirage | post-deadline | — |

### Next Steps
- SPS R277: 12th source pass for `src/sps/jacobian.py` — `verify_spectral_gap_bound()`:
  signature + docstring (Corollary 1 bound / parameters / dict return) +
  body (`import math` / `lower = math.exp(...)` / `theorem3 = math.exp(-lipschitz_constant)` /
  `satisfied = sps_value >= lower - 1e-6` / `return {...}`).
- NV R277: 8th source pass for `src/training/pretrain.py` — training loop body:
  `# ── Training loop` / `global_step = 0` / `best_val_loss = float("inf")` /
  `logger.info(f"Starting MAE pre-training: ...")` /
  `while global_step < max_steps:` / `encoder.train()` / `mae_decoder.train()` /
  `for batch in train_loader:` / `if global_step >= max_steps:` / `break`.

## [13:30 UTC] Session — R277 (Satellite Tracking)

metaMirage deadline (2026-04-16) has passed. Repository is in archive state.

### State at Session Start
- SPS suite: 10,648 | NeuroVoice suite: 12,351
- Phase 0: all repos clean (zero Claude-authored commits)
- Phase 0B: all failure files have **Date:** and **Status:** fields

### SPS R277
- `tests/test_jacobian_source_12_noarch.py` (+30, suite 10,648 → 10,678).
- 12th source pass for `src/sps/jacobian.py` — `verify_spectral_gap_bound()` function:
  section header (`# Corollary 1: numerical verification`) +
  function def + params (`sps_value: float,` / `lipschitz_constant: float,` / `mean_gap: float,`) +
  return annotation (`-> dict[str, float]:`) +
  docstring (title `Numerically verify the Spectral Gap Stability Bound (Corollary 1).` /
  `Checks whether: SPS_eps(f) >= exp(-(1 - gamma_0) * L)` /
  `and reports the tightness of the bound.` /
  Args sps_value / lipschitz_constant / mean_gap /
  Returns dict with keys: lower_bound / theorem3_bound / bound_satisfied / improvement_factor) +
  body (`import math` / `lower = math.exp(-(1.0 - mean_gap) * lipschitz_constant)` /
  `theorem3 = math.exp(-lipschitz_constant)` /
  `satisfied = sps_value >= lower - 1e-6` / `# small tolerance` /
  `return {"lower_bound": lower, "theorem3_bound": theorem3,` /
  `"bound_satisfied": float(satisfied), "improvement_factor": lower / max(theorem3, 1e-12),}`).

### NeuroVoice R277
- `tests/test_pretrain_source_8_noarch.py` (+30, suite 12,351 → 12,381).
- 8th source pass for `src/training/pretrain.py` — training loop body:
  `# ── Training loop` section header / `global_step = 0` / `best_val_loss = float("inf")` /
  `logger.info(f"Starting MAE pre-training: {n_train} train / {n_val} val windows, ...")` /
  `while global_step < max_steps:` / `encoder.train()` / `mae_decoder.train()` /
  `for batch in train_loader:` / `if global_step >= max_steps: break` /
  `neural: torch.Tensor = batch["neural"].to(dev)` / `# (B, C, T)` /
  `mask: torch.Tensor = batch["mask"].to(dev)` / `# (B, T)` /
  `# Forward` / `latent = encoder(neural)` / `# (B, T, D)` /
  `recon = mae_decoder(latent, mask)` / `# (B, T, C)` /
  `# MAE loss: MSE over masked positions only` /
  `target = neural.permute(0, 2, 1)` / `# (B, T, C)` /
  `loss = _masked_mse(recon, target, mask)` / `# Backward` /
  `optimiser.zero_grad()` / `loss.backward()` /
  `nn.utils.clip_grad_norm_(params, grad_clip)` / `global_step += 1`.

| Repo | Suite | Delta |
|------|-------|-------|
| SPS | 10,678 | +30 |
| NeuroVoice | 12,381 | +30 |
| Combined | 23,059 | +60 |
| metaMirage | post-deadline | — |

### Next Steps
- SPS R278: 1st source pass for `src/sps/stats.py` — module docstring + imports +
  `__all__` + `bootstrap_ci()` signature + docstring opening.
- NV R278: 9th source pass for `src/training/pretrain.py` — optimiser + scheduler
  setup + step logging.

## [14:00 UTC] Session — R278 (Satellite Tracking)

metaMirage deadline (2026-04-16) has passed. Repository is in archive state.

### SPS R278
- `tests/test_jacobian_16_noarch.py` (+30, suite 10,678 → 10,708).
- Noarch importlib/getsource pass 16 for `src/sps/jacobian.py` — `full_spectral_norm()` body + `adversarial_worst_direction()` body source checks.

### NeuroVoice R278
- `tests/test_serving_session_adapt_11_noarch.py` (+30, suite 12,381 → 12,411).
- Noarch importlib/getsource pass 11 for `src/serving/session_adapt.py` — `adapt_to_session()` body source checks.

| Repo | Suite | Delta |
|------|-------|-------|
| SPS | 10,708 | +30 |
| NeuroVoice | 12,411 | +30 |
| Combined | 23,119 | +60 |
| metaMirage | post-deadline | — |

### Next Steps
- SPS R279: 1st source pass for `src/sps/stats.py`.
- NV R279: 9th source pass for `src/training/pretrain.py` — optimiser + scheduler setup.

## [15:17 UTC] Session — R279 (Satellite Tracking)

metaMirage deadline (2026-04-16) has passed. Repository is in archive state.

### State at Session Start
- SPS suite: 10,708 | NeuroVoice suite: 12,411
- Phase 0: all repos clean (zero Claude-authored commits)
- Phase 0B: all failure files have **Date:** and **Status:** fields

### SPS R279
- `tests/test_stats_source_22_noarch.py` (+30, suite 10,708 → 10,738).
- 22nd source pass for `src/sps/stats.py` — body expressions via importlib/getsource:
  `TestLooBatchBodyGetsource` (10): `n = len(per_batch_means)` / `if n < 3:` /
  early-return `"stable": False, "n_batches": n` / `total = sum(per_batch_means)` /
  `loo_means = [(total - g) / (n - 1) for g in per_batch_means]` /
  `grand_mean = total / n` /
  `worst_idx = max(range(n), key=lambda i: abs(loo_means[i] - grand_mean))` /
  `"n_batches": n,` / `"grand_mean": round(grand_mean, 6),` / `"worst_batch": worst_idx,`.
  `TestSpectralGapReportGetsource` (10): full bootstrap_ci call / loo_spectral_gap call /
  `loo.get("stable", False)` / `loo.get("loo_range", 0.0)` /
  return keys: mean / ci_lo / ci_hi / `"ci_width": round(ci_hi - ci_lo, 6),` /
  loo_stable / `"n": len(per_sample_gaps),`.
  `TestCompareArchCisGetsource` (10): `reports: dict[str, dict] = {}` /
  `for arch, gaps in arch_gaps.items():` /
  `reports[arch] = spectral_gap_report(...)` /
  `ranking = sorted(reports.keys(),...)` / `key=lambda a: reports[a]["mean"]` /
  `reverse=True` / `reports["ranking"] = ranking` /
  `# type: ignore[assignment]` / `arch_gaps.items()` / `return reports`.

### NeuroVoice R279
- `tests/test_pretrain_source_9_noarch.py` (+30, suite 12,411 → 12,441).
- 9th source pass for `src/training/pretrain.py` — optimiser + scheduler setup +
  training-step logistics + final save, all via ast `_function("run_pretrain")`:
  `TestRunPretrainOptimiserSetupFunctionScope` (10): params list concat /
  `optimiser = AdamW(params, lr=lr, weight_decay=weight_decay, betas=(0.9, 0.95))` /
  `# One-cycle LR with warmup` / `steps_per_epoch = len(train_loader)` /
  `total_epochs = max(1, max_steps // steps_per_epoch)` /
  `scheduler = OneCycleLR(` / `max_lr=lr,` / `total_steps=max_steps,` /
  `pct_start=warmup_steps / max_steps,` / `anneal_strategy="cos",`.
  `TestRunPretrainStepAndCheckpointFunctionScope` (10): `optimiser.step()` /
  `scheduler.step()` / `if global_step % 500 == 0:` /
  `f"Step {global_step}/{max_steps} — train loss: {loss.item():.5f}"` /
  `if global_step % checkpoint_every == 0:` /
  `val_loss = _evaluate(encoder, mae_decoder, val_loader, dev)` /
  `f"Step {global_step} — val loss: {val_loss:.5f}"` /
  `if val_loss < best_val_loss:` / `best_val_loss = val_loss` /
  `checkpoint_dir / "best_encoder.pt",`.
  `TestRunPretrainFinalSaveAndReturnFunctionScope` (10):
  `f"  ↳ New best val loss. Saved encoder."` / `# ── Save final` /
  `checkpoint_dir / "final_encoder.pt",` /
  `f"Pre-training complete. Best val loss: {best_val_loss:.5f}"` /
  `return encoder.cpu()` / `"encoder_state_dict": encoder.state_dict(),` /
  `"step": global_step}` / `torch.save(` / `set_seed(seed)` /
  `checkpoint_dir.mkdir(parents=True, exist_ok=True)`.

| Repo | Suite | Delta |
|------|-------|-------|
| SPS | 10,738 | +30 |
| NeuroVoice | 12,441 | +30 |
| Combined | 23,179 | +60 |
| metaMirage | post-deadline | — |

### Next Steps
- SPS R280: 23rd source pass for `src/sps/stats.py` — loo_batch_spectral_gap docstring
  residuals + spectral_gap_report docstring + compare_architecture_cis docstring
  + permutation_test_gap_diff signature details via getsource.
- NV R280: 10th source pass for `src/training/pretrain.py` — `set_seed()` body +
  `_masked_mse()` docstring + `_evaluate()` docstring via inspect.getsource with mocked imports.

## [16:14 UTC] Session — R280 (Satellite Tracking)

metaMirage deadline (2026-04-16) has passed. Repository is in archive state.

### State at Session Start
- SPS suite: 10,738 | NeuroVoice suite: 12,441
- Phase 0: all repos clean (zero Claude-authored commits)
- Phase 0B: all failure files have **Date:** and **Status:** fields

### SPS R280
- `tests/test_stats_source_23_noarch.py` (+30, suite 10,738 → 10,768).
- 23rd source pass for `src/sps/stats.py` — docstring residuals via importlib/getsource:
  `TestLooBatchDocstringGetsource` (10): title ("Leave-one-batch-out (LOO) stability
  diagnostic for multi-batch runs.") / "Designed for the planned cross-architecture
  experiment where each batch" / "is an independent draw of (B, seq_len) input samples." /
  "checks whether removing any single batch flips the overall mean by more" /
  "signalling that one batch is a statistical outlier." /
  "Per-batch mean spectral gap values.  Each entry is the mean" /
  "gamma-bar(f, T; X_b) computed over the samples in batch b." /
  `n_batches     — number of batches` / `grand_mean    — mean over all batches` /
  `worst_batch   — index of the batch whose removal changes the mean most`.
  `TestSpectralGapReportDocstringGetsource` (10): title ("Consolidated report: bootstrap CI
  + LOO stability for a spectral gap estimate.") /
  "Combines ``bootstrap_ci`` and ``loo_spectral_gap`` into a single call," /
  "matching the reporting format used in Kang (2026) Table 3." /
  "Per-sample normalized spectral gap values." / "Bootstrap resamples (default 1000)." /
  "RNG seed for reproducibility." / "CI coverage level (default 0.05 → 95% CI)." /
  `mean          — sample mean of per_sample_gaps` /
  `loo_stable    — True if LOO range < 0.05` /
  `loo_range     — range of LOO means (0.0 if n < 3)`.
  `TestCompareArchAndPermSigGetsource` (10): "Side-by-side spectral gap report for multiple
  architectures." / "Adds a ``dominates`` field" /
  "indicating which architecture has the strictly higher mean gap (i.e. lower" /
  "Bootstrap resamples passed through to ``spectral_gap_report``." /
  '``"ranking"`` lists architectures sorted from highest to lowest mean gap.' +
  `def permutation_test_gap_diff(` / `gaps_a: list[float],` / `gaps_b: list[float],` /
  `n_perm: int = 10_000,` / `alternative: str = "two-sided",`.

### NeuroVoice R280
- `tests/test_pretrain_source_10_noarch.py` (+30, suite 12,441 → 12,471).
- 10th source pass for `src/training/pretrain.py` — helper function verification:
  `TestSetSeedBodyGetsource` (10, via inspect.getsource with mocked imports):
  `def set_seed(seed: int = 42) -> None:` / `-> None:` / `seed: int = 42` /
  `random.seed(seed)` / `np.random.seed(seed)` / `torch.manual_seed(seed)` /
  `torch.cuda.manual_seed_all(seed)` / `torch.backends.cudnn.deterministic = True` /
  ordering: random before numpy / torch.manual_seed before cuda.manual_seed_all.
  `TestMaskedMseDocstringGetsource` (10, via inspect.getsource with mocked imports):
  "MSE loss over masked positions only." / `recon :` / `target :` / `mask :` /
  "Reconstructed signal, shape (B, T, C)." / "Ground truth signal, shape (B, T, C)." /
  "Boolean mask, shape (B, T). True = masked." / `torch.Tensor` / "Scalar loss." /
  `Returns\n    -------`.
  `TestEvaluateBodyAst` (10, via ast._function — decorator prevents getsource):
  `encoder.eval()` / `mae_decoder.eval()` / `total_loss = 0.0` / `n_batches = 0` /
  `for batch in loader:` / `neural = batch["neural"].to(device)` /
  `latent = encoder(neural)` / `recon  = mae_decoder(latent, mask)` /
  `total_loss += _masked_mse(recon, target, mask).item()` /
  `return total_loss / max(n_batches, 1)`.

| Repo | Suite | Delta |
|------|-------|-------|
| SPS | 10,768 | +30 |
| NeuroVoice | 12,471 | +30 |
| Combined | 23,239 | +60 |
| metaMirage | post-deadline | — |

### Next Steps
- SPS R281: 24th source pass for `src/sps/stats.py` — `permutation_test_gap_diff()`
  docstring via getsource: title ("Two-sample permutation test for the difference in mean
  spectral gap.") / "Tests H0: mean(gaps_a) == mean(gaps_b) by repeatedly permuting group" /
  "labels and computing the fraction of permuted differences at least as extreme" /
  "pooled permutation (label-shuffling) approach" / "valid even when the two samples have
  very different sizes" / param descs (gaps_a/gaps_b/n_perm/seed/alternative) /
  return keys (observed_diff / p_value / n_perm / n_a / n_b / alternative).
- NV R281: 11th source pass for `src/training/pretrain.py` — `_masked_mse()` body
  via inspect.getsource with mocked imports: `diff = (recon - target) ** 2` /
  `diff = diff.mean(dim=-1)` / `masked_loss = (diff * mask.float()).sum() / ...` /
  `mask.float().sum().clamp(min=1)` / `return masked_loss`.

## [17:13 UTC] Session — R281 (Satellite Tracking)

metaMirage deadline (2026-04-16) has passed. Repository is in archive state.

### State at Session Start
- SPS suite: 10,768 | NeuroVoice suite: 12,471
- Phase 0: all repos clean (zero Claude-authored commits)
- Phase 0B: all failure files have **Date:** and **Status:** fields

### SPS R281
- `tests/test_stats_source_24_noarch.py` (+30, suite 10,768 → 10,798).
- 24th source pass for `src/sps/stats.py` — `permutation_test_gap_diff()` docstring + body via getsource:
  `TestPermutationDocstringGetsource` (10): title ("Two-sample permutation test for the difference
  in mean spectral gap.") / H0 statement ("Tests H0: mean(gaps_a) == mean(gaps_b) by repeatedly
  permuting group") / labels+fraction / extreme-as-observed / pooled-label-shuffling /
  bootstrap-valid-sizes / sizes / param gaps_a_gaps_b / n_perm / seed.
  `TestPermutationAlternativeAndReturnGetsource` (10): alternative two-sided/greater/less /
  dict-with-keys / observed_diff key / p_value key / n_perm key / n_a_n_b key /
  alternative key / return annotation `-> dict:` / seed default.
  `TestPermutationBodyGetsource` (10): `pooled = gaps_a + gaps_b` / `obs_diff = mean_a - mean_b` /
  `extreme = 0` / `for _ in range(n_perm):` / `rng.shuffle(pooled)` /
  `perm_a = pooled[:na]` / `perm_b = pooled[na:]` /
  `if alternative == "two-sided":` / `if abs(perm_diff) >= abs(obs_diff):` /
  `p_value = (extreme + 1) / (n_perm + 1)`.

### NeuroVoice R281
- `tests/test_pretrain_source_11_noarch.py` (+30, suite 12,471 → 12,501).
- 11th source pass for `src/training/pretrain.py` — `_masked_mse()` body+sig via getsource
  + `run_pretrain()` structural comments via ast._function:
  `TestMaskedMseBodyGetsource` (10): `diff = (recon - target) ** 2` / `diff = diff.mean(dim=-1)` /
  `masked_loss = (diff * mask.float()).sum() / mask.float().sum().clamp(min=1)` /
  `return masked_loss` / `.clamp(min=1)` / `mask.float()` / `(B, T, C)` / `(B, T)` /
  `mean over channels` / `** 2`.
  `TestMaskedMseSignatureGetsource` (10): `def _masked_mse(` / `recon: torch.Tensor,` /
  `target: torch.Tensor,` / `mask: torch.Tensor,` / `) -> torch.Tensor:` /
  ordering: recon→target / target→mask / mask→annotation / fn-name→recon / annotation→body.
  `TestRunPretrainBodyCommentsAst` (10): `# ── Data split` / `# ── Models` /
  `# ── Training loop` / `# ── Save final` / `# Forward` /
  `# MAE loss: MSE over masked positions only` /
  `# neural shape: (B, C, T) → rearrange to (B, T, C) for comparison` /
  `# Backward` / `# One-cycle LR with warmup` / `# (B, C, T)`.

| Repo | Suite | Delta |
|------|-------|-------|
| SPS | 10,798 | +30 |
| NeuroVoice | 12,501 | +30 |
| Combined | 23,299 | +60 |
| metaMirage | post-deadline | — |

### Next Steps
- SPS R282: 25th source pass for `src/sps/stats.py` — `permutation_test_gap_diff()` body
  residuals via getsource: `rng = random.Random(seed)` / `na, nb = len(gaps_a), len(gaps_b)` /
  early-return on empty / `n_total = na + nb` / `mean_a = sum(gaps_a) / na` /
  `mean_b = sum(gaps_b) / nb` / `elif alternative == "greater":` / `else:  # "less"` /
  `# +1 smoothing avoids p=0` / return dict keys `"n_a": na, "n_b": nb, "alternative"`.
- NV R282: 12th source pass for `src/training/pretrain.py` — `run_pretrain()` docstring
  parameter descriptions via getsource: dataset / encoder_config / decoder_dim /
  decoder_depth / max_steps / warmup_steps / batch_size / lr / weight_decay / grad_clip.

## [18:09 UTC] Session — R282 (Satellite Tracking)

metaMirage deadline (2026-04-16) has passed. Repository is in archive state.

### State at Session Start
- SPS suite: 10,798 | NeuroVoice suite: 12,501
- Phase 0: all repos clean (zero Claude-authored commits)
- Phase 0B: all failure files have **Date:** and **Status:** fields

### SPS R282
- `tests/test_stats_source_25_noarch.py` (+30, suite 10,798 → 10,828).
- 25th source pass for `src/sps/stats.py` — `permutation_test_gap_diff()` body residuals
  + branch bodies + ordering via getsource:
  `TestPermutationBodyResidualsGetsource` (10): `rng = random.Random(seed)` /
  `na, nb = len(gaps_a), len(gaps_b)` / `if na == 0 or nb == 0:` /
  `n_total = na + nb` / `mean_a = sum(gaps_a) / na` / `mean_b = sum(gaps_b) / nb` /
  `elif alternative == "greater":` / `else:  # "less"` /
  `# +1 smoothing avoids p=0` / `"alternative": alternative,`.
  `TestPermutationBranchBodiesGetsource` (10): `perm_diff = sum(perm_a) / na - sum(perm_b) / nb` /
  `if perm_diff >= obs_diff:` / `if perm_diff <= obs_diff:` / `extreme += 1` /
  early-return `{"observed_diff": 0.0, "p_value": 1.0, "n_perm": n_perm,` /
  early-return `"n_a": na, "n_b": nb, "alternative": alternative}` /
  `"observed_diff": round(obs_diff, 6),` / `"p_value": round(p_value, 6),` /
  `"n_perm": n_perm,` / `"n_b": nb,`.
  `TestPermutationOrderingGetsource` (10): rng before pooled / na_nb before pooled /
  mean_a before obs_diff / obs_diff before extreme / for-loop before shuffle /
  perm_a before perm_b / two-sided before elif-greater / elif-greater before else-less /
  p_value before return-dict / empty-check before pooled.

### NeuroVoice R282
- `tests/test_pretrain_source_12_noarch.py` (+30, suite 12,501 → 12,531).
- 12th source pass for `src/training/pretrain.py` — `run_pretrain()` docstring
  parameter descriptions via inspect.getsource (with mocked imports):
  `TestRunPretrainDocstringFirstParamsGetsource` (10): `dataset :` /
  `PretrainDataset of unlabelled neural windows.` / `encoder_config :` /
  `Architecture config for NeuralEncoder.` / `decoder_dim :` /
  `Hidden dim of the MAE reconstruction decoder.` / `decoder_depth :` /
  `Number of transformer layers in the reconstruction decoder.` /
  `max_steps :` / `Total training steps.`.
  `TestRunPretrainDocstringSecondParamsGetsource` (10): `warmup_steps :` /
  `Linear LR warmup steps.` / `batch_size :` / `Training batch size.` /
  `lr :` / `Peak learning rate.` / `weight_decay :` / `AdamW weight decay.` /
  `grad_clip :` / `Max gradient norm.`.
  `TestRunPretrainDocstringThirdParamsGetsource` (10): `val_split :` /
  `Fraction of data held out for validation.` / `checkpoint_every :` /
  `Steps between checkpoints.` / `checkpoint_dir :` /
  `Directory to save checkpoints.` / `device :` / `'cuda' or 'cpu'.` /
  `seed :` / `Random seed.`.

| Repo | Suite | Delta |
|------|-------|-------|
| SPS | 10,828 | +30 |
| NeuroVoice | 12,531 | +30 |
| Combined | 23,359 | +60 |
| metaMirage | post-deadline | — |

### Next Steps
- SPS R283: 26th source pass for `src/sps/stats.py` — `bootstrap_ci()` body residuals
  via getsource: `rng = random.Random(seed)` / `n = len(values)` /
  `if n == 0: return 0.0, 0.0, 0.0` / `boot_means = sorted(...)` /
  `sum(rng.choices(values, k=n)) / n for _ in range(n_boot)` /
  `lo_idx = int((alpha / 2) * n_boot)` / `hi_idx = int((1 - alpha / 2) * n_boot)` /
  `round(sum(values) / n, 6)` / `round(boot_means[lo_idx], 6)` /
  `round(boot_means[hi_idx], 6)`.
- NV R283: 13th source pass for `src/training/pretrain.py` — `run_pretrain()` docstring
  Returns section + function title via getsource: title (`Run masked autoencoder pre-training.`) /
  Parameters section header / Returns section header (`Returns\n    -------`) /
  `NeuralEncoder` in Returns / `Pre-trained encoder (on CPU), ready for fine-tuning.` /
  ordering: dataset before encoder_config / encoder_config before decoder_dim /
  decoder_dim before max_steps / warmup_steps before batch_size /
  grad_clip before val_split / seed last param.
