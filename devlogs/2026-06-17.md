# metaMirage Devlog — 2026-06-17

## [00:00 UTC] Session — R271 (Satellite Tracking)

metaMirage deadline (2026-04-16) has passed. Repository is in archive state.

### State at Session Start
- SPS suite: 10,468 | NeuroVoice suite: 12,171
- Phase 0: all repos clean (zero Claude-authored commits)
- Phase 0B: all failure files have **Date:** and **Status:** fields

### SPS R271
- `tests/test_jacobian_source_1_noarch.py` (+30, suite 10,468 → 10,498).
- 1st source pass for `src/sps/jacobian.py` — module docstring title
  ("Jacobian-based analysis: restricted operator norm and semantic spectral gap.") +
  "Implements Theorem 2 (corrected, two-part) and Definition 5 / Corollary 1." +
  Key functions section (restricted_operator_norm / full_spectral_norm /
  spectral_gap / verify_spectral_gap_bound entries with descriptions) +
  References block ("Kang (2026) Theorem 2 (corrected), Definition 5, Corollary 1.") +
  full import block + `logger` + `ForwardFn = Callable[[torch.Tensor], torch.Tensor]` +
  `SpectralGapResult` `@dataclass(slots=True)` declaration +
  docstring Attributes section (all 6 attributes with E_x[] descriptions) +
  `mean_*: float = field(init=False)` declarations (×3) +
  `__post_init__` body opening: `self.mean_restricted_norm = self.restricted_norm.mean().item()`.

### NeuroVoice R271
- `tests/test_pretrain_source_3_noarch.py` (+30, suite 12,171 → 12,201).
- 3rd source pass for `src/training/pretrain.py` — `val_loader` DataLoader +
  Models block (encoder / mae_decoder) + params + AdamW optimiser +
  OneCycleLR scheduler + `global_step = 0` / `best_val_loss = float("inf")` +
  logger.info startup message + while loop opening
  (encoder.train / mae_decoder.train / for batch / batch["neural"] / batch["mask"]).

| Repo | Suite | Delta |
|------|-------|-------|
| SPS | 10,498 | +30 |
| NeuroVoice | 12,201 | +30 |
| Combined | 22,699 | +60 |
| metaMirage | post-deadline | — |

## [07:30 UTC] Session — R272 (Satellite Tracking)

metaMirage deadline (2026-04-16) has passed. Repository is in archive state.

### State at Session Start
- SPS suite: 10,498 | NeuroVoice suite: 12,201
- Phase 0: all repos clean (zero Claude-authored commits)
- Phase 0B: all failure files have **Date:** and **Status:** fields

### SPS R272
- `tests/test_jacobian_source_2_noarch.py` (+30, suite 10,498 → 10,528).
- 2nd source pass for `src/sps/jacobian.py` — `SpectralGapResult.__post_init__()`
  tail (`self.mean_full_norm = self.full_spectral_norm.mean().item()` /
  `self.mean_gap = self.normalized_gap.mean().item()`) +
  docstring Attributes mean_full_norm / mean_gap entries +
  `__repr__()` full f-string (`f"SpectralGapResult("` /
  `f"mean_restricted_norm={self.mean_restricted_norm:.4f}, "` /
  `f"mean_full_norm={self.mean_full_norm:.4f}, "` /
  `f"mean_gap={self.mean_gap:.4f})"`) +
  `directional_derivative_norm()` signature (`fn: ForwardFn` / `x/v: torch.Tensor` /
  `eps: float = 1e-3` / `-> torch.Tensor`) +
  docstring ("Implements Theorem 2 Part (i)" /
  "lim_{alpha->0} ||f(x + alpha*v) - f(x)|| / alpha = ||Jf(x) v||." /
  JVP note / Args fn/x/v/eps / Returns shape (B,)) +
  body (`v_magnitude = v.flatten(start_dim=1).norm(dim=1)` /
  `v_unit = normalize_directions(v)` /
  `def scalar_fn(emb: torch.Tensor) -> torch.Tensor: return fn(emb)` /
  `_, jvp_val = torch.autograd.functional.jvp(scalar_fn, x, v_unit, create_graph=False)` /
  `norms = jvp_val.flatten(start_dim=1).norm(dim=1)` /
  `return torch.where(v_magnitude > eps, norms, torch.zeros_like(norms))`).

### NeuroVoice R272
- `tests/test_pretrain_source_4_noarch.py` (+30, suite 12,201 → 12,231).
- 4th source pass for `src/training/pretrain.py` — Forward section
  (`latent = encoder(neural)` / `recon  = mae_decoder(latent, mask)`) +
  MAE loss block (`# MAE loss: MSE over masked positions only` /
  `# neural shape: (B, C, T)` comment / `target = neural.permute(0, 2, 1)` /
  `loss = _masked_mse(recon, target, mask)`) +
  Backward section (`# Backward` / `optimiser.zero_grad()` / `loss.backward()` /
  `nn.utils.clip_grad_norm_(params, grad_clip)` / `optimiser.step()` / `scheduler.step()` /
  `global_step += 1` / `if global_step % 500 == 0` /
  `f"Step {global_step}/{max_steps} — train loss: {loss.item():.5f}"`) +
  checkpoint block (`if global_step % checkpoint_every == 0` /
  `val_loss = _evaluate(encoder, mae_decoder, val_loader, dev)` /
  val-loss logger / `best_val_loss = val_loss` / `checkpoint_dir / "best_encoder.pt"`) +
  final save (`# ── Save final` / `checkpoint_dir / "final_encoder.pt"` /
  pre-training-complete logger / `return encoder.cpu()`) +
  `_masked_mse()` body (`diff = (recon - target) ** 2` / `diff.mean(dim=-1)` /
  `masked_loss = (diff * mask.float()).sum() / mask.float().sum().clamp(min=1)` /
  `return masked_loss`).

| Repo | Suite | Delta |
|------|-------|-------|
| SPS | 10,528 | +30 |
| NeuroVoice | 12,231 | +30 |
| Combined | 22,759 | +60 |
| metaMirage | post-deadline | — |

### Next Steps
- SPS R273: 3rd source pass for `src/sps/jacobian.py` — `restricted_operator_norm()`
  signature + docstring (Theorem 2 Part (ii) / sup formula / approximation note /
  tight-when clause / Args fn/x/directions / Returns shape (B,)) +
  body (`B, K, seq_len, hidden = directions.shape` / `norms_per_dir: list[torch.Tensor] = []` /
  `for k in range(K):` / `v_k = directions[:, k, :, :]` /
  `jvp_norm = directional_derivative_norm(fn, x, v_k)` /
  `norms_per_dir.append(jvp_norm)` /
  `stacked = torch.stack(norms_per_dir, dim=1)` /
  `return stacked.max(dim=1).values`).
- NV R273: 5th source pass for `src/training/pretrain.py` — `_evaluate()` function
  (`@torch.no_grad()` decorator / signature (encoder / mae_decoder / loader / device) /
  `encoder.eval()` / `mae_decoder.eval()` / `total_loss = 0.0` / `n_batches = 0` /
  for-batch loop / neural/mask to device / latent/recon/target / `_masked_mse` call /
  `total_loss +=` / `n_batches += 1` / `return total_loss / max(n_batches, 1)`).

## [15:00 UTC] Session — R273 (Satellite Tracking)

metaMirage deadline (2026-04-16) has passed. Repository is in archive state.

### State at Session Start
- SPS suite: 10,528 | NeuroVoice suite: 12,231
- Phase 0: all repos clean (zero Claude-authored commits)
- Phase 0B: all failure files have **Date:** and **Status:** fields

### SPS R273
- `tests/test_jacobian_source_3_noarch.py` (+30, suite 10,528 → 10,558).
- 3rd source pass for `src/sps/jacobian.py` — `restricted_operator_norm()`
  section header (`# Theorem 2, Part (ii): A_x-restricted operator norm`) +
  `__all__` entry (`"restricted_operator_norm",`) +
  signature (`fn: ForwardFn` / `x: torch.Tensor` / `directions: torch.Tensor` /
  `-> torch.Tensor`) +
  docstring (`Compute the A_x-restricted operator norm ||Jf(x)||_{A_x} via JVP.` /
  `Implements Theorem 2 Part (ii):` /
  `||Jf(x)||_{A_x} = sup_{v in A_x, ||v||=1} ||Jf(x) v||` /
  `≈ max_{k=1,...,K} ||Jf(x) v_k||   (empirical, K directions).` /
  tight-when clause / synonym-direction family / `EmbeddingPerturbationConfig` /
  Args fn/x/directions / `Requires grad.` / `TransformationFamily.semantic_directions().` /
  `||Jf(x)||_{A_x} estimates of shape (B,).`) +
  body (`B, K, seq_len, hidden = directions.shape` /
  `norms_per_dir: list[torch.Tensor] = []` /
  `for k in range(K):` / `v_k = directions[:, k, :, :]` /
  `jvp_norm = directional_derivative_norm(fn, x, v_k)` /
  `norms_per_dir.append(jvp_norm)` /
  `stacked = torch.stack(norms_per_dir, dim=1)` /
  `return stacked.max(dim=1).values` /
  inline comments `# (B, seq, h)` / `# (B, K)`).

### NeuroVoice R273
- `tests/test_pretrain_source_5_noarch.py` (+30, suite 12,231 → 12,261).
- 5th source pass for `src/training/pretrain.py` — `_evaluate()` function:
  `@torch.no_grad()` decorator +
  signature (`encoder: NeuralEncoder,` / `mae_decoder: MaskedAutoencoderDecoder,` /
  `loader: DataLoader,` / `device: torch.device,` / `-> float:`) +
  body (`encoder.eval()` / `mae_decoder.eval()` / `total_loss = 0.0` /
  `n_batches = 0` / `for batch in loader:` /
  `neural = batch["neural"].to(device)` / `mask   = batch["mask"].to(device)` /
  `latent = encoder(neural)` / `recon  = mae_decoder(latent, mask)` /
  `target = neural.permute(0, 2, 1)` /
  `total_loss += _masked_mse(recon, target, mask).item()` /
  `n_batches += 1` / `return total_loss / max(n_batches, 1)`) +
  call-site context (`val_loss = _evaluate(encoder, mae_decoder, val_loader, dev)` /
  `if val_loss < best_val_loss:` / val-loss logger f-string).

| Repo | Suite | Delta |
|------|-------|-------|
| SPS | 10,558 | +30 |
| NeuroVoice | 12,261 | +30 |
| Combined | 22,819 | +60 |
| metaMirage | post-deadline | — |

### Next Steps
- SPS R274: 4th source pass for `src/sps/jacobian.py` — `adversarial_worst_direction()`
  function signature + docstring (constructive dual of restricted_operator_norm /
  `argmax_{v in A_x} ||Jf(x) v||` / AdversarialEmbeddingFamily ref /
  Args fn/x/directions / Returns shape (B, seq, h)) + body
  (`B, K, seq_len, hidden = directions.shape` / `norms_per_dir` / `for k in range(K):` /
  `v_k` / `jvp_norm` / `norms_per_dir.append` / `stacked.argmax(dim=1)` /
  `worst_k` / `# Gather the worst direction for each sample` /
  `directions[torch.arange(B, device=x.device), worst_k]` / `return worst_dirs`).
- NV R274: 5th source pass for `src/training/finetune.py` — `_validate()` signature
  + residual body expressions (`def _validate(` / `pipeline: NeuroVoicePipeline,` /
  `mse_criterion: nn.Module,` / `voicing_weight: float,` / `-> float:` /
  `T = min(pred.shape[1], target.shape[1])` /
  `pred, target = pred[:, :T, :], target[:, :T, :]` /
  `loss = mse_criterion(pred, target)` /
  `voicing_pred = pred[:, :, _VOICING_IDX]` /
  `voicing_target = (target[:, :, _VOICING_IDX] > 0.5).float()` /
  `voicing_loss = nn.functional.binary_cross_entropy_with_logits(voicing_pred, voicing_target)` /
  `total += (loss + voicing_weight * voicing_loss).item()`).

