# metaMirage Devlog — 2026-06-17

## [00:00 UTC] Session — R271 (Satellite Tracking)

metaMirage deadline (2026-04-16) has passed. Repository is in archive state.

### State at Session Start
- SPS suite: 10,468 | NeuroVoice suite: 12,171
- Phase 0: all repos clean (zero Claude-authored commits)
- Phase 0B: all failure files have **Date:** and **Status:** fields

### SPS R271
- `tests/test_jacobian_source_1_noarch.py` (+30, suite 10,468 → 10,498).
- 1st source pass for `src/sps/jacobian.py` — module docstring title
  ("Jacobian-based analysis: restricted operator norm and semantic spectral gap.") +
  "Implements Theorem 2 (corrected, two-part) and Definition 5 / Corollary 1." +
  Key functions section (restricted_operator_norm / full_spectral_norm /
  spectral_gap / verify_spectral_gap_bound entries with descriptions) +
  References block ("Kang (2026) Theorem 2 (corrected), Definition 5, Corollary 1.") +
  full import block + `logger` + `ForwardFn = Callable[[torch.Tensor], torch.Tensor]` +
  `SpectralGapResult` `@dataclass(slots=True)` declaration +
  docstring Attributes section (all 6 attributes with E_x[] descriptions) +
  `mean_*: float = field(init=False)` declarations (×3) +
  `__post_init__` body opening: `self.mean_restricted_norm = self.restricted_norm.mean().item()`.

### NeuroVoice R271
- `tests/test_pretrain_source_3_noarch.py` (+30, suite 12,171 → 12,201).
- 3rd source pass for `src/training/pretrain.py` — `val_loader` DataLoader +
  Models block (encoder / mae_decoder) + params + AdamW optimiser +
  OneCycleLR scheduler + `global_step = 0` / `best_val_loss = float("inf")` +
  logger.info startup message + while loop opening
  (encoder.train / mae_decoder.train / for batch / batch["neural"] / batch["mask"]).

| Repo | Suite | Delta |
|------|-------|-------|
| SPS | 10,498 | +30 |
| NeuroVoice | 12,201 | +30 |
| Combined | 22,699 | +60 |
| metaMirage | post-deadline | — |

## [07:30 UTC] Session — R272 (Satellite Tracking)

metaMirage deadline (2026-04-16) has passed. Repository is in archive state.

### State at Session Start
- SPS suite: 10,498 | NeuroVoice suite: 12,201
- Phase 0: all repos clean (zero Claude-authored commits)
- Phase 0B: all failure files have **Date:** and **Status:** fields

### SPS R272
- `tests/test_jacobian_source_2_noarch.py` (+30, suite 10,498 → 10,528).
- 2nd source pass for `src/sps/jacobian.py` — `SpectralGapResult.__post_init__()`
  tail + `__repr__()` + `directional_derivative_norm()`.

### NeuroVoice R272
- `tests/test_pretrain_source_4_noarch.py` (+30, suite 12,201 → 12,231).
- 4th source pass for `src/training/pretrain.py` — Forward + MAE loss + Backward.

| Repo | Suite | Delta |
|------|-------|-------|
| SPS | 10,528 | +30 |
| NeuroVoice | 12,231 | +30 |
| Combined | 22,759 | +60 |
| metaMirage | post-deadline | — |

## [15:00 UTC] Session — R273 (Satellite Tracking)

metaMirage deadline (2026-04-16) has passed. Repository is in archive state.

### SPS R273
- `tests/test_jacobian_source_3_noarch.py` (+30, suite 10,528 → 10,558).

### NeuroVoice R273
- `tests/test_pretrain_source_5_noarch.py` (+30, suite 12,231 → 12,261).

| Repo | Suite | Delta |
|------|-------|-------|
| SPS | 10,558 | +30 |
| NeuroVoice | 12,261 | +30 |
| Combined | 22,819 | +60 |
| metaMirage | post-deadline | — |

## [15:20 UTC] Session — R274 (Satellite Tracking)

metaMirage deadline (2026-04-16) has passed. Repository is in archive state.

### SPS R274
- `tests/test_jacobian_source_9_noarch.py` (+30, suite 10,558 → 10,588).

### NeuroVoice R274
- `tests/test_training_finetune_source_5_noarch.py` (+30, suite 12,261 → 12,291).

| Repo | Suite | Delta |
|------|-------|-------|
| SPS | 10,588 | +30 |
| NeuroVoice | 12,291 | +30 |
| Combined | 22,879 | +60 |
| metaMirage | post-deadline | — |

## [10:30 UTC] Session — R275 (Satellite Tracking)

metaMirage deadline (2026-04-16) has passed. Repository is in archive state.

### SPS R275
- `tests/test_jacobian_source_10_noarch.py` (+30, suite 10,588 → 10,618).

### NeuroVoice R275
- `tests/test_pretrain_source_6_noarch.py` (+30, suite 12,291 → 12,321).

| Repo | Suite | Delta |
|------|-------|-------|
| SPS | 10,618 | +30 |
| NeuroVoice | 12,321 | +30 |
| Combined | 22,939 | +60 |
| metaMirage | post-deadline | — |

## [12:00 UTC] Session — R276 (Satellite Tracking)

metaMirage deadline (2026-04-16) has passed. Repository is in archive state.

### SPS R276
- `tests/test_jacobian_source_11_noarch.py` (+30, suite 10,618 → 10,648).

### NeuroVoice R276
- `tests/test_pretrain_source_7_noarch.py` (+30, suite 12,321 → 12,351).

| Repo | Suite | Delta |
|------|-------|-------|
| SPS | 10,648 | +30 |
| NeuroVoice | 12,351 | +30 |
| Combined | 22,999 | +60 |
| metaMirage | post-deadline | — |

## [13:30 UTC] Session — R277 (Satellite Tracking)

metaMirage deadline (2026-04-16) has passed. Repository is in archive state.

### SPS R277
- `tests/test_jacobian_source_12_noarch.py` (+30, suite 10,648 → 10,678).

### NeuroVoice R277
- `tests/test_pretrain_source_8_noarch.py` (+30, suite 12,351 → 12,381).

| Repo | Suite | Delta |
|------|-------|-------|
| SPS | 10,678 | +30 |
| NeuroVoice | 12,381 | +30 |
| Combined | 23,059 | +60 |
| metaMirage | post-deadline | — |

## [14:00 UTC] Session — R278 (Satellite Tracking)

metaMirage deadline (2026-04-16) has passed. Repository is in archive state.

### SPS R278
- `tests/test_jacobian_16_noarch.py` (+30, suite 10,678 → 10,708).

### NeuroVoice R278
- `tests/test_serving_session_adapt_11_noarch.py` (+30, suite 12,381 → 12,411).

| Repo | Suite | Delta |
|------|-------|-------|
| SPS | 10,708 | +30 |
| NeuroVoice | 12,411 | +30 |
| Combined | 23,119 | +60 |
| metaMirage | post-deadline | — |

## [15:17 UTC] Session — R279 (Satellite Tracking)

metaMirage deadline (2026-04-16) has passed. Repository is in archive state.

### SPS R279
- `tests/test_stats_source_22_noarch.py` (+30, suite 10,708 → 10,738).

### NeuroVoice R279
- `tests/test_pretrain_source_9_noarch.py` (+30, suite 12,411 → 12,441).

| Repo | Suite | Delta |
|------|-------|-------|
| SPS | 10,738 | +30 |
| NeuroVoice | 12,441 | +30 |
| Combined | 23,179 | +60 |
| metaMirage | post-deadline | — |

## [16:14 UTC] Session — R280 (Satellite Tracking)

metaMirage deadline (2026-04-16) has passed. Repository is in archive state.

### SPS R280
- `tests/test_stats_source_23_noarch.py` (+30, suite 10,738 → 10,768).

### NeuroVoice R280
- `tests/test_pretrain_source_10_noarch.py` (+30, suite 12,441 → 12,471).

| Repo | Suite | Delta |
|------|-------|-------|
| SPS | 10,768 | +30 |
| NeuroVoice | 12,471 | +30 |
| Combined | 23,239 | +60 |
| metaMirage | post-deadline | — |

## [17:13 UTC] Session — R281 (Satellite Tracking)

metaMirage deadline (2026-04-16) has passed. Repository is in archive state.

### SPS R281
- `tests/test_stats_source_24_noarch.py` (+30, suite 10,768 → 10,798).

### NeuroVoice R281
- `tests/test_pretrain_source_11_noarch.py` (+30, suite 12,471 → 12,501).

| Repo | Suite | Delta |
|------|-------|-------|
| SPS | 10,798 | +30 |
| NeuroVoice | 12,501 | +30 |
| Combined | 23,299 | +60 |
| metaMirage | post-deadline | — |

## [18:09 UTC] Session — R282 (Satellite Tracking)

metaMirage deadline (2026-04-16) has passed. Repository is in archive state.

### SPS R282
- `tests/test_stats_source_25_noarch.py` (+30, suite 10,798 → 10,828).

### NeuroVoice R282
- `tests/test_pretrain_source_12_noarch.py` (+30, suite 12,501 → 12,531).

| Repo | Suite | Delta |
|------|-------|-------|
| SPS | 10,828 | +30 |
| NeuroVoice | 12,531 | +30 |
| Combined | 23,359 | +60 |
| metaMirage | post-deadline | — |

## [19:00 UTC] Session — R283 (Satellite Tracking)

metaMirage deadline (2026-04-16) has passed. Repository is in archive state.

### State at Session Start
- SPS suite: 10,828 | NeuroVoice suite: 12,531
- Phase 0: all repos clean (zero Claude-authored commits)
- Phase 0B: all failure files have **Date:** and **Status:** fields

### SPS R283
- `tests/test_stats_source_26_noarch.py` (+30, suite 10,828 → 10,858).
- 26th source pass for `src/sps/stats.py` — `bootstrap_ci()` body residuals via getsource:
  `TestBootstrapCiBodyGetsource` (10): `n = len(values)` / `if n == 0:` /
  `return 0.0, 0.0, 0.0` / `rng = random.Random(seed)` / `boot_means = sorted(` /
  `sum(rng.choices(values, k=n)) / n for _ in range(n_boot)` /
  `lo_idx = int((alpha / 2) * n_boot)` / `hi_idx = int((1 - alpha / 2) * n_boot)` /
  `round(sum(values) / n, 6),` / `round(boot_means[lo_idx], 6),`.
  `TestBootstrapCiReturnAndIndexGetsource` (10): `round(boot_means[hi_idx], 6),` /
  `rng.choices(values, k=n)` / `for _ in range(n_boot)` / `alpha / 2` /
  `1 - alpha / 2` / `boot_means[lo_idx]` / `boot_means[hi_idx]` /
  `sum(values) / n` / `round(sum(values) / n, 6)` /
  `-> tuple[float, float, float]:`.
  `TestBootstrapCiOrderingGetsource` (10): n before rng / empty-check before rng /
  rng before boot_means / boot_means before lo_idx / lo_idx before hi_idx /
  hi_idx before return-tuple / round-mean before round-lo / round-lo before round-hi /
  choices before lo_idx / return-zero before rng.

### NeuroVoice R283
- `tests/test_pretrain_source_13_noarch.py` (+30, suite 12,531 → 12,561).
- 13th source pass for `src/training/pretrain.py` — `run_pretrain()` docstring Returns
  section + function title + parameter ordering via inspect.getsource:
  `TestRunPretrainDocstringTitleAndSectionsGetsource` (10): title
  "Run masked autoencoder pre-training." / "Parameters" section header /
  "----------" underline / "Returns" section header / "-------" underline /
  "NeuralEncoder" in Returns / "Pre-trained encoder (on CPU), ready for fine-tuning." /
  title before Parameters / Parameters before Returns /
  Returns before "Pre-trained encoder".
  `TestRunPretrainDocstringParamOrderingFirstGetsource` (10): dataset→encoder_config /
  encoder_config→decoder_dim / decoder_dim→decoder_depth / decoder_depth→max_steps /
  max_steps→warmup_steps / warmup_steps→batch_size / batch_size→lr /
  lr→weight_decay / weight_decay→grad_clip / grad_clip→val_split.
  `TestRunPretrainDocstringParamOrderingSecondGetsource` (10): val_split→checkpoint_every /
  checkpoint_every→checkpoint_dir / checkpoint_dir→device / device→seed /
  seed before Returns / NeuralEncoder before "Pre-trained encoder" /
  dataset before max_steps / warmup_steps before grad_clip /
  checkpoint_dir before seed / encoder_config before max_steps.

| Repo | Suite | Delta |
|------|-------|-------|
| SPS | 10,858 | +30 |
| NeuroVoice | 12,561 | +30 |
| Combined | 23,419 | +60 |
| metaMirage | post-deadline | — |

## [20:00 UTC] Session — R284 (Satellite Tracking)

metaMirage deadline (2026-04-16) has passed. Repository is in archive state.

### State at Session Start
- SPS suite: 10,858 | NeuroVoice suite: 12,561
- Phase 0: all repos clean (zero Claude-authored commits)
- Phase 0B: all failure files have **Date:** and **Status:** fields

### SPS R284
- `tests/test_stats_source_27_noarch.py` (+30, suite 10,858 → 10,888).
- 27th source pass for `src/sps/stats.py` — `bootstrap_ci()` docstring via getsource:
  `TestBootstrapCiDocstringTitleAndArgsGetsource` (10): title
  ("Nonparametric bootstrap 95% CI for the mean of ``values``.") /
  "Uses percentile bootstrap (not BCa)" / "appropriate when the sample size is small" /
  "and the distribution of the mean is unknown." /
  "Parameters" section header / "values :" / "Sample of scalar observations." /
  "n_boot :" / "Number of bootstrap resamples." / "alpha :".
  `TestBootstrapCiDocstringReturnAndNotesGetsource` (10):
  "Two-sided coverage level. Default 0.05 gives a 95% CI." /
  "seed :" / "RNG seed for reproducibility." / "Returns" section header /
  "(mean, lower_bound, upper_bound) : tuple[float, float, float]" /
  "All values rounded to 6 decimal places." / "Notes" section header /
  "The percentile CI is asymptotically consistent." /
  "For n < 10 the coverage may be" /
  "below nominal — report alongside the sample size.".
  `TestBootstrapCiDocstringOrderingGetsource` (10): title before percentile-bootstrap /
  not-BCa before small / small before unknown / Parameters before values: /
  values: before n_boot: / n_boot: before alpha: / alpha: before seed: /
  seed: before Returns / Returns before Notes /
  Notes before "The percentile CI is asymptotically consistent.".

### NeuroVoice R284
- `tests/test_pretrain_source_14_noarch.py` (+30, suite 12,561 → 12,591).
- 14th source pass for `src/training/pretrain.py` — module docstring via
  inspect.getsource(module) with mocked imports:
  `TestModuleDocstringFirstBlockGetsource` (10): title
  ("Masked Autoencoder pre-training loop.") /
  "Trains NeuralEncoder + MaskedAutoencoderDecoder on unlabelled neural" /
  "recordings. The decoder is discarded after training; only encoder weights" /
  "are saved for downstream fine-tuning." / "Training objective:" /
  "Reconstruct the original signal at masked positions." /
  "Loss = mean squared error over masked timesteps only (following MAE)." /
  "L = (1 / |M|) Σ_{t ∈ M} ||x_t - x̂_t||²_2" /
  "where M is the set of masked timestep indices" / "x̂_t is the reconstruction.".
  `TestModuleDocstringSecondBlockGetsource` (10): "decoder is discarded after training" /
  "only encoder weights" / "saved for downstream fine-tuning" /
  "Σ_{t ∈ M}" / "||x_t - x̂_t||²_2" / "(1 / |M|)" /
  "masked timestep indices" / "following MAE" /
  "MaskedAutoencoderDecoder on unlabelled neural" /
  "NeuralEncoder + MaskedAutoencoderDecoder".
  `TestModuleDocstringOrderingGetsource` (10): title→Trains / Trains→Training-objective /
  Training-objective→Reconstruct / Reconstruct→Loss / Loss→L-formula /
  L-formula→where-M / where-M→x̂_t / decoder-discarded→Training-objective /
  only-encoder-weights→Training-objective / following-MAE→where-M.

| Repo | Suite | Delta |
|------|-------|-------|
| SPS | 10,888 | +30 |
| NeuroVoice | 12,591 | +30 |
| Combined | 23,479 | +60 |
| metaMirage | post-deadline | — |

### Next Steps
- SPS R285: 28th source pass for `src/sps/stats.py` — `loo_spectral_gap()` docstring
  via getsource: title ("Leave-one-out (LOO) stability diagnostic for the mean spectral
  gap.") / "Computes the LOO mean estimate for each sample and reports the range" /
  "[loo_min, loo_max]. A narrow range (< 0.05) indicates the mean is" /
  "not driven by any single sample." / "per_sample_gaps :" /
  "Per-sample normalized spectral gap values gamma-bar(f, T; x_i)." /
  dict return keys (n / mean / loo_min / loo_max / loo_range / stable) /
  "If n < 3, returns" / "`{\"stable\": False, \"n\": n}`" / "LOO undefined at n ≤ 2".
- NV R285: 15th source pass for `src/training/pretrain.py` — module-level import block
  + `__all__` via inspect.getsource(module): `from __future__ import annotations` /
  `import random` / `from pathlib import Path` / `from typing import Optional` /
  `import numpy as np` / `import torch` / `import torch.nn as nn` /
  `from torch.optim import AdamW` / `from torch.optim.lr_scheduler import OneCycleLR` /
  `from torch.utils.data import DataLoader` / `from loguru import logger` /
  `from ..models.encoder import NeuralEncoder, EncoderConfig, MaskedAutoencoderDecoder` /
  `from ..data.dataset import PretrainDataset` / `__all__ = ["run_pretrain"]`.
