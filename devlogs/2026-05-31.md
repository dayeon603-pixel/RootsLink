# 2026-05-31 — satellite: SPS 797 (+90), NeuroVoice 2661 (+90)

## [~02:00 UTC] Session — satellite

### Cross-project work
- **SPS**: `tests/test_sps_spectral_gap_report_noarch.py` (30 tests, new) —
  SPSReport spectral_gap_result field storage + relative_sps interpretation branches
  (>1 / =1 / <1 thresholds) + per-combination line counts + section gating rules.
  SPS noarch suite: 737 tests (was 707, +30).
- **NeuroVoice**: `tests/test_llm_corrector_noarch.py` (30 tests, new) —
  CorrectionResult dataclass (slots=True) + LLMCorrector init defaults + correct()
  RuntimeError + _build_prompt context gating + _parse_correction / _count_edits.
  NeuroVoice noarch suite: 2601 tests (was 2571, +30).

### No new metaMirage work this session
Sign-flip confirmed. Dashboard final. Satellite-only from this point forward.

---

## [02:09 UTC] Session — satellite

### Cross-project work
- **SPS**: `tests/test_stats_loo_batch_delta_noarch.py` (30 tests, new) —
  loo_batch_spectral_gap boundary cases (n<3, uniform, outlier worst_batch) +
  spectral_gap_report combined report keys/arithmetic + delta_method_rsps_ci
  z-score pinning (0.05/0.01/0.10 branches), zero-std collapse, emb_n=0 guard.
  SPS noarch suite: 767 tests (was 737, +30).
- **NeuroVoice**: `tests/test_whisper_hypotheses_extended_noarch.py` (30 tests, new) —
  Hypothesis score semantics (negative, zero, −inf) + equality + generate()
  RuntimeError guards (model None / processor None / both None) + custom init
  storage + multi-instance independence.
  NeuroVoice noarch suite: 2631 tests (was 2601, +30).

### No new metaMirage work this session
Satellite-only from this point forward.

---

## [03:13 UTC] Session — satellite

### Cross-project work
- **SPS**: `tests/test_get_embedding_layer_noarch.py` (30 tests, new) —
  `_get_embedding_layer()` callable priority path + roberta/bert/transformer/model
  attribute walk + fallthrough/AttributeError behavior + search ordering.
  SPS noarch suite: 797 tests (was 767, +30).
- **NeuroVoice**: `tests/test_concept_aligner_noarch.py` (30 tests, new) —
  SONAR_LANG_CODES dict coverage + `_cosine_similarity` / `_cosine_matrix` pure numpy +
  ConceptAligner init (encoder/decoder/threshold) + `_get_sonar_code` lookup behavior.
  NeuroVoice noarch suite: 2661 tests (was 2631, +30).

### No new metaMirage work this session
Satellite-only from this point forward.

---

## [~04:00 UTC] Session — satellite

### Cross-project work
- **SPS**: `tests/test_permutation_alternatives_noarch.py` (30 tests, new) —
  permutation_test_gap_diff "greater" / "less" directional p-value semantics;
  smoothing floor verification; observed_diff 6dp rounding; seed reproducibility.
  SPS noarch suite: 827 tests (was 797, +30).
- **NeuroVoice**: `tests/test_gaddy_calibration_noarch.py` (30 tests, new) —
  GADDY_SAMPLE_RATE=1000 constant; GaddyCalibrationDataset window/stride arithmetic
  (int(ms*1000/1000)=ms); summary() dict keys and empty-dataset values;
  make_feature_alignment_loss / make_temporal_consistency_loss signature defaults.
  NeuroVoice noarch suite: 2691 tests (was 2661, +30).

### No new metaMirage work this session
Satellite-only from this point forward.

---

## [05:16 UTC] Session — satellite

### Cross-project work
- **SPS**: `tests/test_sps_config_pydantic_noarch.py` (30 tests, new) —
  SPSConfig pydantic v2 API: model_copy() independence/update, model_validate()
  from dict, model_dump(include/exclude), model_dump_json(), model_fields metadata,
  __eq__/__ne__ across all 7 fields.
  SPS noarch suite: 857 tests (was 827, +30).
- **NeuroVoice**: `tests/test_on_device_extended_noarch.py` (30 tests, new) —
  AdapterWeights dataclass structure (field count/names/independence), size_kb
  arithmetic (512/1024/2048 bytes), OnDeviceAdapter extended init, get_adapter
  return-value contracts (version=1, n_corrections=0, weights_path type),
  has_minimum_corrections boundary cases.
  NeuroVoice noarch suite: 2721 tests (was 2691, +30).

### No new metaMirage work this session
Satellite-only from this point forward.

---

## [06:13 UTC] Session — satellite

### Cross-project work
- **SPS**: `tests/test_relative_sps_fields_noarch.py` (30 tests, new) —
  RelativeSPSReport mean_sens_t/arb + std_sens_t/arb + n_samples_t/arb stored fields;
  sens_ci_t tuple structure (3 elements, lo ≤ hi); loo_t dict contracts (stable key,
  bool type, extra keys preserved, instance independence); summary LOO content (True/
  False/N/A branches); std_sens_arb 4dp in summary; compare_architecture_cis ci_hi/n/
  loo_stable/ci_width/loo_range keys + ordering invariant + 4-arch ranking length.
  SPS noarch suite: 887 tests (was 857, +30).
- **NeuroVoice**: `tests/test_vocabulary_tiers_save_load_noarch.py` (30 tests, new) —
  Module constants exact values (TIER_1_VOCAB_SIZE=50, SONAR_DIM=1024, etc.); save()/
  load() filesystem contracts (creates dir/JSON, roundtrip for user_id/tier/words);
  from_dict() with TIER_2/TIER_3 name strings; next_tier_utterances_needed Tier-2
  arithmetic (200→800, 999→1, 1001→0 clamped); active_vocabulary ordering (tier words
  before custom words).
  NeuroVoice noarch suite: 2751 tests (was 2721, +30).

---

## [07:00 UTC] Session — satellite

### Cross-project work
- **SPS**: `tests/test_adv_report_ci_loo_noarch.py` (30 tests, new) —
  AdversarialSPSReport emb_sens_ci/adv_sens_ci tuple storage (type, len=3, lo≤hi
  invariant), loo_adv extended dict (extra keys preserved, empty dict → N/A in
  summary()), loo_emb/loo_adv instance independence, std_sens_emb/adv/mean storage,
  n_samples int type and integer-format in summary.
  SPS noarch suite: 917 tests (was 887, +30).
- **NeuroVoice**: `tests/test_session_adapt_config_noarch.py` (30 tests, new) —
  SessionAdaptConfig all 8 defaults + custom values + is_dataclass; AdaptResult
  NamedTuple structure (5 fields, tuple subclass); SessionAdapter init contracts
  (self.encoder/self.cfg/self._saved_state); recommend_strategy() thresholds
  (≥7.0 days or <0.70 accuracy → "deep"); rollback() RuntimeError guard.
  NeuroVoice noarch suite: 2781 tests (was 2751, +30).

### No new rootslink work this session
Satellite-only from this point forward.

---

## [~session-time UTC] Session — satellite

### Cross-project work
- **SPS**: `tests/test_sps_estimator_dictkeys_noarch.py` (30 tests, new) —
  SPSEstimator.estimate() dict-key contracts: all 6 keys (sps/mean_sensitivity/
  std_sensitivity/n_samples/sens_ci/loo) present, correct types (float×3, int, tuple
  len=3, dict), SPS ∈ (0,1] bound, exp formula, RuntimeError on empty iterator,
  sens_ci lo≤point≤hi ordering, loo["stable"] and loo["n"] invariants.
  torch.cat patched via patch.object(_core, "torch"); bootstrap_ci/loo_spectral_gap
  are real pure-Python functions.
  SPS noarch suite: 947 tests (was 917, +30).
- **NeuroVoice**: `tests/test_emg_ble_bridge_noarch.py` (30 tests, new) —
  segment_to_decode_frame (dict keys, channel/timestep values, return_audio flag,
  ValueError on non-2D/zero-dim); base64 roundtrip (single/multi-channel, float64
  cast, ValueError paths); decode_response_articulatory (shape, dtype, KeyError);
  decode_response_audio (None on absent/null, ndarray+shape when present);
  make_batch_frame (length contracts). Pure numpy; no torch required.
  NeuroVoice noarch suite: 2811 tests (was 2781, +30).

### No new rootslink work this session
Satellite-only from this point forward.

---

## [~session-time UTC] Session — satellite

### Cross-project work
- **SPS**: `tests/test_adversarial_family_delegation_noarch.py` (30 tests, new) —
  AdversarialEmbeddingFamily._t_emb delegation: all 3 config field propagations
  (n_directions/use_synonym_directions/projection_noise_scale), embedding_layer identity
  (assertIs), synonym_map={} when None, map dict preserved, adv.embedding_layer is
  _t_emb.embedding_layer, forward_fn/config stored attrs. Pre-registers sps.transformations
  in sys.modules for Python 3.11 slots=True dataclasses.
  SPS noarch suite: 977 tests (was 947, +30).
- **NeuroVoice**: `tests/test_emg_features_extended_noarch.py` (30 tests, new) —
  EMGFeatureExtractor._handcrafted_features() extended: WL formula (constant/step/random),
  SSC formula (constant/monotone/alternating), spectral features (n_fft effects,
  determinism, finite/nonneg bounds), multi-channel independence (rms/zcr by channel,
  len=14/21 for 2/3 channels, amplitude isolation), edge cases (n=1/2, all-zeros,
  dtype check). Pure numpy; no torch required.
  NeuroVoice noarch suite: 2841 tests (was 2811, +30).

### No new rootslink work this session
Satellite-only from this point forward.
