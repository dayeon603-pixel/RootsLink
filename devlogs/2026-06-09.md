# 2026-06-09 — Mirror: SPS R90–R91 + NeuroVoice R90–R91

*This devlog mirrors activity from the primary SPS and NeuroVoice repositories.*

## Summary

Sessions R90+R91: SPS 5038→5098 (+60), NeuroVoice 6741→6801 (+60). Combined noarch total: 11,899 tests.

### SPS R90 — `test_cross_arch_adversarial_6_noarch.py`

6th noarch pass on experiments/cross_arch_adversarial.py. New territory: runtime-requiring function bodies that prior passes only covered by signature.

**TestRunArchitectureBodySource (10):** `display_name = arch_name.split("/")[-1]`, `arch_synonym_map = synonym_map`, `use_synonyms = bool(arch_synonym_map)`, `emb_cfg = EmbeddingPerturbationConfig(`, `use_synonym_directions=use_synonyms,`, `t_emb = EmbeddingPerturbationFamily(`, `build_adversarial_sps_estimator(model, t_emb, t_adv, config)`, `report: AdversarialSPSReport = estimator.estimate(batches)`, `collect_spectral_gaps(model, batches, t_emb, args.device)`, `return ArchResult(name=display_name, ...)`.

**TestCollectSpectralGapsBodySource (10):** `gaps: list[float] = []`, `def _make_fn(mask):`, `gaps.extend(result.normalized_gap.tolist())`, `dirs = t_emb.semantic_directions(emb, ids)`, `result = spectral_gap(_make_fn(mask), emb, dirs, n_probe_full=16)`, `for batch in batches:`, emb/ids/mask `.to(device)` assignments, `return gaps`.

**TestMainBodySource (10):** `n_sentences = args.n_batches * args.batch_size`, sentences slice, `results: list[ArchResult] = []`, `for arch in args.architectures:`, `arch_gap_dict` dict comp, `print_cross_arch_ranking(...)`, `all_batch_means` list comp, `validate_hypotheses(results)`, `print(r.report.summary())`, `if args.output:`.

All 30 tests pass (0.05s, raw source read).

### NeuroVoice R90 — `test_evaluation_metrics_source_noarch.py`

First source-expression pass on `src/evaluation/metrics.py` — the first test file for src/evaluation/ directory (previously 0 tests).

**TestArticulatoryMetricsDataclassSource (10):** `__all__`, `_F0_IDX = 6`, `_VOICING_IDX = 8`, `@dataclass`, and 6 field annotations (rmse_per_dim, corr_per_dim, mean_rmse, voicing_accuracy, n_frames, n_voiced_frames).

**TestArticulatoryMetricsSummarySource (10):** `def summary(self) -> str:`, 5 format strings (mean_rmse 4dp, mean_corr 4dp, voicing_accuracy 3dp, f0_rmse_voiced 2dp, f0_corr_voiced 4dp), per-dimension `enumerate(ARTICULATORY_NAMES)` loop, per-dim fstring, `return "\n".join(lines)`, plus `mean_corr: float` field.

**TestComputeMetricsBodySource (10):** `def compute_metrics(`, shape mismatch `raise ValueError(`, `N, A = predicted.shape`, `assert A == ARTICULATORY_DIM`, `for i in range(A):`, `rmse_per_dim.append(float(np.sqrt(...)))`, `voiced_pred = (predicted[:, _VOICING_IDX] > voicing_threshold).astype(int)`, `voiced_mask = voiced_target.astype(bool)`, `if n_voiced > 1:`, `f0_rmse = float("nan")`.

All 30 tests pass (0.06s, --noconftest, raw source read).

### SPS R91 — `test_spectral_gap_loo_5_noarch.py`

5th source pass on experiments/spectral_gap_loo_analysis.py. New territory: main() body sections not yet source-locked.

**TestMainArchLoopBodySource (10):** `scaled_sigma = cfg["sigma"] * args.epsilon_scale`, `arch_seed = args.seed + arch_idx * 100`, `batch_means = generate_batch_means(`, `arch_mu=cfg["mu"],`, `arch_sigma=scaled_sigma,`, `n_batches=args.n_batches,`, `samples_per_batch=args.samples_per_batch,`, `arch_results[arch] = {`, `"batch_means": batch_means,`, `arch_per_sample[arch] = per_sample`.

**TestMainComparisonAndPermutationSrc (10):** `rb_gaps = arch_per_sample["roberta-base"]`, `n_boot_values = [100, 500, 1000, 5000]`, `print_bootstrap_sensitivity(...)`, `rep = comp[arch]`, `gp = arch_results[arch]["gap_report"]`, `excludes_zero = "YES" if rep["ci_lo"] > 0.0 else "NO"`, `stable_mark = "YES" if gp["loo_stable"] else "NO"`, `for rank_i, arch in enumerate(ranking, 1):`, `pair_label = f"{arch_a} vs {arch_b}"`, `sig_label = "* p<0.05" if perm["p_value"] < 0.05 else "  ns"`.

**TestMainEpsilonSensAndH3FinalSrc (10):** `for scale in [0.5, 1.0, 1.5, 2.0]:`, `cfg = ARCH_CONFIGS["roberta-base"]`, `bm = generate_batch_means(`, `arch_sigma=cfg["sigma"] * scale,`, `ps = generate_per_sample_gaps(bm, ...)`, `b_loo = loo_batch_spectral_gap(bm)`, `mean, ci_lo, ci_hi = bootstrap_ci(...)`, `gm = b_loo.get("grand_mean", mean)`, `n_stable = sum(`, `n_total = len(arch_results)`.

All 30 tests pass (0.05s, raw source read).

### NeuroVoice R91 — `test_vocoder_source_noarch.py`

First source-expression pass on `src/models/vocoder.py`.

**TestFiLMAndResBlockBodySource (10):** `gamma = self.to_gamma(art).permute(0, 2, 1)`, `beta  = self.to_beta(art).permute(0, 2, 1)`, `return (1 + gamma) * h + beta`, `for conv1, conv2, film in zip(self.convs1, self.convs2, self.film_layers):`, `h = F.leaky_relu(x, LRELU_SLOPE)`, `h = conv1(h)`, `h = film(h, art)`, `h = conv2(h)`, `x = x + h`, `for conv in list(self.convs1) + list(self.convs2):`.

**TestHiFiGANForwardBodySource (10):** `LRELU_SLOPE = 0.1`, `x = articulatory.permute(0, 2, 1)`, `x = self.input_conv(x)`, `for up, mrf_blocks in zip(self.ups, self.mrfs):`, `x = F.leaky_relu(x, LRELU_SLOPE)`, `T_curr = x.shape[-1]`, `align_corners=False,`, `xs = torch.stack([block(x, art_up) for block in mrf_blocks], dim=0)`, `x = xs.mean(dim=0)`, `out: torch.Tensor = self.post_net(x)`.

**TestLPCNetVocoderBodySource (10):** `self.feature_gru = nn.GRU(A, F, num_layers=1, batch_first=True)`, `self.lpc_head = nn.Linear(F, P)`, `self.cond_head = nn.Linear(F, Ga)`, `self.sample_embed = nn.Embedding(Q, 8)`, `self.gru_a_cell = nn.GRUCell(8 + Ga, Ga)`, `self.gru_b_cell = nn.GRUCell(Ga, Gb)`, `self.output_proj = nn.Linear(Ga + Gb, Q)`, `nn.init.kaiming_normal_(conv.weight)`, `waveform = waveform.clamp(-1.0, 1.0)`, `return ((compressed + 1) / 2 * mu + 0.5).long().clamp(0, mu)`.

All 30 tests pass (0.06s, --noconftest, raw source read).

## Status

| Repo | Suite | Session delta (R90+R91) |
|------|-------|--------------------------|
| SPS | 5,098 tests | +60 |
| NeuroVoice | 6,801 tests | +60 |
| AlphaNexus | satellite-only | — |
| RootsLink | satellite-only | — |
| metaMirage | post-deadline | — |
