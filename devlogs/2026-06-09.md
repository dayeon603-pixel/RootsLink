# 2026-06-09 — Mirror: SPS R90 + NeuroVoice R90

*This devlog mirrors activity from the primary SPS and NeuroVoice repositories.*

## Summary

Session R90: SPS 5038→5068 (+30), NeuroVoice 6741→6771 (+30). Combined noarch total: 11,839 tests.

### SPS R90 — 6th noarch pass on cross_arch_adversarial.py

Locked run_architecture body (display_name split, arch_synonym_map init, use_synonyms bool, EmbeddingPerturbationConfig/Family construction, build_adversarial_sps_estimator chain, estimator.estimate, collect_spectral_gaps call, spectral_gap_report, return ArchResult), collect_spectral_gaps body (gaps list, _make_fn closure, for batch loop, emb/ids/mask .to(device), semantic_directions, spectral_gap n_probe_full=16, gaps.extend, return), main() body (n_sentences arithmetic, sentences slice, results list, for arch loop, arch_gap_dict dict comp, all_batch_means, validate_hypotheses, r.report.summary(), args.output guard).

30/30 tests pass. Suite: 5038→5068.

### NeuroVoice R90 — First coverage of src/evaluation/metrics.py

First test file for the src/evaluation/ directory. Locked: __all__, _F0_IDX=6, _VOICING_IDX=8, @dataclass, 9 field annotations (rmse_per_dim/corr_per_dim/mean_rmse/mean_corr/voicing_accuracy/f0_rmse_voiced/f0_corr_voiced/n_frames/n_voiced_frames), summary() 5 format strings + per-dim enumerate loop + return join, compute_metrics() shape guard, N/A unpacking, ARTICULATORY_DIM assert, per-dim rmse/corr append, voiced_pred cast, voiced_mask bool, n_voiced > 1 guard, f0_rmse NaN else branch.

30/30 tests pass. Suite: 6741→6771.

## Status
SPS: 5,068 | NeuroVoice: 6,771 | RootsLink: satellite-only
