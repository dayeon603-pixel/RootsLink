# 2026-06-09 — Mirror: SPS R90–R91 + NeuroVoice R90–R91

*This devlog mirrors activity from the primary SPS and NeuroVoice repositories.*

## Summary

Sessions R90+R91: SPS 5038→5098 (+60), NeuroVoice 6741→6801 (+60). Combined noarch total: 11,899 tests.

### SPS R90 — `test_cross_arch_adversarial_6_noarch.py`

6th noarch pass on experiments/cross_arch_adversarial.py. New territory: runtime-requiring function bodies that prior passes only covered by signature.

**TestRunArchitectureBodySource (10):** `display_name = arch_name.split("/")[-1]`, `arch_synonym_map = synonym_map`, `use_synonyms = bool(arch_synonym_map)`, `emb_cfg = EmbeddingPerturbationConfig(`, `use_synonym_directions=use_synonyms,`, `t_emb = EmbeddingPerturbationFamily(`, `build_adversarial_sps_estimator(model, t_emb, t_adv, config)`, `report: AdversarialSPSReport = estimator.estimate(batches)`, `collect_spectral_gaps(model, batches, t_emb, args.device)`, `return ArchResult(name=display_name, ...)`.

**TestCollectSpectralGapsBodySource (10):** `gaps: list[float] = []`, `def _make_fn(mask):`, `gaps.extend(result.normalized_gap.tolist())`, `dirs = t_emb.semantic_directions(emb, ids)`, `result = spectral_gap(_make_fn(mask), emb, dirs, n_probe_full=16)`, `for batch in batches:`, emb/ids/mask `.to(device)` assignments, `return gaps`.

**TestMainBodySource (10):** `n_sentences = args.n_batches * args.batch_size`, sentences slice, `results: list[ArchResult] = []`, `for arch in args.architectures:`, `arch_gap_dict` dict comp, `print_cross_arch_ranking(...)`, `all_batch_means` list comp, `validate_hypotheses(results)`, `print(r.report.summary())`, `if args.output:`.

All 30 tests pass (0.05s, raw source read).

### NeuroVoice R90 — `test_evaluation_metrics_source_noarch.py`

First source-expression pass on `src/evaluation/metrics.py` — the first test file for src/evaluation/ directory (previously 0 tests). All 30 tests pass (0.06s, --noconftest, raw source read).

### SPS R91 — `test_spectral_gap_loo_5_noarch.py`

5th source pass on experiments/spectral_gap_loo_analysis.py. All 30 tests pass (0.05s, raw source read).

### NeuroVoice R91 — `test_vocoder_source_noarch.py`

First source-expression pass on `src/models/vocoder.py`. All 30 tests pass (0.06s, --noconftest, raw source read).

## Status

| Repo | Suite | Session delta (R90+R91) |
|------|-------|--------------------------|
| SPS | 5,098 tests | +60 |
| NeuroVoice | 6,801 tests | +60 |
| AlphaNexus | satellite-only | — |
| RootsLink | satellite-only | — |
| metaMirage | post-deadline | — |

## [07:45 UTC] Satellite Update — R92
- SPS R92: `test_estimate_sps_5_noarch.py` (+30, suite→5128). estimate_sps.py 5th source pass: SPSConfig constructor kwargs, EmbeddingPerturbationFamily build, estimator/arb-SPS chain, adversarial block, gap batch prep, LOO/bootstrap, results rows.
- NeuroVoice R92: `test_vocoder_lpcnet_source_noarch.py` (+30, suite→6831). vocoder.py 2nd source pass: LPCNetVocoder.forward (frame-rate conditioning + teacher-forced GRU loop), synthesise_chunk body (lpc_buf, prev_q, a_k/c_frame, lpc_sum, new_state).

## [09:30 UTC] Satellite Update — R93
- SPS R93: `test_estimate_sps_6_noarch.py` (+30, suite→5158). estimate_sps.py 6th source pass: AdversarialEmbeddingFamily full constructor, adv_estimator build/estimate, adv_ratio, rSPS CI interpretation, LayerwiseSPSAnalyzer import/init/compute_profile/print loop.
- NeuroVoice R93: `test_vocoder_disc_source_noarch.py` (+30, suite→6861). vocoder.py 3rd source pass: synthesise_chunk inner LPC synthesis, state-restore branch, VocoderDiscriminator.forward.

## [08:08 UTC] Satellite Update — R94
- SPS R94: `test_bootstrap_sign_flip_4_noarch.py` (+30, suite→5188). bootstrap_sign_flip.py 4th noarch source pass: main() rows labels, print body, confirmed/JSON output, data-loading list comps, module meta, INLINE_DATA model name strings.
- NeuroVoice R94: `test_encoder_source_noarch.py` (+30, suite→6891). encoder.py first source-expression pass: EncoderConfig all 7 field defaults, _TCNBlock.__init__/forward, NeuralEncoder.__init__/forward, MaskedAutoencoderDecoder.__init__/forward.

## [10:15 UTC] Satellite Update — R95
- SPS R95: `test_adversarial_sps_results_analysis_5_noarch.py` (+30, suite→5218). adversarial_sps_results_analysis.py 6th source pass: format_results_table/verify_hypotheses/build_summary/load_results/main() body expressions.
- NeuroVoice R95: `test_encoder_source_2_noarch.py` (+30, suite→6921). encoder.py 2nd source pass: _TCNBlock Dropout/GELU, NeuralEncoder channel_attn kwargs/output_proj/output_norm, MAE decoder init/forward.

## [10:05 UTC] Satellite Update — R96
- SPS R96: `test_adversarial_sps_results_analysis_behavioral_noarch.py` (+30, suite→5248). First behavioral integration tests for adversarial_sps_results_analysis.py — live round-trips: verify_hypotheses/build_summary/format_results_table.
- NeuroVoice R96: `test_pipeline_source_noarch.py` (+30, suite→6951). pipeline.py first source-expression pass: __all__/class def/init/forward/load_encoder_weights/save/from_checkpoint.

## [11:30 UTC] Satellite Update — R97
- SPS R97: `test_kaggle_adversarial_sps_source_noarch.py` (+30, suite→5278). kaggle_adversarial_sps.ipynb first source pass across all 7 code cells: TestCellSetupAndInstallSource (REPO_URL/REPO_DIR/git-clone-depth-1/capture_output/RuntimeError/Cloned-msg/PyTorch-version/cuda.is_available/VRAM/assert), TestCellConfigAndRunSource (DEVICE/N_BATCHES/EPSILON/N_BOOT/OUT_PATH/script-path/--device-arg/t0-timer/elapsed-print/returncode-error), TestCellResultsAndSummarySource (results-header/for-loop/loo_sym-✓✗/gap_base-large-deb-keys/H1_LO-H1_HI/h3-all-generator/summary_path/json.dump-indent=2).
- NeuroVoice R97: `test_decoder_source_5_noarch.py` (+30, suite→6981). decoder.py 5th source pass — saturation reached. TestHeadSequentialBodySource, TestForwardToneCondBranchSource, TestDecoderForwardCoreSource.

## [11:45 UTC] Satellite Update — R98
- SPS R98: `test_sps_stats_behavioral_noarch.py` (+30, suite→5308). First behavioral integration tests for sps/stats.py — live imports, stdlib only (stats.py uses only math+random). TestBootstrapCIBehavioral: empty→zeros, single-value mean, mean within CI, constant zero-width CI, mean correct, seed reproducible, lo≤hi, 3 floats, larger-alpha narrower CI, 6-decimal rounding. TestLOOAndBatchSpectralGapBehavioral: loo n<3 stable=False, constant stable=True/loo_range=0, mean correct, n returned, outlier unstable, batch_loo grand_mean/worst_batch_idx, spectral_gap_report 7 keys/ci_width. TestDeltaMethodAndPermutationBehavioral: equal-means rsps=1.0, arb>emb rsps>1, large-gap CI lo>1, CI brackets point, empty-group p=1, identical p>0.05, very-different p<0.05, 6 keys, compare_arch ranking/reports.
- NeuroVoice R98: `test_pretrain_source_noarch.py` (+30, suite→7011). pretrain.py first source pass: TestSetSeedAndModuleSource (__all__/encoder-imports/PretrainDataset/random.seed/np.random.seed/torch.manual_seed/cuda.manual_seed_all/cudnn.deterministic/device-default/max_steps=100_000), TestRunPretrainDataSplitSource (n_val-max/n_train/Generator.manual_seed/train-loader-num_workers4-drop_last/val-loader-num_workers2/encoder_dim-kwarg/in_channels-kwarg/betas=(0.9,0.95)/pct_start/anneal_strategy="cos"), TestTrainingLoopAndLossSource (global_step=0/best_val_loss=inf/neural-key/mask-key/mae_decoder-forward/target-permute/clip_grad_norm_/diff-squared/masked_loss-clamp/evaluate-return).

| Repo | Suite | R98 delta |
|------|-------|-----------|
| SPS | 5,308 tests | +30 |
| NeuroVoice | 7,011 tests | +30 |
| Combined | 12,319 tests | +60 |

## [12:30 UTC] Session — R99 satellite

SPS R99 (+30, suite 5,308→5,338): `test_metrics_source_2_noarch.py` — sps/metrics.py 2nd source pass.
NeuroVoice R99 (+30, suite 7,011→7,041): `test_pretrain_source_2_noarch.py` — pretrain.py 2nd source pass.
Combined: 5,338 + 7,041 = 12,379.

## [13:10 UTC] Satellite — R100

- SPS R100: `tests/test_sps_core_source_2_noarch.py` (+30, suite 5,338 → 5,368). sps/core.py 2nd source pass — SSE.estimate() transform-sample/ratio block, _forward() dim==3 branch, build_sps_estimator() constructor wiring, _get_embeddings() callable path + guards.
- NeuroVoice R100: `tests/test_finetune_source_noarch.py` (+30, suite 7,041 → 7,071). src/training/finetune.py first source pass — module constants, setup, AdamW param groups, loader+scheduler, training loop, voicing loss, warmup, checkpoint, return.
- Combined: 5,368 + 7,071 = 12,439 tests.

## [14:11 UTC] Satellite — R101

- SPS R101: `tests/test_transformations_source_2_noarch.py` (+30, suite 5,368 → 5,398). sps/transformations.py 2nd source pass — EPF.sample() body residuals, _synonym_directions/_random_orthogonal_directions residuals, AdversarialEmbeddingFamily.sample() full body, build_wordnet_synonym_map body residuals.
- NeuroVoice R101: `tests/test_finetune_source_2_noarch.py` (+30, suite 7,071 → 7,101). src/training/finetune.py 2nd source pass — _validate() complete body + signature, run_finetune() training loop residuals.
- Combined: 5,398 + 7,101 = 12,499 tests.
