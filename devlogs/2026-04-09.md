# RootsLink devlog — 2026-04-09

## [08:28 UTC] Session

### State at Session Start

Last commit: `feat: initial RootsLink MVP`. Full MVP is committed: FastAPI backend with 4 matching algorithms (OpportunityFit, MentorMatch, BrainDrainRisk, RetentionPriority), SQLAlchemy models, Pydantic schemas, API routers, and HTML/CSS frontend. No devlogs/ directory existed prior to this session.

### Work Completed

Created `devlogs/` directory and this initial session entry. No code changes — RootsLink is the lowest priority project.

### Results / Observations

Current state:
- `backend/main.py`: FastAPI app with CORS, lifespan DB init, seeder integration
- `backend/algorithms/`: all 4 core algorithms implemented
- `backend/routers/`: users, mentors, opportunities, matching endpoints
- `frontend/`: 5 HTML pages (index, register-student, register-mentor, opportunities, dashboard)
- `data/seed.py`: sample seed data (mentors, opportunities, test user)

No live deployment. SQLite for local dev.

### Hypotheses

None this session.

### Next Steps

- Add authentication (FastAPI OAuth2 / JWT)
- Add email notification integration for match alerts
- Improve opportunity filtering (tag-based, geographic, field-of-study)
- Lower priority than metaMirage, SPS, NeuroVoice, AlphaNexus
