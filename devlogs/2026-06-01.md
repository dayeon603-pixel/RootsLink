# 2026-06-01 — satellite: SPS 1157 tests (+30); NeuroVoice 3021 tests (+30)

## [00:30 UTC] Session

No new RootsLink work this session. Satellite tracking only.

- SPS noarch suite: 1127 tests (+30 from test_sps_report_layerwise_noarch.py).
- NeuroVoice noarch suite: 2991 tests (+30 from test_articulatory_metrics_extended_noarch.py).

---

## [03:00 UTC] Session — satellite

### Cross-project work
- **SPS**: `tests/test_adversarial_relative_estimator_init_noarch.py` (30 tests, new) —
  mock-based noarch coverage for AdversarialSPSEstimator, RelativeSPSEstimator,
  build_adversarial_sps_estimator, and build_relative_sps_estimator: field storage,
  config default resolution (config or emb/t_estimator.config), custom config override,
  independence invariants, factory return types, family/model propagation.
  SPS noarch suite: 1157 tests (was 1127, +30).
- **NeuroVoice**: `tests/test_lexicon_rescorer_extended_noarch.py` (30 tests, new) —
  extended noarch coverage for src/correction/lexicon_rescorer.py: levenshtein boundary
  conditions (max_d=0 identical/different, length-diff=max_d, prefix distance), from_counts
  exact log-prob arithmetic with smoothing, snap_word with empty vocab / keep_unmatched,
  rescore_sentence all-OOV result, rescore_batch edge cases, restrict_to propagation of
  max_edit_distance and lm_weight.
  NeuroVoice noarch suite: 3021 tests (was 2991, +30).

### No new rootslink work this session
rootslink suite stable. Satellite-only from this point forward.

---

## [02:10 UTC] Session — satellite

- **SPS**: `tests/test_sps_estimator_invocation_noarch.py` (+30) — SPS noarch suite: 1187.
- **NeuroVoice**: `tests/test_session_adapter_deep_noarch.py` (+30) — NeuroVoice noarch suite: 3051.
- No new rootslink work this session.

---

## [03:15 UTC] Session — satellite

- **SPS**: `tests/test_adversarial_relative_report_noarch.py` (+30 noarch).
  SPS noarch suite: 1217 tests (was 1187, +30).
- **NeuroVoice**: `tests/test_session_adapter_freeze_noarch.py` (+30 noarch).
  NeuroVoice noarch suite: 3081 tests (was 3051, +30).

---

## [04:08 UTC] Session — satellite

- **SPS**: `tests/test_adversarial_sps_estimator_estimate_noarch.py` (30 tests, new) —
  mock-based noarch coverage for AdversarialSPSEstimator.estimate(): both sub-estimator
  call count/order (emb before adv), iter-type argument contracts, adv_gap_ratio
  arithmetic (round(sps_adv/max(sps_emb, 1e-12), 6)), _collect_sensitivities
  integration with real sps.stats bootstrap_ci/loo_spectral_gap, and all
  AdversarialSPSReport field values.
  SPS noarch suite: 1247 tests (was 1217, +30).
- **NeuroVoice**: `tests/test_session_adapter_integrated_noarch.py` (30 tests, new) —
  integrated _freeze_all() + _unfreeze_target() pair: final requires_grad state per
  module type (LN=True, plain=False, non-top-k-TCN=False in fast; top-k-TCN=True in
  deep), call-sequence contracts (LN _calls=[False,True], plain=[False]), and cycle
  repeatability.
  NeuroVoice noarch suite: 3111 tests (was 3081, +30).
- No new rootslink work this session.

---

## [05:14 UTC] Session — satellite

- **SPS**: `tests/test_relative_sps_estimator_estimate_noarch.py` (30 tests, new) —
  mock-based noarch coverage for RelativeSPSEstimator.estimate(): t and arb sub-estimator
  call count and ordering (t first), iter-type argument contracts, rsps_point from
  delta_method_rsps_ci, rsps_ci_excludes_one logic (lo>1.0 or hi<1.0 strict), boundary
  tests (lo=1.0 → False, hi=1.0 → False via patch), _collect_sensitivities called once
  (T only), and all RelativeSPSReport field values.
  SPS noarch suite: 1277 tests (was 1247, +30).
- **NeuroVoice**: `tests/test_session_adapter_adapt_noarch.py` (30 tests, new) —
  noarch coverage for SessionAdapter.adapt(): early-return path (trainable=[]) with all
  AdaptResult fields (loss_curve=[], n_steps=0, converged=True, wall_seconds=0.0,
  n_params=0), encoder.to(device) called before early return, training-loop contracts
  (encoder.train/eval lifecycle, n_steps==len(loss_curve), n_params from numel, loss_fn
  call count), and early stopping (constant loss → converged=True, decreasing → False).
  NeuroVoice noarch suite: 3141 tests (was 3111, +30).
- No new metaMirage work this session.

---

## [06:21 UTC] Session — satellite

- **SPS**: `tests/test_collect_sensitivities_noarch.py` (+30 noarch tests).
  SPS noarch suite: 1307 tests (was 1277, +30).
- **NeuroVoice**: `tests/test_session_adapter_calibration_dispatch_noarch.py` (+30 noarch tests).
  NeuroVoice noarch suite: 3171 tests (was 3141, +30).
- No new rootslink work this session.

---

## [07:30 UTC] Session — satellite

- **SPS**: `tests/test_build_factory_functions_noarch.py` (30 tests, new) —
  mock-based noarch coverage of `build_adversarial_sps_estimator` and
  `build_relative_sps_estimator` factory helpers: return type identity
  (AdversarialSPSEstimator / RelativeSPSEstimator), config=None → single SPSConfig
  created and shared between both sub-estimator builds (identity confirmed),
  explicit config → same object stored (.config is cfg), build_sps_estimator called
  exactly 2 times per factory call, arg routing (emb_family → first call /
  adv_family → second; t_family / arb_family same pattern), model identity across
  both calls, and .emb_estimator/.adv_estimator/.t_estimator/.arb_estimator attribute
  accessibility.
  SPS noarch suite: 1337 tests (was 1307, +30).
- **NeuroVoice**: `tests/test_session_adapter_save_rollback_noarch.py` (30 tests, new) —
  noarch coverage for SessionAdapter safety API: save_state() deepcopy semantics
  (snapshot isolated from post-save mutations, state_dict() call count, deepcopy
  receives state_dict() result — patched via copy.deepcopy not module alias to
  survive cross-test sys.modules reloads), rollback() RuntimeError guard (no
  save_state → RuntimeError with "save_state" in message), load_state_dict
  contracts (called once per rollback, receives exact snapshot reference),
  non-destructive design (rollback() does not clear _saved_state), and
  save/rollback cycle repeatability (3-cycle test confirming per-cycle snapshot
  isolation).
  NeuroVoice noarch suite: 3201 tests (was 3171, +30).
- No new metaMirage work this session.

---

## [08:09 UTC] Session — satellite

- **SPS**: `tests/test_build_sps_estimator_noarch.py` (+30 noarch tests, new) —
  direct noarch coverage of `build_sps_estimator` factory: return type
  (SPSEstimator, not subclass), config=None creates fresh SPSConfig each call
  (two calls produce independent objects), explicit config propagated by identity,
  StructuredSensitivityEstimator receives model/transform_family/epsilon/device
  by identity or value match, sens_estimator does NOT store config (only individual
  fields), and divergence_fn wiring (cosine → cosine_divergence, l2 → l2_divergence,
  both confirmed via is-identity against get_divergence_fn and against each other).
  SPS noarch suite: 1367 tests (was 1337, +30).
- **NeuroVoice**: `tests/test_session_adapter_optimizer_noarch.py` (+30 noarch tests, new) —
  noarch coverage for AdamW optimizer lifecycle in adapt(): AdamW called once per
  adapt() with lr=cfg.lr and weight_decay=cfg.weight_decay (two custom lr values,
  two weight_decay values confirmed), params arg contains trainable list from
  _unfreeze_target, zero_grad/step called exactly once per training step (counts
  match n_steps and len(loss_curve)), zero_grad always before step (call_log ordering
  confirmed), clip_grad_norm_ triggered when cfg.clip_grad_norm>0 (1e-6 inclusive),
  suppressed when =0.0, called with correct max_norm (3.7 test) and trainable params,
  call count equals step count.
  NeuroVoice noarch suite: 3231 tests (was 3201, +30).
- No new metaMirage/AlphaNexus/rootslink work this session.

---

## [09:30 UTC] Session — satellite

- **SPS**: `tests/test_structured_sensitivity_estimate_noarch.py` (30 tests, new) —
  importlib-based noarch coverage for `StructuredSensitivityEstimator.estimate()` using
  a pass-through torch.no_grad stub and a _FT fake-tensor system: device routing (.to()
  on all three inputs, moved-tensor identity checks), transform_family.sample call
  count (m=0→0, m=3→3) and arg contracts (moved embeddings/input_ids, epsilon),
  model forward invocation (m+1 when all valid, 1 when m=0), divergence_fn gating
  (all-invalid → 0 calls, partial-invalid → only valid count), and max aggregation
  (second larger wins, first larger persists, all-invalid → zeros).
  SPS noarch suite: 1397 tests (was 1367, +30).
- **NeuroVoice**: `tests/test_session_adapter_loss_fn_noarch.py` (30 tests, new) —
  loss_fn invocation contracts for adapt(): loss_fn receives encoder(x) output (not
  raw input, verified by identity), called n_samples × n_steps times total, results
  accumulated as sum then divided by n_samples (mean, not first/last), loss_curve
  entries reflect per-step divided totals, early-return → empty loss_curve.
  NeuroVoice noarch suite: 3261 tests (was 3231, +30).
- No new metaMirage/AlphaNexus/rootslink work this session.

---

## [10:45 UTC] Session — satellite

- **SPS**: `tests/test_sps_get_embeddings_noarch.py` (30 tests, new) —
  SPS noarch suite: 1427 tests (was 1397, +30).
- **NeuroVoice**: `tests/test_session_adapter_warmup_noarch.py` (30 tests, new) —
  NeuroVoice noarch suite: 3291 tests (was 3261, +30).
- No new rootslink work this session.

---

## [12:00 UTC] Session — satellite

- **SPS**: `tests/test_sps_estimate_stats_pipeline_noarch.py` (30 tests, new) —
  SPS noarch suite: 1457 tests (was 1427, +30).
- **NeuroVoice**: `tests/test_session_adapter_patience_noarch.py` (30 tests, new) —
  NeuroVoice noarch suite: 3321 tests (was 3291, +30).
- No new rootslink work this session.
