# rootslink Devlog — 2026-06-11

## [00:15 UTC] Session — R125 (Satellite Tracking)

metaMirage is post-deadline steady state. No new code or tests this session.

### SPS R125
- `tests/test_cross_arch_adversarial_11_noarch.py` (+30, suite 6,088 → 6,118).
- 11th source-expression pass for `experiments/cross_arch_adversarial.py`:
  remaining sps import block names (`build_adversarial_sps_estimator`, `loo_batch_spectral_gap`,
  `permutation_test_gap_diff`, `set_seed`, `spectral_gap`, `spectral_gap_report`),
  `logging.basicConfig` datefmt, `sys.exit(1)`; `collect_spectral_gaps` body — SDPBackend
  import, `with _sdpa_kernel(SDPBackend.MATH):`, `except (ImportError, AttributeError):`,
  `dirs = t_emb.semantic_directions(emb, ids)`, `gaps.extend(result.normalized_gap.tolist())`;
  `run_architecture` residuals — `display_name = arch_name.split("/")[-1]`, SPSConfig call,
  `use_synonyms = bool(arch_synonym_map)`, return ArchResult.

### NeuroVoice R125
- `tests/test_pretrain_source_4_noarch.py` (+30, suite 7,791 → 7,821).
- 4th source-expression pass for `src/training/pretrain.py`:
  module docstring (description + MAE formula), `set_seed` body (random/np/torch/cuda/cudnn),
  data-split formulas (`n_val = max(1, int(...))`, `generator=torch.Generator().manual_seed`),
  DataLoader kwargs (`num_workers=4, pin_memory=True, drop_last=True`), model instantiations,
  `params = list(encoder.parameters()) + list(mae_decoder.parameters())`, `betas=(0.9, 0.95)`,
  `steps_per_epoch`, `pct_start=warmup_steps / max_steps`, `anneal_strategy="cos"`,
  `target = neural.permute(0, 2, 1)`, checkpoint names, `diff = (recon - target) ** 2`.

| Repo | Suite | Delta |
|------|-------|-------|
| SPS | 6,118 | +30 |
| NeuroVoice | 7,821 | +30 |
| Combined | 13,939 | +60 |
| metaMirage | post-deadline | — |

---

## [01:45 UTC] Session — R126 (Satellite Tracking)

metaMirage is post-deadline steady state. No new code or tests this session.

### SPS R126
- `tests/test_cross_arch_adversarial_12_noarch.py` (+30, suite 6,118 → 6,148).
- 12th source-expression pass for `experiments/cross_arch_adversarial.py`:
  `prepare_batches` tokenizer kwargs (`padding=True`, `truncation=True`, `max_length=64`,
  `return_tensors="pt"`), `batches: list[dict] = []`, range loop stride, chunk slice,
  `enc["input_ids"]/enc["attention_mask"]` reads, `batches.append(...)` dict literal;
  `_make_fn`/`_make_mask_fn` lambda bodies (`def _fn(emb: torch.Tensor)`,
  `return model(inputs_embeds=emb, ...).last_hidden_state[:, 0, :]`, `return _fn`,
  count assertions ≥4 defs, `with _sdpa_kernel_adv(SDPBackend.MATH):`,
  `_make_mask_fn(batches[0]["attention_mask"].to(args.device))`);
  `gap_rep["mean/ci_lo/ci_hi/loo_stable"]` key accesses, `spectral_gap_report` call,
  `"%s: gap_mean=%.4f  CI=[%.4f, %.4f]  LOO_stable=%s"` format string,
  `gp['mean/ci_lo/loo_range/loo_stable']` in `print_adversarial_table`.

### NeuroVoice R126
- `tests/test_training_finetune_source_4_noarch.py` (+30, suite 7,821 → 7,851).
- 4th source-expression pass for `src/training/finetune.py`:
  module docstring (objective, MSE formula, Howard & Ruder 2018 citation, LR policy text);
  `_F0_IDX = 6`, `_VOICING_IDX = 8`, `__all__ = ["run_finetune"]`; all 10 `run_finetune`
  signature defaults (lr_encoder 1e-5, lr_decoder 1e-4, early_stop_patience 10,
  max_epochs 100, voicing_loss_weight 0.1, warmup_steps 500, batch_size 16, etc.);
  training loop inline comments (align, MSE, voicing, warmup), section header markers
  (`# ── Model`, `# ── Optimiser with separate LRs`, `# ── Training loop`),
  `val_loss = _validate(...)` full call, `batch["articulatory"].to(dev)`.

| Repo | Suite | Delta |
|------|-------|-------|
| SPS | 6,148 | +30 |
| NeuroVoice | 7,851 | +30 |
| Combined | 13,999 | +60 |
| metaMirage | post-deadline | — |

---

## [10:30 UTC] Session — R127 (Satellite Tracking)

metaMirage is post-deadline steady state. No new code or tests this session.

### SPS R127
- `tests/test_cross_arch_adversarial_13_noarch.py` (+30, suite 6,148 → 6,178).
- 13th source-expression pass for `experiments/cross_arch_adversarial.py`:
  `validate_hypotheses` H1 branch residuals (`print("  Hypothesis Validation ...")`, `if rb:`,
  observed f-string, `if not h1_pass:`, `if ratio < 0.82:`, below/above-range messages,
  `if rb_name in by_name and deb_name in by_name:`, H2 DeBERTa/roberta prints),
  H2/H3 branches (`✓ CONFIRMED/✗ REFUTED` f-string, DeBERTa not-tighter messages, H3 header,
  H3 row f-string), `print_adversarial_table` row format strings (`rp = r.report`,
  `rp.sps_emb:>8.4f/rp.sps_adv:>8.4f/rp.adv_gap_ratio:>7.4f`, spectral gap row),
  main() JSON block (`import json as _json`, `from pathlib import Path as _Path`,
  mkdir, `_out = serialise_results(results)`, `with open`, `_json.dump`, `Results saved →`),
  pairwise loops (`for i in range(len(ranking)):`, `for j in range(i+1, ...)`).

### NeuroVoice R127
- `tests/test_ctc_head_source_2_noarch.py` (+30, suite 7,851 → 7,881).
- 2nd source-expression pass for `src/models/ctc_head.py`:
  `CharTokenizer.__init__` body (`BLANK_INDEX: int = 0`, base string, `chars = list(base) +
  list(extra_chars)`, `seen: set[str]`, `ordered: list[str]`, dedup for-loop,
  `self._idx_to_char = ["<blank>"] + ordered`, `c: i for i, c in enumerate(...)`),
  `encode`/`decode` bodies (`text.lower().strip()`, `out: list[int] = []`, `if c in
  self._char_to_idx:`, append, debug log, `chars: list[str] = []`, blank guard,
  bounds check, `chars.append(self._idx_to_char[idx])`, `return "".join(chars)`),
  `CTCHeadConfig` declarations (`@dataclasses.dataclass(slots=True)`, `encoder_dim: int = 256`,
  `vocab_size: int = 29`, `dropout: float = 0.1`), `logits: torch.Tensor = self.proj(x)`,
  ctc_greedy_decode inner loop (`for token in seq:`, collapse condition, `collapsed.append`,
  `prev = token`), `input_lengths.to(log_probs.device),`.

| Repo | Suite | Delta |
|------|-------|-------|
| SPS | 6,178 | +30 |
| NeuroVoice | 7,881 | +30 |
| Combined | 14,059 | +60 |
| metaMirage | post-deadline | — |

---

## [11:30 UTC] Session — R128 (Satellite Tracking)

metaMirage is post-deadline steady state. No new code or tests this session.

### SPS R128
- `tests/test_adversarial_results_analysis_source_noarch.py` (+30, suite 6,178 → 6,208).
- First source-expression pass for `experiments/adversarial_sps_results_analysis.py`:
  module constants (_BASE/roberta-base, _LARGE/roberta-large, _DEBERTA/deberta-v3-base,
  H1_LO_DEFAULT=0.85, H1_HI_DEFAULT=0.92), `import json`, `from pathlib import Path`,
  `with open(path) as f:`, `return json.load(f)`; `format_results_table` body
  (header string, `{'Model':<35}` column format, `"-" * 82` separator, models loop,
  bootstrap_ci get, CI format string, loo_sym conditional, spectral_gap row get, join return);
  `verify_hypotheses` body (gap_base/large/deb model.get chains, h1/h2/h3 bool expressions,
  n_pass sum, gap_ratio = gap_base / max(gap_large, 1e-12), gap_ratio_roberta_base_large key).

### NeuroVoice R128
- `tests/test_encoder_body_source_noarch.py` (+30, suite 7,881 → 7,911).
- 1st body source-expression pass for `src/models/encoder.py`:
  `_TCNBlock.__init__` body (causal padding formula, Conv1d/LayerNorm/Dropout/GELU init),
  `_TCNBlock.forward` body (conv/causal-trim/rearrange/residual `return out + x`),
  `NeuralEncoder.__init__` (input_proj Conv1d kernel_size=1, tcn ModuleList, dilation=2**i,
  embed_dim/num_heads/batch_first, attn_norm/output_proj/output_norm),
  `NeuralEncoder.forward` (z=input_proj(x), rearrange, attn_out unpack, attn_norm residual,
  output_proj), `MaskedAutoencoderDecoder` init (mask_token Parameter, proj Linear,
  enable_nested_tensor=False, nn.init.normal_ std=0.02) and forward (mask_tokens.expand(B,T,D)).

| Repo | Suite | Delta |
|------|-------|-------|
| SPS | 6,208 | +30 |
| NeuroVoice | 7,911 | +30 |
| Combined | 14,119 | +60 |
| metaMirage | post-deadline | — |

---

## [05:18 UTC] Session — R129 (Satellite Tracking)

metaMirage is post-deadline steady state. No new code or tests this session.

### SPS R129
- `tests/test_adversarial_results_analysis_source_2_noarch.py` (+30, suite 6,208 → 6,238).
- 2nd source-expression pass for `experiments/adversarial_sps_results_analysis.py`:
  `verify_hypotheses` H1/H2/H3 format strings, return dict spectral_gaps sub-keys;
  `build_summary` body (verify_hypotheses call, H1/H2/H3 hyps keys, gap_ratio float,
  roberta_base/large/deberta_v3_base floats, run_config conditional);
  `format_run_report` body (table/hyp_report calls, concat return);
  `main()` body (argv check, Usage print, load_results call).

### NeuroVoice R129
- `tests/test_session_adapt_source_noarch.py` (+30, suite 7,911 → 7,941).
- 1st source-expression pass for `src/personalization/session_adapt.py`:
  imports, SessionAdaptConfig fields, AdaptResult NamedTuple fields,
  SessionAdapter.__init__ body, adapt() body (AdamW/best_loss/t0/patience/clip_grad_norm),
  save_state deepcopy, recommend_strategy guard, _unfreeze_target TCNBlock check/top_k loop.

| Repo | Suite | Delta |
|------|-------|-------|
| SPS | 6,238 | +30 |
| NeuroVoice | 7,941 | +30 |
| Combined | 14,179 | +60 |
| metaMirage | post-deadline | — |

---

## [12:00 UTC] Session — R130 (Satellite Tracking)

metaMirage is post-deadline steady state. No new code or tests this session.

### SPS R130
- `tests/test_bootstrap_sign_flip_source_3_noarch.py` (+30, suite 6,238 → 6,268).
- 3rd source-expression pass for `experiments/bootstrap_sign_flip.py`: `analyse()` signature
  and return dict residuals ("label", "r", "bootstrap_ci95", "permutation_p_two_sided", "loo"),
  `permutation_p` signature, INLINE_DATA key residuals ("aq_clean", "tdr_forced_abstention",
  "tdr_expertise_trap"), main() data list-comps, print loop, 4 analyse() calls in rows.

### NeuroVoice R130
- `tests/test_session_adapt_source_2_noarch.py` (+30, suite 7,941 → 7,971).
- 2nd source-expression pass for `src/personalization/session_adapt.py`: `_freeze_all` body,
  `_unfreeze_target` LayerNorm scan, `rollback` body, adapt() early-return path, AdaptResult
  return kwargs, adapt() loop init/accumulation residuals.

| Repo | Suite | Delta |
|------|-------|-------|
| SPS | 6,268 | +30 |
| NeuroVoice | 7,971 | +30 |
| Combined | 14,239 | +60 |
| metaMirage | post-deadline | — |

---

## [07:15 UTC] Session — R131 (Satellite Tracking)

rootslink is in steady state. No new code or tests this session.

### SPS R131
- `tests/test_spectral_gap_loo_analysis_source_2_noarch.py` (+30, suite 6,268 → 6,298).

### NeuroVoice R131
- `tests/test_correction_store_source_noarch.py` (+30, suite 7,971 → 8,001). Suite crossed 8,000.

| Repo | Suite | Delta |
|------|-------|-------|
| SPS | 6,298 | +30 |
| NeuroVoice | 8,001 | +30 |
| Combined | 14,299 | +60 |

---

## [08:11 UTC] Session — R132 (Satellite Tracking)

### SPS R132
- `tests/test_spectral_gap_loo_analysis_source_3_noarch.py` (+30, suite 6,298 → 6,328).
- 3rd source pass for `spectral_gap_loo_analysis.py`: validate_h3 residuals, main() arch-loop,
  cross-arch comparison, pairwise permutation sections.

### NeuroVoice R132
- `tests/test_on_device_source_noarch.py` (+30, suite 8,001 → 8,031).
- 1st source pass for `src/personalization/on_device.py`.

| Repo | Suite | Delta |
|------|-------|-------|
| SPS | 6,328 | +30 |
| NeuroVoice | 8,031 | +30 |
| Combined | 14,359 | +60 |

---

## [HH:MM UTC] Session — R133 (Satellite Tracking)

### SPS R133
- `tests/test_estimate_sps_source_2_noarch.py` (+30, suite 6,328 → 6,358).

### NeuroVoice R133
- `tests/test_progress_federated_source_noarch.py` (+30, suite 8,031 → 8,061).

| Repo | Suite | Delta |
|------|-------|-------|
| SPS | 6,358 | +30 |
| NeuroVoice | 8,061 | +30 |
| Combined | 14,419 | +60 |
| metaMirage | post-deadline | — |

---

## [10:13 UTC] Session — R134 (Satellite Tracking)

### SPS R134
- `tests/test_jacobian_source_2_noarch.py` (+30, suite 6,358 → 6,388).
- 2nd source pass for `src/sps/jacobian.py`: module imports, logger, __all__ residuals,
  __repr__ f-string fragments, restricted_operator_norm for-k loop body, adversarial_worst_direction
  gather expression, full_spectral_norm loop residuals, spectral_gap return kwargs, verify_spectral_gap_bound theorem3.

### NeuroVoice R134
- `tests/test_inference_source_noarch.py` (+30, suite 8,061 → 8,091).
- 1st source pass for `src/serving/inference.py`: StreamingDecoder imports, __all__, docstring
  fragments, __init__/push/infer/reset/decode_offline body expressions.

| Repo | Suite | Delta |
|------|-------|-------|
| SPS | 6,388 | +30 |
| NeuroVoice | 8,091 | +30 |
| Combined | 14,479 | +60 |
| metaMirage | post-deadline | — |

---

## [11:12 UTC] Session — R135 (Satellite Tracking)

metaMirage is post-deadline steady state. No new code or tests this session.

### SPS R135
- `tests/test_transformations_source_3_noarch.py` (+30, suite 6,388 → 6,418).
- 3rd source-expression pass for `src/sps/transformations.py`.

### NeuroVoice R135
- `tests/test_serving_session_adapt_source_noarch.py` (+30, suite 8,091 → 8,121).
- 1st source-expression pass for `src/serving/session_adapt.py`.

| Repo | Suite | Delta |
|------|-------|-------|
| SPS | 6,418 | +30 |
| NeuroVoice | 8,121 | +30 |
| Combined | 14,539 | +60 |
| metaMirage | post-deadline | — |

---

## [12:22 UTC] Session — R136 (Satellite Tracking)

metaMirage is post-deadline steady state. No new code or tests this session.

### SPS R136
- `tests/test_spectral_gap_loo_analysis_source_4_noarch.py` (+30, suite 6,418 → 6,448).
- 4th source-expression pass for `experiments/spectral_gap_loo_analysis.py`.

### NeuroVoice R136
- `tests/test_emg2pose_dataset_source_noarch.py` (+30, suite 8,121 → 8,151).
- 1st source-expression pass for `src/data/emg2pose_dataset.py`.

| Repo | Suite | Delta |
|------|-------|-------|
| SPS | 6,448 | +30 |
| NeuroVoice | 8,151 | +30 |
| Combined | 14,599 | +60 |
| metaMirage | post-deadline | — |

## [02:30 UTC] Session — R137 (satellite)

### R137 Progress (SPS + NeuroVoice)
- SPS R137: `tests/test_cross_arch_adversarial_source_2_noarch.py` (+30, suite 6,448 → 6,478).
  2nd source-expression pass for `experiments/cross_arch_adversarial.py`.
- NeuroVoice R137: `tests/test_gaddy_dataset_source_noarch.py` (+30, suite 8,151 → 8,181).
  1st source-expression pass for `src/data/gaddy_dataset.py` (455 lines).

| Repo | Suite | Delta |
|------|-------|-------|
| SPS | 6,478 | +30 |
| NeuroVoice | 8,181 | +30 |
| Combined | 14,659 | +60 |
| metaMirage | post-deadline | — |

---

## [13:16 UTC] Session — R138 (Satellite Tracking)

metaMirage is post-deadline steady state. No new code or tests this session.

### SPS R138
- `tests/test_metrics_source_3_noarch.py` (+30, suite 6,478 → 6,508).
- 3rd source-expression pass for `src/sps/metrics.py`:
  LayerwiseSPSAnalyzer.__init__ body, compute_profile NotImplementedError stub,
  compute_profile_from_batches loop, _compute_sps_at_layer hook internals (_LayerProxy,
  captured dict, isinstance/detach branches, register_forward_hook, handle.remove),
  full_sps_analysis spectral gap block (batch.get/raise/semantic_directions/_fn/
  normalized_gap.tolist/bootstrap_ci), relative block, layerwise block.

### NeuroVoice R138
- `tests/test_gaddy_dataset_source_2_noarch.py` (+30, suite 8,181 → 8,211).
- 2nd source-expression pass for `src/data/gaddy_dataset.py`:
  GADDY_CHANNEL_NAMES, _reshape_emg body, _normalize_text re.sub,
  __getitem__ body, vocabulary_stats return dict, top_n_words sorted lambda,
  utterances_in_vocab enumerate/issubset, gaddy_collate_fn output residuals.

| Repo | Suite | Delta |
|------|-------|-------|
| SPS | 6,508 | +30 |
| NeuroVoice | 8,211 | +30 |
| Combined | 14,719 | +60 |
| metaMirage | post-deadline | — |
