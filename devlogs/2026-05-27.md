# 2026-05-27 — Mirror: NeuroVoice +30 tests (MAE pre-training + EMG adaptation)

*This devlog mirrors activity from the primary NeuroVoice repository.*

## Summary

Two test modules added to NeuroVoice today: total +30 tests.

### `tests/test_pretrain.py` (20 tests) — commit d29e2be6

MAE pre-training loop (`src/training/pretrain.py`) was fully implemented but had
zero test coverage. Added `_ToyDataset` helper (no HDF5) then:

- **TestMaskedMSE (6):** `_masked_mse` unit tests — perfect recon = 0, unmasked
  ignored, gradient flows, all-mask equals full MSE
- **TestRunPretrainSmoke (4):** 2-step runs verify return type, CPU, config match,
  checkpoint written
- **TestRunPretrainCheckpoints (5):** `checkpoint_every=3, max_steps=6`;
  `best_encoder.pt` (triggered at step 3 since initial best=inf) and
  `final_encoder.pt` both written
- **TestRunPretrainOutput (5):** shape `(2, T, output_dim)`, finite, eval-deterministic,
  decoder recon finite, weights differ from init

### `tests/integration/test_emg_adapt_pipeline.py` (10 tests) — commit 20ddc35c

First integration test linking the full session-adaptation pipeline:
`GaddyCalibrationDataset → build_calibration_batches → SessionAdapter.adapt → rollback`

- **TestSyntheticCalibrationPipeline (5):** fast/deep adapt, rollback exact equality,
  finite encoder output, window shape `(1, 8, 500)`
- **TestAdaptationConvergence (5):** FA loss non-increasing, temporal loss finite,
  `recommend_strategy` heuristic (all branches), `n_params_fast < n_params_deep`,
  combined FA+TC loss end-to-end

## Other projects

- **SPS:** stable; adversarial SPS notebook pending Kaggle GPU run
- **metaMirage:** submission-ready; manual Kaggle steps pending
